---
title: "Sparse but Critical: A Token-Level Analysis of Distributional Shifts in RLVR Fine-Tuning of LLMs"
title_zh: 稀疏但关键：大语言模型 RLVR 微调中分布偏移的 Token 级分析
authors: "Haoming Meng, Kexin Huang, Shaohang Wei, Chiyu Ma, Shuo Yang, Xue Wang, Guoyin Wang, Bolin Ding, Jingren Zhou"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22446v1"
tags: ["query:rl-tl"]
score: 10.0
evidence: 具有可验证奖励的强化学习 (RLVR) 的标记级分析
tldr: 本研究深入探讨了带验证奖励的强化学习（RLVR）在微调大语言模型时的Token级分布偏移。研究发现，RL微调引起的分布变化具有高度稀疏性和针对性，仅极少数关键Token的概率分布发生了显著变化。通过交叉采样干预实验，作者证明了这些稀疏的Token决策是性能提升的核心原因：在基础模型生成中插入少量RL采样的Token即可大幅提升性能，反之则会导致性能崩溃。该研究为理解RLVR的微调机制提供了细粒度的视角。
selection_source: fresh_fetch
motivation: 旨在揭示RLVR微调如何在大语言模型的Token级别诱导分布偏移，并探究这些微观变化与宏观推理性能提升之间的因果关系。
method: 采用Token级分布特征分析、跨采样干预实验（在基础模型和RL模型间交换Token决策）以及散度加权优势信号等手段进行系统研究。
result: 发现RL微调仅引起了极少数Token分布的显著变化，但这些稀疏的改变对推理性能至关重要，少量关键Token的替换即可决定生成序列的成败。
conclusion: RLVR微调是一个高度针对性的精炼过程，通过优化少数关键决策点而非全局分布偏移，实现了模型推理能力的显著提升。
---

## 摘要
带可验证奖励的强化学习（RLVR）显著提升了大语言模型（LLMs）的推理能力，但这些提升背后的 Token 级机制仍不明确。我们对 RLVR 的分布效应进行了系统的实证研究，主要围绕三个核心分析展开：(1) 基础模型与 RL 模型之间分布偏移的 Token 级特征描述；(2) 通过交叉采样干预研究 Token 级分布偏移对序列级推理性能的影响；(3) 这些偏移在 Token 级的细粒度机制。我们发现，RL 微调诱导了高度稀疏且具针对性的变化，仅有极小比例的 Token 分布在基础策略与 RL 策略之间表现出显著差异。我们通过分析 Token 熵、位置集中度和概率质量重分配，进一步刻画了这些偏移的结构与演变。为了评估这些稀疏变化的泛函重要性，我们进行了交叉采样实验，在不同的干预预算下有选择地交换基础模型与 RL 模型之间的 Token 选择。结果表明，仅将极小比例的 RL 采样 Token 插入基础模型的生成序列中，即可逐步恢复 RL 的性能增益；而将同样少量的基础模型 Token 选择注入 RL 生成的序列中，则会导致性能坍缩至基础模型水平，从而分离出一组直接负责 RLVR 性能增益的少量 Token 级决策。最后，我们探索了优势信号的散度加权变体作为一种诊断性干预，发现它们能够取得优于基准的效果。总之，我们的研究结果揭示了 RLVR 诱导的分布变化，并为将 RLVR 微调理解为一个有针对性的精炼过程提供了一个细粒度的 Token 级视角。

## Abstract
Reinforcement learning with verifiable rewards (RLVR) has significantly improved reasoning in large language models (LLMs), yet the token-level mechanisms underlying these improvements remain unclear. We present a systematic empirical study of RLVR's distributional effects organized around three main analyses: (1) token-level characterization of distributional shifts between base and RL models, (2) the impact of token-level distributional shifts on sequence-level reasoning performance through cross-sampling interventions, and (3) fine-grained mechanics of these shifts at the token level. We find that RL fine-tuning induces highly sparse and targeted changes, with only a small fraction of token distributions exhibiting meaningful divergence between the base and RL policies. We further characterize the structure and evolution of these shifts through analyses of token entropy, positional concentration, and reallocation of probability mass. To assess the functional importance of these sparse changes, we conduct cross-sampling experiments that selectively swap token choices between the base and RL models with varying intervention budgets. We show that inserting only a small fraction of RL-sampled tokens into base generations progressively recovers RL performance gains, while injecting a similarly small number of base token choices into otherwise RL-generated sequences collapses performance to base levels, isolating a small set of token-level decisions directly responsible for RLVR's performance gains. Finally, we explore divergence-weighted variants of the advantage signal as a diagnostic intervention, finding that they can yield improvements over baselines. Together, our results shed light on the distributional changes induced by RLVR and provide a fine-grained, token-level lens for understanding RLVR fine-tuning as a targeted refinement process.

---

## 论文详细总结（自动生成）

这篇论文由阿里巴巴 Qwen 团队发布，深入探讨了强化学习（RL）微调大语言模型（LLM）的内在机制。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：虽然带可验证奖励的强化学习（RLVR，如 GRPO、DAPO）显著提升了模型的推理能力，但这种提升在 **Token（标记）级别** 是如何实现的？是全局性的分布改变，还是局部关键点的微调？
*   **研究动机**：目前的评估多集中在宏观指标（如准确率、响应长度），缺乏对模型行为改变的细粒度理解。揭示 RLVR 如何重塑 Token 预测分布，有助于设计更高效、更可控的训练方法。

### 2. 方法论：核心思想与技术细节
论文通过三个维度构建分析框架：
*   **分布偏移表征**：使用 **Jensen-Shannon (JS) 散度** 量化基础模型 ($\pi_{base}$) 与 RL 模型 ($\pi_{RL}$) 在相同上下文下的预测分布差异。
*   **交叉采样干预（Cross-Sampling）**：
    *   **正向干预**：在基础模型的生成过程中，仅在散度高的位置插入 RL 模型的 Token，观察性能恢复情况。
    *   **反向干预**：在 RL 模型的生成过程中，在散度高的位置换回基础模型的 Token，观察性能退化情况。
*   **细粒度机制分析**：研究高散度位置的 Top-k 候选集重合度、排名变化以及概率质量的重新分配。
*   **散度加权优势信号（Divergence-Weighted Advantage）**：提出一种实验性目标函数，根据 Token 级的 KL 散度对强化学习的优势信号（Advantage）进行加权，以引导模型关注特定区域。

### 3. 实验设计
*   **模型**：Qwen2.5-32B（核心）、Qwen2.5-Math-7B、Qwen3-8B、Mistral-Small-24B。
*   **RL 方法**：DAPO（解耦剪裁与动态采样）、SimpleRL（基于 GRPO 的变体）。
*   **数据集**：数学竞赛（AIME 2024/2025、AMC）、科学推理（GPQA-Diamond）。
*   **对比基准**：基础模型（Base）、监督微调模型（SFT）。

### 4. 资源与算力
*   **算力消耗**：论文在附录中提到，Qwen3-32B-Base 的一次完整训练（约 500 步）消耗了超过 **140,000 GPU 小时**。
*   **框架**：使用了 `verl` 训练框架和 `vllm` 推理引擎。
*   **细节**：虽然未逐一列出每组实验的 GPU 型号，但从模型规模（32B）和训练时长来看，实验依赖于大规模 A100/H100 集群。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 7B 到 32B 的多种架构，对比了不同的 RL 算法（DAPO vs SimpleRL）和微调范式（RL vs SFT）。
*   **充分性**：实验设计非常详尽，不仅有宏观的准确率对比，还有大量的 Token 级统计分析（如词云、熵分布、位置热力图）。
*   **客观性**：通过正反向交叉采样建立了因果关系，证明了“稀疏 Token”的决定性作用，实验逻辑严密且具有统计显著性。

### 6. 主要结论与发现
*   **稀疏性（Sparsity）**：RLVR 引起的变化极度稀疏。在 SimpleRL 中，超过 98% 的 Token 分布几乎没有变化；即使是不带 KL 惩罚的 DAPO，也有 80% 以上的 Token 保持不变。
*   **关键性（Criticality）**：这些极少数的变化决定了成败。实验显示，仅修改约 **1.5% - 10%** 的关键 Token，就能让基础模型达到 RL 模型的推理水平，或让 RL 模型退化回基础水平。
*   **精炼而非创造**：在高散度位置，RL 并没有创造全新的 Token，而是对基础模型原本就有的候选 Token 进行了**重新排序和概率重分配**（通常是将原本排在 Top-3 的正确路径提拔到 Top-1）。
*   **位置特征**：分布偏移主要集中在序列的**开头**（决定思路方向）和**结尾**（格式化答案），中间过程相对稳定。

### 7. 优点
*   **视角独特**：从微观的 Token 分布角度解释了宏观的推理能力提升，填补了 RLVR 机制研究的空白。
*   **因果论证强**：交叉采样实验有力地证明了分布偏移与性能提升之间的因果联系，而非简单的相关性。
*   **泛化性好**：结论在多个模型家族（Qwen, Mistral）和多个任务（数学、科学）上保持一致。

### 8. 不足与局限
*   **任务局限性**：研究主要集中在具有“可验证奖励”的推理任务（数学、逻辑），对于创意写作、对话等主观性任务的适用性尚未验证。
*   **训练干预尚处早期**：提出的散度加权优势信号虽然有效，但仍属于诊断性工具，尚未形成一套成熟的、可广泛替代标准 RL 的新算法。
*   **算力门槛高**：论文揭示的某些现象（如性能在训练后期的突增）需要极高的算力投入才能观察到，普通研究者难以复现。

（完）
