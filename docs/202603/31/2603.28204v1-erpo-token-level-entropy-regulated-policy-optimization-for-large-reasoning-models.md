---
title: "ERPO: Token-Level Entropy-Regulated Policy Optimization for Large Reasoning Models"
title_zh: ERPO：大推理模型的标记级熵正则化策略优化
authors: "Song Yu, Li Li"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28204v1"
tags: ["query:rl-tl"]
score: 9.0
evidence: 针对推理任务的可验证奖励强化学习 (RLVR)
tldr: 针对大语言模型推理中的奖励分配不均问题，本文提出ERPO。传统GRPO采用序列级奖励，导致熵过早塌陷和冗余生成。ERPO通过识别关键决策点（CDP），引入熵感知门控、桶式隐式归一化和结果锚定优势合成，实现Token级的精细化策略优化。实验证明，ERPO在MATH和AIME等基准上显著提升了推理准确性与路径简洁度。
selection_source: fresh_fetch
motivation: 传统的序列级奖励分配忽略了推理链中的Token异质性，导致模型在关键决策点缺乏有效探索并产生冗余路径。
method: 提出ERPO框架，包含熵感知门控以增强关键点探索、桶式归一化缓解难度偏差以及结果锚定的Token级优势合成。
result: 在MATH和AIME等数学竞赛级基准测试中，ERPO在准确率和推理效率上均显著优于GRPO。
conclusion: ERPO通过Token级的熵调节优化，为大型推理模型建立了一个兼顾准确性与简洁性的新前沿。
---

## 摘要
基于可验证奖励的强化学习 (RLVR) 显著提升了大语言模型的推理能力。然而，标准的群体相对策略优化 (GRPO) 通常为所有标记分配统一的序列级优势值，从而忽略了推理链中内在的信息异质性。我们表明，这种粗粒度的信用分配会导致过早的熵坍缩，并促使模型生成冗余且低质量的推理路径。通过系统的实证分析，我们识别出了关键决策枢纽 (CDPs)：即策略轨迹对扰动最敏感的瞬时高熵状态。这些枢纽代表了“分叉路口”，在这些位置，有效的多路径探索至关重要，但往往被统一的优势信号所抑制。基于这些见解，我们提出了熵正则化策略优化 (ERPO)，将优化重点从粗粒度序列转向细粒度标记动态。ERPO 引入了三个协同组件：(i) 熵感知门控，自适应地放大 CDP 处的探索，以促进多样化路径的发现；(ii) 基于桶的隐式归一化，通过对齐标记进度窗口来减轻难度偏差；(iii) 结果锚定优势综合，通过结果驱动的锚点重新加权标记级信号。在具有竞争力的数学基准测试（如 MATH、AIME）上的广泛实验表明，ERPO 显著优于 GRPO。值得注意的是，ERPO 不仅提高了推理准确性，还产生了显著更简洁且鲁棒的推导路径，为大推理模型建立了新的效率-准确性前沿。

## Abstract
Reinforcement learning from verifiable rewards (RLVR) has significantly advanced the reasoning capabilities of large language models. However, standard Group Relative Policy Optimization (GRPO) typically assigns a uniform, sequence-level advantage to all tokens, thereby overlooking the intrinsic information heterogeneity along reasoning chains. We show that this coarse-grained credit assignment leads to premature entropy collapse and encourages the model to generate redundant, low-quality reasoning paths. Through systematic empirical analysis, we identify Critical Decision Pivots (CDPs): transient high-entropy states where the policy's trajectory is most sensitive to perturbations. These pivots represent the "forks in the road" where effective multi-path exploration is most crucial yet often suppressed by uniform advantage signals. Building on these insights, we propose Entropy-Regulated Policy Optimization (ERPO), which transitions the optimization focus from coarse sequences to fine-grained token dynamics. ERPO introduces three synergistic components: (i) Entropy-aware Gating, which adaptively amplifies exploration at CDPs to facilitate diverse path discovery; (ii) Bucket-based Implicit Normalization, which mitigates difficulty bias by aligning token progress windows; and (iii) Result-anchored Advantage Synthesis, which re-weights token-level signals via outcome-driven anchors. Extensive experiments on competitive mathematical benchmarks (e.g., MATH, AIME) demonstrate that ERPO significantly outperforms GRPO. Notably, ERPO not only boosts reasoning accuracy but also yields significantly more concise and robust derivation paths, establishing a new efficiency-accuracy frontier for large reasoning models.

---

## 论文详细总结（自动生成）

### ERPO：大推理模型的标记级熵正则化策略优化 论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：现有的推理模型强化学习方法（如 DeepSeek 提出的 GRPO）通常采用**序列级（Sequence-level）**的奖励分配方式，即给一条推理链中的所有标记（Token）分配统一的优势值（Advantage）。
*   **研究动机**：
    *   **信息异质性**：推理链中的 Token 贡献并不相等。作者识别出“关键决策枢纽”（Critical Decision Pivots, CDPs），即模型面临逻辑分支、高熵状态的时刻，这些点对推理成败至关重要。
    *   **缺陷**：粗粒度的信用分配会导致“熵塌陷”（模型过早失去探索能力）和“冗余生成”（模型为了获取奖励而生成大量无用的推导步骤）。
    *   **目标**：实现 Token 级的精细化信用分配，在关键决策点加强探索，在确定性步骤抑制冗余。

#### 2. 方法论：ERPO 核心思想与技术细节
ERPO（Entropy-Regulated Policy Optimization）将优化重心从粗粒度序列转向细粒度标记动态，包含三个核心组件：
*   **熵感知门控（Entropy-aware Gating）**：
    *   利用 Token 级的策略熵（Entropy）作为不确定性的指标。
    *   通过自适应门控函数放大高熵 Token（即 CDPs）的优势信号，引导模型在逻辑分叉路口进行更多探索。
*   **桶式隐式归一化（Bucket-based Implicit Normalization）**：
    *   推理过程具有阶段性（如计划、推导、验证）。为了公平比较，ERPO 将推理序列按进度划分为 $K$ 个时间桶（Buckets）。
    *   仅在相同进度窗口内的 Token 之间进行优势值归一化，消除因推理阶段不同带来的难度偏差。
*   **结果锚定优势合成（Result-anchored Advantage Synthesis）**：
    *   引入隐式进度信号（当前策略与参考策略的 log 概率差）。
    *   **结果锚定机制**：使用序列最终结果的符号（正/负）作为“护栏”，确保即使是高置信度的 Token，如果最终导致错误答案，也会受到惩罚，防止“奖励黑客”（Reward Hacking）行为。

#### 3. 实验设计
*   **模型规模**：基于 Qwen2.5 系列（1.5B、3B、7B）进行实验。
*   **训练数据**：使用 MATH 数据集（难度 3-5 级）进行强化学习训练。
*   **评估基准（Benchmarks）**：
    *   AMC23、AIME24、AIME25（高难度数学竞赛）。
    *   Minerva（通用定量推理）。
*   **对比方法**：Base 模型、SFT（监督微调）模型、Instruct 官方指令微调模型、以及标准的 GRPO 算法。

#### 4. 资源与算力
*   **训练技术**：采用 LoRA（Rank=32）进行高效微调，针对所有线性层。
*   **超参数**：Global Batch Size 为 16，每条 Prompt 生成 $G=8$ 条响应，最大序列长度 2048。
*   **算力消耗**：
    *   文中提到 7B 模型在特定配置下的总训练时长约为 **14.46 小时**（与 GRPO 的 14.02 小时相当）。
    *   **未明确说明**具体的 GPU 型号（如 A100 或 H100）及具体使用的显卡数量。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1.5B 到 7B 的三个参数量级，并在四个独立的数学竞赛级数据集上进行了验证。
*   **消融与动态分析**：提供了详细的训练曲线（准确率、熵、奖励、KL 散度、梯度范数）以及推理长度演变曲线。
*   **充分性评价**：实验设计较为充分，通过 Pass@k 等指标展示了模型在多样本采样下的扩展能力。对比实验涵盖了从基础模型到最先进的开源 RL 算法，具有较强的客观性和说服力。

#### 6. 主要结论与发现
*   **性能超越**：ERPO 在所有规模上均显著优于 GRPO。7B 规模的 ERPO 模型在某些指标上甚至超越了参数量大得多的商业模型（如 DeepSeek-R1-671B 的早期版本或 Qwen3-235B-Instr）。
*   **推理效率**：ERPO 能够生成更简洁的推理路径。在保持更高准确率的同时，平均生成 Token 数显著低于 GRPO，实现了更好的“准确性-效率”帕累托前沿。
*   **防止塌陷**：ERPO 有效缓解了强化学习后期的熵塌陷问题，保持了模型生成的多样性和鲁棒性。

#### 7. 优点与亮点
*   **无需额外奖励模型**：ERPO 仅利用模型自身的熵和参考模型的概率差来构建 Token 级信号，不需要训练额外的过程奖励模型（PRM），降低了实现门槛。
*   **精细化信用分配**：成功识别并利用了 CDPs（关键决策枢纽），解决了 RLVR 中长期存在的“稀疏奖励”和“信用分配模糊”问题。
*   **推理简洁性**：通过抑制低熵区域的冗余，自动诱导模型生成“干货”更多的推理链。

#### 8. 不足与局限
*   **小模型初期波动**：在 1.5B 规模模型上，ERPO 初期表现出探索滞后，准确率上升较慢，说明该机制对模型底座的逻辑稳定性有一定要求。
*   **领域局限性**：实验主要集中在数学推理任务上，尚未在代码生成、创意写作或多模态任务中验证其通用性。
*   **硬件细节缺失**：未公布具体的硬件拓扑和显卡型号，不便于其他研究者精确评估复现成本。

（完）
