---
title: Bridging Visual Representation and Reinforcement Learning from Verifiable Rewards in Large Vision-Language Models
title_zh: 在大视觉语言模型中桥接视觉表示与基于可验证奖励的强化学习
authors: "Yuhang Han, Yuyang Wu, Zhengbo Jiao, Yiyu Wang, Xuyang Liu, Shaobo Wang, Hanlin Xu, Xuming Hu, Linfeng Zhang"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27375v1"
tags: ["query:rl-tl"]
score: 9.0
evidence: 用于推理的可验证奖励强化学习 (RLVR)
tldr: 本研究针对大视觉语言模型（LVLM）在可验证奖励强化学习（RLVR）中存在的视觉表示瓶颈问题，提出了KAWHI插件式奖励重权机制。该方法通过分层几何聚合定位关键视觉区域，并利用结构化归因识别视觉关键注意力头，实现段落级的信用重新分配，从而将空间视觉证据与推理步骤对齐。实验证明，KAWHI能显著提升GRPO和GSPO等优化方法在多模态推理任务中的表现，增强了模型的复杂推理能力。
selection_source: fresh_fetch
motivation: 现有LVLM的强化学习方法缺乏对视觉信息的显式建模，导致视觉表示与优化过程脱节，限制了多模态推理性能的进一步提升。
method: 提出KAWHI机制，通过分层几何聚合定位显著区域并结合结构化归因进行段落级奖励重权分配，实现视觉证据与推理步骤的精准对齐。
result: 在多个推理基准测试中，KAWHI作为通用增强模块，显著且一致地提高了GRPO和GSPO等主流强化学习优化方法的性能。
conclusion: KAWHI有效地桥接了视觉表示与强化学习优化过程，为提升大视觉语言模型在多模态任务中的推理效率和准确性提供了有效方案。
---

## 摘要
基于可验证奖励的强化学习（RLVR）已显著增强了大语言模型在抽象推理任务中的推理能力。然而，其在大视觉语言模型（LVLMs）中的应用仍受限于结构性表示瓶颈。现有方法通常缺乏对视觉信息的显式建模和有效利用，导致视觉表示无法与强化学习优化过程紧密耦合，从而限制了多模态推理性能的进一步提升。为解决这一局限性，我们提出了 KAWHI（关键区域对齐加权调和激励），这是一种即插即用的奖励重加权机制，可将结构化视觉信息显式地融入统一奖励策略优化方法（如 GRPO 和 GSPO）中。该方法通过分层几何聚合自适应地定位语义显著区域，通过结构化归因识别视觉关键注意力头，并进行段落级信用重新分配，以使空间视觉证据与语义决定性的推理步骤相对齐。在多种推理基准上的广泛实证评估证实，KAWHI 是一种通用的增强模块，能够持续提升各种统一奖励优化方法的性能。项目主页：KAWHI (https://kawhiiiileo.github.io/KAWHI_PAGE/)

## Abstract
Reinforcement Learning from Verifiable Rewards (RLVR) has substantially enhanced the reasoning capabilities of large language models in abstract reasoning tasks. However, its application to Large Vision-Language Models (LVLMs) remains constrained by a structural representational bottleneck. Existing approaches generally lack explicit modeling and effective utilization of visual information, preventing visual representations from being tightly coupled with the reinforcement learning optimization process and thereby limiting further improvements in multimodal reasoning performance. To address this limitation, we propose KAWHI (Key-Region Aligned Weighted Harmonic Incentive), a plug-and-play reward reweighting mechanism that explicitly incorporates structured visual information into uniform reward policy optimization methods (e.g., GRPO and GSPO). The method adaptively localizes semantically salient regions through hierarchical geometric aggregation, identifies vision-critical attention heads via structured attribution, and performs paragraph-level credit reallocation to align spatial visual evidence with semantically decisive reasoning steps. Extensive empirical evaluations on diverse reasoning benchmarks substantiate KAWHI as a general-purpose enhancement module, consistently improving the performance of various uniform reward optimization methods. Project page: KAWHI (https://kawhiiiileo.github.io/KAWHI_PAGE/)

---

## 论文详细总结（自动生成）

以下是对论文《Bridging Visual Representation and Reinforcement Learning from Verifiable Rewards in Large Vision-Language Models》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：虽然基于可验证奖励的强化学习（RLVR，如 GRPO）显著提升了语言模型（LLM）的推理能力，但在大视觉语言模型（LVLM）中效果受限。
*   **核心瓶颈**：作者发现 LVLM 在多模态推理中存在“结构性表示瓶颈”。现有的 RL 训练将视觉 token 视为均匀分布的密集编码，忽略了视觉信息（如数学图表、几何图形）在空间上的**稀疏性**和**任务相关性**。
*   **诊断分析**：通过对 Qwen2.5-VL 的错误归因分析发现，**48.9% 的失败源于视觉感知错误（VP）**，远高于计算错误或规则应用错误。这表明视觉表示与 RL 优化过程之间缺乏有效的耦合。

### 2. 方法论：KAWHI 机制
作者提出了 **KAWHI (Key-Region Aligned Weighted Harmonic Incentive)**，一种即插即用的奖励重权模块，核心步骤如下：

*   **SGUF (结构引导的并查集算法)**：
    *   利用**结构张量（Structure Tensor）**提取图像的局部几何特征（如线条、符号边缘）。
    *   通过**并查集（Union-Find）**进行分层聚合，自适应地识别出语义显著的关键视觉区域（Key Regions），从而过滤掉冗余的背景信息。
*   **视觉关键头识别（Vision-Critical Head Identification）**：
    *   通过在 MME 基准上进行全局消融实验，识别出对视觉感知贡献最大的特定注意力头，将其作为空间对齐的锚点。
*   **空间感知信用分配（Spatially-Aware Credit Assignment）**：
    *   将生成的文本 token 视为“查询（Query）”，将 SGUF 选定的关键视觉 token 视为“键（Key）”。
    *   计算两者的余弦相似度，量化推理步骤对关键视觉证据的关注程度。
*   **段落级奖励重权（Paragraph-level Reweighting）**：
    *   为了保持语义完整性，将响应按段落（`\n\n`）分割。
    *   根据段落内 token 的平均空间对齐得分计算权重，并对 RL 的优势函数（Advantage）进行调制，使关注关键视觉区域的推理步骤获得更高的奖励。

### 3. 实验设计
*   **数据集与场景**：
    *   **数学推理**：训练集使用 Geo3K；评估集包括 MathVista, MathVerse, MathVision, WeMath。
    *   **图表理解**：训练集使用 ChartQA 子集；评估集包括 ChartXiv (Desc/Rea), ChartQA, ChartQA-Pro, ChartMimic。
*   **基准模型（Backbones）**：Qwen2.5-VL-7B-Instruct 和 Qwen3-VL-4B-Instruct。
*   **对比方法**：
    *   **细粒度奖励方法**：Step-GRPO, FT-RL, VPPO。
    *   **统一奖励方法（Baseline）**：GRPO, DAPO, GSPO。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA H200 GPU**。
*   **训练细节**：
    *   采用 FSDP（全分片数据并行）提高效率。
    *   GRPO/GSPO 配置：学习率 1e-6，Batch Size 512。
    *   DAPO 配置：Batch Size 1024。
    *   最大响应长度：2048 tokens。
*   **效率分析**：KAWHI 引入的额外计算开销仅为 **3.6%**（单步时长从 521s 增至 540.6s），证明了其高效性。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 2 个不同架构的模型、9 个主流多模态基准测试。
*   **消融实验**：进行了 5 组详细的消融实验，验证了区域选择（SGUF）、奖励指标（Q-K 匹配）、响应粒度（段落级 vs token 级）、关键头选择以及位置编码的影响。
*   **客观性**：通过“LLM-as-a-judge”（使用 Qwen3-VL-30B）和“Exact Match”两种方式评估，减少了单一评估指标的偏差。实验对比了多种最先进的 RL 优化框架，结果具有较强的说服力。

### 6. 主要结论与发现
*   **性能提升**：KAWHI 在所有基准测试中均实现了显著提升。例如，在 Qwen2.5-VL-7B 上结合 GSPO，平均性能提升了 **5.26%**。
*   **通用性**：KAWHI 作为一个插件，能稳定提升 GRPO、DAPO 和 GSPO 等不同 RL 算法的表现。
*   **视觉对齐的重要性**：实验证明，显式地将视觉空间先验引入奖励建模，能有效缓解模型在复杂推理中的“视觉幻觉”问题。

### 7. 优点（亮点）
*   **即插即用**：无需修改模型架构，不改变前向传播逻辑，易于集成到现有 RL 工作流中。
*   **结构化视觉建模**：首次显式地将几何结构信息（SGUF）引入 RLVR，解决了结构化图像（数学、图表）中信息稀疏的问题。
*   **合理的信用分配**：采用段落级而非 token 级的权重分配，既保证了语义连贯性，又实现了细粒度的奖励调节。

### 8. 不足与局限
*   **视觉头依赖性**：识别“视觉关键头”需要预先在 MME 等基准上进行消融分析，这增加了针对新模型部署时的预处理步骤。
*   **场景局限性**：SGUF 算法主要针对具有明显几何特征的结构化图像（数学题、图表），在纹理极其复杂的自然场景图像中的泛化效果尚需进一步验证。
*   **超参数敏感性**：SGUF 的 `skip_ratio` 等超参数对性能有一定影响，可能需要针对不同任务进行微调。

（完）
