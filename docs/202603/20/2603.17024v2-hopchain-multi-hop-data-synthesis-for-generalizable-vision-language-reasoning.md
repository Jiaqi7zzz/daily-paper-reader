---
title: "HopChain: Multi-Hop Data Synthesis for Generalizable Vision-Language Reasoning"
title_zh: HopChain：面向泛化视觉语言推理的多跳数据合成
authors: "Shenzhi Wang, Shixuan Liu, Jing Zhou, Chang Gao, Xiong-Hui Chen, Binghai Wang, An Yang, Shiji Song, Bowen Yu, Gao Huang, Junyang Lin"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.17024v2"
tags: ["query:rl-tl"]
score: 9.0
evidence: 为具有可验证奖励的强化学习（RLVR）合成多步推理数据
tldr: 针对视觉语言模型在细粒度多步推理中的感知和逻辑错误，本文提出了 HopChain 框架。该框架能自动合成具有逻辑依赖关系的多跳视觉推理数据，并通过可验证的数值答案支持强化学习（RLVR）训练。实验表明，HopChain 在 24 个基准测试中显著提升了模型性能，特别是在长思维链推理任务中表现优异，证明了其在增强模型通用推理能力方面的有效性。
selection_source: fresh_fetch
motivation: 现有的视觉语言强化学习数据缺乏复杂的逻辑推理链，导致模型在处理细粒度多步推理时容易出现感知和幻觉错误。
method: 提出 HopChain 框架，通过合成基于实例定位的逻辑依赖多跳查询，生成具有明确数值答案的数据用于 RLVR 训练。
result: 在 Qwen3.5 模型上的实验显示，该方法提升了 24 个基准测试中的 20 个，且在超长思维链推理中性能提升超过 50 分。
conclusion: HopChain 是一种有效且可扩展的多跳数据合成框架，能显著增强视觉语言模型的通用推理能力和长思维链表现。
---

## 摘要
视觉语言模型 (VLMs) 展现出强大的多模态能力，但在细粒度视觉语言推理方面仍面临挑战。我们发现，长思维链 (CoT) 推理暴露了多种失败模式，包括感知、推理、知识和幻觉错误，这些错误可能会在中间步骤中累积。然而，大多数用于可验证奖励强化学习 (RLVR) 的现有视觉语言数据并不涉及贯穿始终依赖视觉证据的复杂推理链，导致这些弱点在很大程度上未被暴露。因此，我们提出了 HopChain，这是一个可扩展的框架，用于合成多跳视觉语言推理数据，以进行 VLMs 的 RLVR 训练。每个合成的多跳查询都形成一个逻辑相关的实例落地 (instance-grounded) 跳步链，其中前序跳步确定了后续跳步所需的实例、集合或条件，而最终答案保持为适合可验证奖励的特定、明确的数字。我们在两种 RLVR 设置下训练了 Qwen3.5-35B-A3B 和 Qwen3.5-397B-A17B：仅使用原始数据，以及原始数据加上 HopChain 的多跳数据，并在涵盖 STEM 与谜题、通用 VQA、文本识别与文档理解以及视频理解的 24 个基准测试中进行了比较。尽管这些多跳数据并非针对任何特定基准测试合成，但它在两个模型上的 24 个基准测试中改进了 20 个，表明了广泛且可泛化的提升。一致地，将完整的链式查询替换为半多跳或单跳变体，使五个代表性基准测试的平均得分分别从 70.4 下降到 66.7 和 64.3。值得注意的是，多跳带来的增益在长 CoT 视觉语言推理中达到顶峰，在超长 CoT 机制下超过了 50 分。这些实验证明 HopChain 是一个有效且可扩展的框架，用于合成能够提升泛化视觉语言推理能力的多跳数据。

## Abstract
Vision-language models (VLMs) show strong multimodal capabilities but still struggle with fine-grained vision-language reasoning. We find that long chain-of-thought (CoT) reasoning exposes diverse failure modes, including perception, reasoning, knowledge, and hallucination errors, which can compound across intermediate steps. However, most existing vision-language data used for reinforcement learning with verifiable rewards (RLVR) does not involve complex reasoning chains that rely on visual evidence throughout, leaving these weaknesses largely unexposed. We therefore propose HopChain, a scalable framework for synthesizing multi-hop vision-language reasoning data for RLVR training of VLMs. Each synthesized multi-hop query forms a logically dependent chain of instance-grounded hops, where earlier hops establish the instances, sets, or conditions needed for later hops, while the final answer remains a specific, unambiguous number suitable for verifiable rewards. We train Qwen3.5-35B-A3B and Qwen3.5-397B-A17B under two RLVR settings: the original data alone, and the original data plus HopChain's multi-hop data, and compare them across 24 benchmarks spanning STEM and Puzzle, General VQA, Text Recognition and Document Understanding, and Video Understanding. Although this multi-hop data is not synthesized for any specific benchmark, it improves 20 of 24 benchmarks on both models, indicating broad and generalizable gains. Consistently, replacing full chained queries with half-multi-hop or single-hop variants reduces the average score across five representative benchmarks from 70.4 to 66.7 and 64.3, respectively. Notably, multi-hop gains peak in long-CoT vision-language reasoning, exceeding 50 points in the ultra-long-CoT regime. These experiments establish HopChain as an effective, scalable framework for synthesizing multi-hop data that improves generalizable vision-language reasoning.

---

## 论文详细总结（自动生成）

以下是对论文《HopChain: Multi-Hop Data Synthesis for Generalizable Vision-Language Reasoning》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：视觉语言模型（VLM）在处理复杂的细粒度推理任务时，尽管能够生成长思维链（CoT），但极易在中间步骤出现感知错误、逻辑断裂或幻觉，且这些错误会随推理链条不断累积。
*   **研究背景**：现有的视觉语言强化学习（RLVR）数据通常缺乏深度依赖的推理链，模型往往可以通过“语言捷径”或简单的视觉匹配找到答案，而不需要在整个推理过程中持续关注视觉证据。
*   **研究动机**：开发一种能够强制模型在每一步推理中都进行“视觉重定位（Re-grounding）”的数据合成框架，以提升模型在长 CoT 场景下的鲁棒性和通用推理能力。

### 2. 方法论：HopChain 框架
*   **核心思想**：合成具有**逻辑依赖关系**的多跳（Multi-Hop）查询。前一步推理的结果（如定位的对象、识别的属性）必须是下一步推理的前提，最终指向一个可验证的数值答案。
*   **关键技术细节**：
    *   **两类跳步定义**：
        1.  **感知层跳步（Perception-level hop）**：在单对象感知（如读字、识色）与多对象关系推理（如比较大小、计数）之间切换。
        2.  **实例链跳步（Instance-chain hop）**：遵循显式的依赖链（如 A → B → C），只有找到 A 才能定位 B。
    *   **四阶段合成流程**：
        1.  **类别识别**：利用 Qwen3-VL 识别图中所有语义类别。
        2.  **实例分割**：使用 SAM3 对识别出的类别进行像素级分割和定位。
        3.  **多跳查询生成**：由教师模型根据实例组合生成逻辑严密的链式问题，确保无法跳过中间步骤。
        4.  **人工验证与难度校准**：通过 4 人一致性检查确定真值，并利用弱模型过滤掉过于简单的样本。
*   **强化学习算法**：采用 **SAPO（Soft Adaptive Policy Optimization）** 算法，通过可验证的数值奖励进行训练。

### 3. 实验设计
*   **实验对象**：Qwen3.5-35B-A3B 和 Qwen3.5-397B-A17B。
*   **基准测试（Benchmarks）**：涵盖 4 大类共 24 个基准，包括：
    *   **STEM 与谜题**：MathVision, MMMU Pro, MathVista, ZeroBench 等。
    *   **通用 VQA**：MMBench, RealWorldQA, MMStar 等。
    *   **文本与文档**：CharXiv, DocVQA, InfoVQA。
    *   **视频理解**：Video-MME, VideoMMMU, MVBench 等。
*   **对比设置**：
    1.  **Before RLVR**：SFT 后的基准模型。
    2.  **RLVR w/o Multi-Hop**：仅使用原始 RLVR 数据训练。
    3.  **RLVR w/ Multi-Hop**：原始数据 + HopChain 合成的多跳数据。

### 4. 资源与算力
*   **训练细节**：35B 模型进行 1000 步梯度更新，397B 模型进行 800 步。每个 Rollout 为每个查询采样 16 个响应。
*   **算力说明**：论文**未明确提及**具体的 GPU 型号（如 H100/A100）及具体数量，也未给出总训练时长。但考虑到模型规模（最高 397B）和采样策略，该实验需要极大规模的计算集群支持。

### 5. 实验数量与充分性
*   **实验规模**：在 24 个主流基准上进行了全面测试，合成数据量约为 6k-8k 条。
*   **消融实验**：对比了“全多跳”、“半多跳”和“单跳”变体，证明了完整逻辑链的必要性。
*   **分析深度**：进行了推理长度分析（证明在长 CoT 下增益更大）、难度分布分析以及错误类型分析（证明能修复多种类型的感知和推理错误）。
*   **评价**：实验设计非常充分，跨越了不同规模的模型和极其广泛的任务领域，结果具有高度的客观性和说服力。

### 6. 主要结论与发现
*   **通用性提升**：HopChain 数据在两个模型上的 20/24 个基准测试中均取得了提升，证明了合成数据具有极强的泛化性，而非过拟合。
*   **长 CoT 优势**：在超长思维链（Ultra-long CoT）区间，多跳训练带来的性能提升甚至超过 50 分。
*   **跨模态迁移**：虽然训练数据基于静态图像，但在视频理解基准上也表现出显著提升，说明模型底层推理逻辑得到了加强。
*   **结构重要性**：消融实验显示，缩短推理链会导致性能显著下降（平均分从 70.4 降至 64.3）。

### 7. 优点
*   **逻辑严密**：通过实例依赖链设计，从根本上杜绝了模型通过语言先验“猜”出答案的可能性。
*   **可扩展性强**：全自动化的合成管线配合人工质量控制，能够大规模生成高质量、高难度的推理数据。
*   **任务无关性**：不针对特定 Benchmark 设计，而是专注于提升通用的视觉感知与逻辑对齐能力。

### 8. 不足与局限
*   **分割模型依赖**：该框架高度依赖实例分割模型（如 SAM3）的效果，对于无法清晰分割的图像（如抽象艺术、极度密集的纹理场景）难以处理。
*   **合成成本**：生成过程需要极强的教师模型（Qwen3-VL-235B）进行复杂推理，且人工验证环节虽然保证了质量，但也限制了数据的即时生成速度。
*   **覆盖范围**：目前主要集中在具有明确数值答案的任务，对于开放式主观评价任务的提升效果尚待验证。

（完）
