---
title: "Towards Effective Experiential Learning: Dual Guidance for Utilization and Internalization"
title_zh: 迈向有效的经验学习：利用与内化的双重引导
authors: "Fei Bai, Zhipeng Chen, Chuan Hao, Ming Yang, Ran Tao, Bryan Dai, Wayne Xin Zhao, Jian Yang, Hongteng Xu"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24093v1"
tags: ["query:rl-tl"]
score: 10.0
evidence: 针对可验证奖励强化学习（RLVR）的双重指导优化
tldr: 针对大语言模型在强化学习（RLVR）中对经验利用不足的问题，本文提出双重引导优化（DGO）框架。该框架模拟人类学习过程，结合外部经验库与模型内部知识共同引导探索。通过构建经验库、联合引导探索及闭环优化，DGO 实现了经验的高效利用与内化，显著提升了模型在推理任务中的表现。
selection_source: fresh_fetch
motivation: 现有的强化学习训练仅是人类学习的粗略模拟，缺乏对外部和内部经验的有效结合与内化。
method: 提出 DGO 框架，通过构建经验库并结合模型内部知识引导探索，形成经验利用与内化的闭环优化。
result: 实验结果表明，DGO 在推理任务中持续优于基准方法，证明了双重引导机制的有效性。
conclusion: 更好地利用和内化经验是提升大语言模型推理能力的关键路径。
---

## 摘要
最近，强化学习（RL）已成为提升大语言模型（LLMs）能力的重要方法。特别是，基于可验证奖励的强化学习（RLVR）已成为推理任务中一种极具前景的范式。然而，现有的基于强化学习的训练仍然只是对人类学习的粗略模拟。人类学习者利用外部和内部经验来引导探索，并逐渐将有用的轨迹内化为稳定的知识。受此差距启发，我们提出一个问题：在 RLVR 训练过程中，LLMs 如何更好地利用和内化经验？为了回答这个问题，我们提出了双重引导优化（DGO），这是一个利用外部和内部经验来提高训练有效性的统一框架。具体而言，DGO 首先从先前探索的轨迹中构建一个经验库。然后，策略在经验库和模型内部知识的共同引导下进行探索。生成的轨迹进一步用于完善经验库并优化模型参数，形成经验利用与内化的闭环。实验表明，DGO 一致优于基线方法，这表明更好地利用和内化经验可以带来更有效的推理。

## Abstract
Recently, reinforcement learning~(RL) has become an important approach for improving the capabilities of large language models~(LLMs). In particular, reinforcement learning from verifiable rewards~(RLVR) has emerged as a promising paradigm for reasoning tasks. However, existing RL-based training still remains only a rough approximation to human learning. Human learners leverage both external and internal experience to guide exploration and gradually internalize useful trajectories into stable knowledge. Motivated by this gap, we ask: how can LLMs better utilize and internalize experience during RLVR training? To answer this question, we propose \textbf{D}ual \textbf{G}uidance \textbf{O}ptimization~(\textbf{DGO}), a unified framework that leverages \emph{external} and \emph{internal experience} to improve training effectiveness. Specifically, DGO first constructs an experience bank from previously explored trajectories. The policy then performs exploration under the joint guidance of the experience bank and the model's internal knowledge. The resulting trajectories are further used to refine the experience bank and optimize model parameters, forming a closed loop of experience utilization and internalization. Experiments show that DGO consistently outperforms baseline methods, suggesting that better utilization and internalization of experience lead to more effective reasoning.

---

## 论文详细总结（自动生成）

以下是对论文《Towards Effective Experiential Learning: Dual Guidance for Utilization and Internalization》的深度结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：基于可验证奖励的强化学习（RLVR）在提升大语言模型（LLM）推理能力方面表现出色，但现有的 RL 训练往往只是对人类学习过程的粗略模拟。
*   **核心问题**：人类学习者能够同时利用**外部经验**（如书籍、专家建议）和**内部经验**（自身积累的知识）来引导探索，并逐渐将有效的策略内化。而现有的 LLM 训练通常要么只关注参数化学习（内化），要么只关注非参数化引导（利用），两者缺乏有机结合。
*   **研究目标**：探索 LLM 如何在 RLVR 训练中更好地利用外部经验并将其转化为内部能力，从而实现推理能力的持续进化。

### 2. 方法论：双重引导优化 (DGO)
DGO 框架通过一个闭环过程将经验的“利用”与“内化”结合，包含三个关键阶段：
*   **经验构建 (Experience Construction)**：从历史轨迹中提取可复用的策略。经验以三元组形式表示：$(c, \kappa, \tau)$，分别代表领域、关键条件/错误场景、以及建议采取或避免的动作。
*   **联合轨迹-策略细化 (Joint Trajectory-Policy Refinement)**：
    *   **经验退火探索 (Experience Annealing)**：使用 GRPO 算法进行训练，通过混合“经验引导”和“无经验引导”的数据，并随迭代次数逐渐降低经验引导的比例（$\alpha$ 系数退火），促使模型从依赖外部引导转向独立推理。
    *   **经验更新**：利用新生成的轨迹不断精炼和替换经验库中的内容。
*   **经验内化 (Experience Internalization)**：
    *   **轨迹改写 (Trajectory Rephrasing)**：将带有外部经验引用的轨迹改写为自洽的推理过程，消除对外部提示词的显式依赖。
    *   **策略蒸馏 (Policy Distillation)**：将改写后的高质量轨迹通过监督微调（SFT）蒸馏回模型参数中，巩固其内在推理能力。

### 3. 实验设计
*   **模型与数据**：
    *   **基座模型**：Qwen3-4B/8B/14B-Base。
    *   **训练数据**：从 SkyWork 中采样 9,600 个数学问题进行 RL 训练。
    *   **经验生成器**：基于 DeepSeek v3.2 蒸馏出的数据微调 Qwen3-8B 得到。
*   **评测基准 (Benchmarks)**：
    *   **域内 (In-domain)**：AIME24、AIME25、HMMT25-Feb、MATH500。
    *   **域外 (Out-of-domain)**：GPQA-Diamond、HLE-Verified。
*   **对比方法**：SFT、GRPO、DAPO（参数化学习基线）以及 EGI（纯经验引导推理）。

### 4. 资源与算力
*   **硬件环境**：论文明确指出所有训练实验均在 **32 张 NVIDIA H20 GPU** 上完成。
*   **训练细节**：进行了 3 轮迭代，每轮包含 RL 训练和蒸馏过程。RL 阶段最大响应长度设为 12K，Mini-batch size 为 32。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 4B 到 14B 三个参数量级的模型，验证了方法的可扩展性。
*   **消融实验**：针对经验退火 (EA)、经验更新 (ER)、轨迹改写 (TR) 和策略蒸馏 (PD) 四个核心组件分别进行了消融，证明了每个模块的必要性。
*   **深度分析**：通过 t-SNE 可视化分析了轨迹分布，并使用 Pass@K 指标评估了推理模式的多样性。实验设计较为全面，对比了多种主流 RL 算法，结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能领先**：DGO 在所有模型规模上均显著优于 GRPO 和 DAPO。例如，Qwen3-8B 在内在推理（无外部经验）下达到 32.41% 的平均准确率，通过测试时缩放（TTS）进一步提升至 39.38%。
*   **利用与内化双赢**：DGO 训练的模型不仅在有经验引导时表现更好（利用能力强），在无引导时也优于基线（内化效果好）。
*   **鲁棒性提升**：相比基线，DGO 对噪声经验（无关经验）的抵抗力更强，能够更精准地识别并利用相关信息。
*   **模式扩展**：DGO 能够引导模型发现标准 RL 难以触及的有效推理路径，拓宽了模型的解题思路。

### 7. 优点与亮点
*   **闭环设计**：将非参数化的经验库与参数化的模型更新有机结合，模拟了人类“学习-实践-总结-内化”的完整链路。
*   **退火机制**：巧妙地解决了模型对外部引导的依赖问题，确保了模型在脱离经验库时依然具备强大的独立推理能力。
*   **改写技术**：通过轨迹改写消除了训练数据中的“提示词偏差”，提高了内化过程的质量。

### 8. 不足与局限
*   **领域局限性**：实验主要集中在数学推理任务上，虽然在 GPQA 等科学任务上有一定泛化表现，但在更广泛的通用对话或创意写作领域的有效性尚待验证。
*   **依赖初始教师**：经验生成器的初始训练依赖于更强大的模型（如 DeepSeek v3.2），这在一定程度上限制了系统的完全自主进化。
*   **计算开销**：迭代式的 RL + 蒸馏过程相比单次 RL 训练需要更多的计算资源和时间成本。

（完）
