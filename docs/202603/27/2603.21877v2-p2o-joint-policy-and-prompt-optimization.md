---
title: "P^2O: Joint Policy and Prompt Optimization"
title_zh: P^2O：策略与提示词联合优化
authors: "Xinyu Lu, Kaiqi Zhang, Jinglin Yang, Boxi Cao, Yaojie Lu, Hongyu Lin, Min He, Xianpei Han, Le Sun"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21877v2"
tags: ["query:rl-tl"]
score: 9.0
evidence: 针对 LLM 推理的具有可验证奖励的强化学习 (RLVR)
tldr: 针对大语言模型在强化学习（RLVR）中面临的“困难样本”探索效率低、奖励稀疏问题，本文提出P^2O框架。该框架结合了提示词优化与策略优化，利用GEPA算法为困难样本进化出更优提示词，引导模型生成成功轨迹，并将这些推理增益蒸馏至模型参数中。实验证明，P^2O在分布内和分布外基准测试上均显著提升了模型性能和泛化能力。
selection_source: fresh_fetch
motivation: 传统的RLVR在面对成功率接近零的困难样本时，由于奖励稀疏导致模型缺乏有效的监督信号。
method: 提出P^2O框架，通过GEPA算法优化提示词以引导模型探索困难样本，并将优化后的推理成果蒸馏到模型参数中。
result: "实验显示P^2O不仅在分布内表现优异，在分布外基准测试中也实现了平均4.7%的显著性能提升。"
conclusion: 结合提示词进化与策略优化的协同机制能有效解决RL中的探索难题，显著增强大语言模型的推理与泛化能力。
---

## 摘要
具有可验证奖励的强化学习（RLVR）已成为增强大语言模型（LLM）推理能力的强大范式。然而，原生 RLVR 面临探索效率低下的问题，尤其是在面对成功率接近于零的“困难样本”时。在此类场景下，对稀疏结果奖励的依赖通常会导致零优势估计，尽管这些实例具有很高的信息价值，但模型实际上却缺乏监督信号。为解决这一问题，我们提出了 P^2O，这是一个协同提示词优化（Prompt Optimization）与策略优化（Policy Optimization）的新型框架。P^2O 在训练迭代过程中识别困难样本，并利用 GeneticPareto (GEPA) 提示词优化算法来演化提示词模板，引导模型发现成功的轨迹。关键在于，与依赖输入增强的传统提示词工程方法不同，P^2O 将这些优化提示词所带来的推理增益直接蒸馏到模型参数中。这种机制为困难样本提供了更密集的正向监督信号，并加速了收敛。大量实验表明，P^2O 不仅在分布内数据集上取得了卓越性能，还表现出强大的泛化能力，在分布外基准测试中实现了显著提升（平均提升 4.7%）。

## Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) has emerged as a powerful paradigm for enhancing the reasoning capabilities of Large Language Models (LLMs). However, vanilla RLVR suffers from inefficient exploration, particularly when confronting "hard samples" that yield nearzero success rates. In such scenarios, the reliance on sparse outcome rewards typically results in zero-advantage estimates, effectively starving the model of supervision signals despite the high informational value of these instances. To address this, we propose P^2O, a novel framework that synergizes Prompt Optimization with Policy Optimization. P^2O identifies hard samples during training iterations and leverages the GeneticPareto (GEPA) prompt optimization algorithm to evolve prompt templates that guide the model toward discovering successful trajectories. Crucially, unlike traditional prompt engineering methods that rely on input augmentation, P^2O distills the reasoning gains induced by these optimized prompts directly into the model parameters. This mechanism provides denser positive supervision signals for hard samples and accelerates convergence. Extensive experiments demonstrate that P^2O not only achieves superior performance on in-distribution datasets but also exhibits strong generalization, yielding substantial improvements on out-of-distribution benchmarks (+4.7% avg.).

---

## 论文详细总结（自动生成）

以下是对论文《P^2O: Joint Policy and Prompt Optimization》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：在针对大语言模型（LLM）推理能力的强化学习（RLVR）中，模型常面临“**探索瓶颈**”。
*   **研究背景**：当训练集中包含大量“困难样本”（即模型在多次采样中成功率为零的题目）时，由于奖励极其稀疏，模型无法获得有效的梯度反馈（零优势估计），导致模型在简单样本上过拟合，而在复杂推理任务上停滞不前。
*   **动机**：借鉴提示词工程（Prompt Engineering）的经验，即使模型在标准输入下失败，优秀的提示词也能激发其潜在的推理路径。论文旨在将这种“提示词引导”的能力转化为模型自身的参数权重。

### 2. 方法论：P^2O 框架
P^2O 采用**交替优化**的策略，包含两个核心阶段：

*   **阶段 1：带上下文蒸馏的策略优化（Policy Optimization with Context Distillation）**
    *   **核心思想**：利用优化后的提示词作为“脚手架”引导模型生成正确轨迹，但更新参数时只针对原始输入。
    *   **关键技术**：
        *   **困难样本挖掘**：识别成功率低于阈值 $\tau$ 的样本。
        *   **上下文蒸馏**：对于困难样本，使用增强输入 $\tilde{x} = T(x, z)$（原始问题+优化提示词）进行采样生成轨迹 $\tilde{y}$，但在计算策略梯度时，计算的是 $P(y|x)$（原始问题下生成该轨迹的概率）。这迫使模型将提示词诱导出的推理能力内化到参数中，避免推理时对复杂提示词的依赖。
*   **阶段 2：进化提示词优化（Evolutionary Prompt Optimization）**
    *   **核心思想**：针对模型仍无法解决的困难样本，动态进化出一组能够“破解”这些题目的提示词模板。
    *   **关键技术**：
        *   **GEPA 算法**：利用一个“反思模型”（Reflection LLM）分析失败案例并提出改进建议，通过变异生成新提示词。
        *   **帕累托选择（Pareto Selection）**：维护一个多样化的提示词群体，确保覆盖不同类型的错误模式。
        *   **贪婪提示词分配**：为每个困难样本分配最适合的 $K$ 个提示词模板。

### 3. 实验设计
*   **训练数据集**：从 DeepScaler（5K 样本）和 DeepMath（5K 样本，难度 $\ge 7$）中采样。
*   **评估基准（Benchmarks）**：涵盖 6 个极具挑战性的数学竞赛数据集：AIME24、AIME25、AMC、MATH500、MINERVA、OLYMPIAD。
*   **对比方法**：
    *   基础模型：Qwen3-4B (Base/Instruct)。
    *   强基线：标准 GRPO（Group Relative Policy Optimization）算法。
    *   P^2O 变体：自反思版（Self-Ref）和教师反思版（Teacher-Ref，使用 Kimi-K2 作为反思模型）。

### 4. 资源与算力
*   **模型规模**：主要使用 Qwen3-4B 作为策略模型。
*   **训练细节**：训练 5 个 Epoch，全局 Batch Size 为 128，最大生成长度 12k tokens，学习率 $1 \times 10^{-6}$。
*   **算力说明**：论文**未明确列出**具体的 GPU 型号（如 A100/H100）或总计算时数。但提到在提示词优化阶段使用了并行化的 Beam Search（宽度 $W=16$）来提高计算效率。

### 5. 实验数量与充分性
*   **实验规模**：在两个不同的训练分布（DeepMath 和 DeepScaler）上进行了完整实验，并在 6 个主流基准上验证。
*   **消融实验**：
    1.  **上下文蒸馏的必要性**：证明了如果不进行蒸馏（直接在带提示词的输入上训练），模型会产生依赖性，导致测试性能大幅下降。
    2.  **提示词多样性**：证明了在 Rollout 组内使用多样化的帕累托最优提示词比使用单一提示词效果更好。
*   **充分性评价**：实验设计较为全面，不仅有定量分数，还有训练曲线分析和定性案例分析，对比基线（GRPO）是目前该领域的主流方法，实验结果具有说服力。

### 6. 主要结论与发现
*   **性能飞跃**：P^2O 在所有基准上均优于 GRPO。在最难的 AIME24/25 上，相比 GRPO 分别提升了约 **12.9%** 和 **11.7%**。
*   **泛化能力**：P^2O 在分布外（OOD）任务上表现强劲，平均提升达 **4.7%**。
*   **破解瓶颈**：提示词优化能有效降低困难样本的数量（实验观察到困难样本数随 Epoch 稳步下降），证明了该方法确实解决了探索难题。
*   **反思模型的影响**：在某些任务中，模型自身的反思（Self-Ref）甚至优于更强的外部教师模型，说明提示词需要与模型自身的推理容量相匹配。

### 7. 优点
*   **创新性**：首次将离散的提示词进化与连续的策略梯度优化深度结合，解决了 RLVR 中长期存在的稀疏奖励问题。
*   **无需人工干预**：相比于课程学习（需要人工设计难度梯度）或奖励塑造（需要专家知识），P^2O 是全自动的自改进过程。
*   **推理高效**：通过蒸馏机制，模型在推理阶段无需携带冗长的优化提示词，保持了原生模型的推理速度。

### 8. 不足与局限
*   **计算开销**：虽然论文优化了效率，但引入 GEPA 进化过程意味着在训练循环中需要额外的采样和反思模型调用，增加了训练成本。
*   **任务局限性**：目前仅在数学推理（具有明确可验证奖励）任务上验证，在缺乏客观验证机制的开放域任务（如创意写作）中应用难度较大。
*   **依赖反思模型**：提示词进化的质量高度依赖于反思模型（Reflection LLM）的水平，若反思模型无法准确识别错误原因，进化可能失效。

（完）
