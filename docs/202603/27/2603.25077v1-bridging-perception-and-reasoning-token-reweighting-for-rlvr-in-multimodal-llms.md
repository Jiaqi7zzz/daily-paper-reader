---
title: "Bridging Perception and Reasoning: Token Reweighting for RLVR in Multimodal LLMs"
title_zh: 连接感知与推理：多模态大语言模型中 RLVR 的 Token 重加权
authors: "Jinda Lu, Junkang Wu, Jinghan Li, Kexin Huang, Shuo Yang, Guoyin Wang, Jiancan Wu, Xiang Wang, Xiangnan He"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25077v1"
tags: ["query:rl-tl"]
score: 8.0
evidence: 多模态大模型中RLVR的标记重加权
tldr: 本研究针对多模态大语言模型在可验证奖励强化学习（RLVR）中感知与推理令牌相互交织的挑战，提出了一种名为Token-Reweighting (ToR) 的即插即用策略。通过实证分析发现，仅优化感知或推理令牌的效果均不如全面优化，说明两者存在强耦合。ToR通过识别并动态重新加权这两类关键令牌，显著提升了模型在多模态推理任务中的视觉定位与逻辑推理能力，在多个基准测试中达到了SOTA水平。
selection_source: fresh_fetch
motivation: 多模态模型在强化学习中面临感知令牌（视觉定位）与推理令牌（逻辑构建）内在耦合且难以平衡的挑战。
method: 提出Token-Reweighting (ToR) 策略，通过识别关键的感知和推理令牌并在RLVR训练过程中动态调整其权重。
result: 在GRPO和DAPO等现有方法上应用ToR后，模型在多个多模态推理基准测试中均取得了显著的性能提升并达到SOTA。
conclusion: ToR有效地桥接了感知与推理，证明了在多模态强化学习中协同优化不同类型令牌对提升模型性能至关重要。
---

## 摘要
将具有可验证奖励的强化学习（RLVR）扩展到多模态大语言模型（MLLMs）面临一个根本性挑战：它们的响应本质上交织着感知相关 token（用于定位视觉内容）和推理相关 token（用于构建推理链）。这些 token 类型实例化了不同但又相互依赖的能力——视觉定位和符号推理——这使得孤立的优化变得不足。通过 token 级别的实证分析，我们证明了仅优化感知或仅优化推理 token 的表现始终不如全面优化，这强调了它们内在的耦合性。为了解决这个问题，我们提出了一种即插即用的 Token 重加权（ToR）策略，该策略通过识别这两类关键 token 并在 RLVR 训练期间动态重新加权，显式地对这种相互依赖性进行建模。在现有方法（如 GRPO 和 DAPO）的基础上应用 ToR，在多个多模态推理基准测试中实现了持续的性能提升，在准确的视觉定位和连贯的推理方面均达到了最先进的性能。

## Abstract
Extending Reinforcement Learning with Verifiable Rewards (RLVR) to multimodal large language models (MLLMs) faces a fundamental challenge: their responses inherently interleave perception-related tokens, which ground visual content, with reasoning-related tokens, which construct reasoning chains. These token types instantiate distinct yet interdependent capacities -- visual grounding and symbolic reasoning -- making isolated optimization insufficient. Through token-level empirical analysis, we demonstrate that optimizing either perception- or reasoning-only tokens consistently underperforms full optimization, underscoring their inherent coupling. To address this, we propose a plug-and-play Token-Reweighting (ToR) strategy that explicitly models this interdependence by identifying critical tokens of both types and dynamically reweighting them during RLVR training. Applied on top of existing methods (e.g., GRPO and DAPO), ToR delivers consistent performance gains across multiple multi-modal reasoning benchmarks, achieving state-of-the-art performance with both accurate visual grounding and coherent reasoning.

---

## 论文详细总结（自动生成）

### 论文总结：连接感知与推理——多模态大语言模型中 RLVR 的 Token 重加权

#### 1. 核心问题与整体含义（研究动机和背景）
*   **背景**：具有可验证奖励的强化学习（RLVR，如 DeepSeek-R1 采用的 GRPO）在提升大语言模型的复杂推理能力方面表现卓越。
*   **核心问题**：将 RLVR 扩展到多模态大语言模型（MLLM）时，模型生成的响应中交织着**感知相关 Token**（用于定位视觉内容）和**推理相关 Token**（用于构建逻辑链）。
*   **研究动机**：现有的多模态 RL 方法通常将感知和推理能力孤立地进行优化。作者通过实证研究发现，感知与推理在 Token 级别是深度耦合的，仅优化其中一类 Token 会导致另一类能力受损，从而限制了模型的整体表现。

#### 2. 方法论：核心思想与关键技术
*   **核心思想**：提出 **Token-Reweighting (ToR)** 策略。这是一种即插即用的模块，通过识别响应中关键的感知和推理 Token，并在强化学习训练过程中动态调整它们的梯度权重，从而实现两者的协同优化。
*   **关键技术细节**：
    *   **推理 Token 识别**：利用**预测熵（Entropy）**。高熵位置通常对应推理链中的关键决策点（即“分叉点”）。
    *   **感知 Token 识别**：利用**视觉敏感度（Visual Sensitivity）**。通过对比“有图像输入”和“无图像输入（空占位符）”时 Token 的对数概率差值（logp-diff）来确定哪些 Token 强依赖于视觉信息。
    *   **算法流程**：
        1.  在 Rollout 阶段计算每个 Token 的熵和视觉敏感度。
        2.  根据预设比例（如前 30%）筛选出推理和感知 Token 集合。
        3.  在计算策略梯度（如 GRPO 或 DAPO 目标函数）时，为这两类 Token 分配特定的权重（$\gamma_r$ 和 $\gamma_p$），而将非关键 Token 的权重设为零。
    *   **公式集成**：将重加权掩码集成到 GRPO 或 DAPO 的优势函数计算中，显式建模两者的相互依赖性。

#### 3. 实验设计
*   **数据集与场景**：
    *   **训练集**：Geometry3K（2.1K 样本，用于基础实验）和 ViRL-39K（用于扩展实验）。
    *   **评估基准（Benchmark）**：涵盖视觉推理（MathVerse, MathVision, MathVista, WeMath）和视觉感知（HalluBench）五大主流榜单。
*   **对比方法**：
    *   **开源模型**：InternVL-2.5/3, LLaVA-OneVision, Qwen2.5-VL-7B-Instruct。
    *   **RLVR 基准**：R1-VL, Vision-R1, R1-OneVision, OpenVLThinker, MM-Eureka, NoisyRollout 等。
    *   **基础算法**：原生 GRPO 和 DAPO。

#### 4. 资源与算力
*   **算力设备**：使用了 **8 台 NVIDIA H800 GPU**（每张显卡 80GB 显存）。
*   **训练配置**：基于 EasyR1 框架，学习率为 1e-6，全局 Batch Size 为 128，Rollout Batch Size 为 512，每个样本生成 12 个响应。
*   **训练时长**：文中未明确给出具体的总训练小时数，但实验涵盖了从 2.1K 到 39K 样本规模的完整训练流程。

#### 5. 实验数量与充分性
*   **实验规模**：作者进行了多维度的消融实验和对比实验。
*   **充分性分析**：
    *   **消融实验**：详细测试了不同 Token 选择比例（20% 到 80%）、不同感知权重系数（0.1 到 1.5）对结果的影响。
    *   **代理指标对比**：对比了概率差、对数概率差、熵差等多种识别感知 Token 的指标，验证了 logp-diff 的优越性。
    *   **模型泛化**：在 Qwen2.5-VL 的 3B 和 7B 两个版本上均验证了有效性。
    *   **结论**：实验设计严谨，通过“孤立优化”与“联合优化”的对比有力地证明了论文的核心假设，实验结果具有统计学意义和说服力。

#### 6. 主要结论与发现
*   **耦合性验证**：仅优化推理 Token 会导致视觉定位错误；仅优化感知 Token 会导致逻辑链断裂。全量 Token 优化虽好，但不如有侧重的重加权优化。
*   **性能提升**：ToR 策略在所有基准测试中均实现了持续的性能增益。例如，在 Geometry3K 训练下，ToR-DAPO 在 WeMath 和 MathVista 上的表现显著优于原始 DAPO。
*   **SOTA 状态**：在相同训练数据规模下，ToR 结合 DAPO 达到了当前开源多模态推理模型的领先水平。

#### 7. 优点
*   **即插即用**：无需修改模型架构或增加额外的推理开销，易于集成到现有的 RL 框架中。
*   **自给自足**：关键 Token 的识别完全依赖模型自身的内在信号（熵和概率），不需要外部昂贵的标注或辅助模型。
*   **平衡性好**：通过显式重加权，解决了多模态学习中常见的“感知-推理”失衡问题。

#### 8. 不足与局限
*   **细粒度不足**：目前的感知 Token 识别基于全局概率变化，可能无法像 SAM 等视觉模型那样实现像素级的精确关联。
*   **静态权重**：虽然 Token 选择是动态的，但权重系数（$\gamma$）在训练过程中是固定的，未来可探索更复杂的动态权重调整策略。
*   **上下文依赖**：该方法主要在 Token 级别操作，对于需要极长程上下文理解的复杂多模态任务，其效果上限仍有待进一步验证。

（完）
