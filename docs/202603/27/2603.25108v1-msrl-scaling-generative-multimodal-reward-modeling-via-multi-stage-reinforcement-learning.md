---
title: "MSRL: Scaling Generative Multimodal Reward Modeling via Multi-Stage Reinforcement Learning"
title_zh: MSRL：通过多阶段强化学习扩展生成式多模态奖励建模
authors: "Chenglong Wang, Yifu Huo, Yang Gan, Qiaozhi He, Qi Meng, Bei Li, Yan Wang, Junfu Liu, Tianhua Zhou, Jingbo Zhu, Tong Xiao"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25108v1"
tags: ["query:rl-tl"]
score: 8.0
evidence: 利用RLVR增强多模态奖励模型
tldr: 针对多模态奖励模型（MRM）训练中多模态偏好数据获取成本高、难以扩展的问题，本文提出多阶段强化学习（MSRL）框架。该方法通过先从大规模文本数据学习奖励推理能力，再通过基于描述和全多模态的强化学习阶段逐步迁移至多模态任务，并结合跨模态知识蒸馏提升泛化性。实验证明，MSRL在无需额外多模态标注的情况下，显著提升了模型在视觉理解与生成任务上的表现。
selection_source: fresh_fetch
motivation: 现有的多模态奖励模型训练高度依赖昂贵的人工标注多模态偏好数据，限制了模型的扩展性。
method: 提出一种多阶段强化学习方法，通过文本预训练、基于描述的RL和全多模态RL三个阶段，将奖励推理能力从纯文本迁移至多模态领域。
result: "在VL-RewardBench和GenAI-Bench等基准测试中，模型性能分别从66.6%提升至75.9%以及从70.2%提升至75.7%。"
conclusion: MSRL有效地实现了生成式多模态奖励模型的规模化训练，在不增加多模态标注负担的前提下显著增强了模型的跨模态偏好建模能力。
---

## 摘要
近期多模态奖励建模的进展主要由从判别式方法向生成式方法的范式转变所驱动。在此基础上，近期的研究进一步采用了基于可验证奖励的强化学习（RLVR）来增强多模态奖励模型（MRM）。尽管取得了成功，但基于 RLVR 的训练通常依赖于标注的多模态偏好数据，这些数据的获取成本高昂且耗费人力，导致难以扩展 MRM 的训练规模。为了克服这一限制，我们提出了一种多阶段强化学习（MSRL）方法，该方法可以在多模态数据有限的情况下实现 MRM 的可扩展强化学习。MSRL 取代了传统的基于 RLVR 的训练范式，首先从大规模文本偏好数据中学习可泛化的奖励推理能力，然后通过基于描述（caption-based）和全多模态的强化学习阶段，逐步将这种能力迁移到多模态任务中。此外，我们引入了一种跨模态知识蒸馏方法，以提高 MSRL 内部的偏好泛化能力。大量实验表明，MSRL 有效地扩展了生成式 MRM 的 RLVR 训练，并在视觉理解和视觉生成任务中显著提升了性能（例如，在 VL-RewardBench 上从 66.6% 提升至 75.9%，在 GenAI-Bench 上从 70.2% 提升至 75.7%），且无需额外的多模态偏好标注。我们的代码可在以下地址获取：https://github.com/wangclnlp/MSRL。

## Abstract
Recent advances in multimodal reward modeling have been largely driven by a paradigm shift from discriminative to generative approaches. Building on this progress, recent studies have further employed reinforcement learning from verifiable rewards (RLVR) to enhance multimodal reward models (MRMs). Despite their success, RLVR-based training typically relies on labeled multimodal preference data, which are costly and labor-intensive to obtain, making it difficult to scale MRM training. To overcome this limitation, we propose a Multi-Stage Reinforcement Learning (MSRL) approach, which can achieve scalable RL for MRMs with limited multimodal data. MSRL replaces the conventional RLVR-based training paradigm by first learning a generalizable reward reasoning capability from large-scale textual preference data, and then progressively transferring this capability to multimodal tasks through caption-based and fully multimodal reinforcement-learning stages. Furthermore, we introduce a cross-modal knowledge distillation approach to improve preference generalization within MSRL. Extensive experiments demonstrate that MSRL effectively scales the RLVR-based training of generative MRMs and substantially improves their performance across both visual understanding and visual generation tasks (e.g., from 66.6% to 75.9% on VL-RewardBench and from 70.2% to 75.7% on GenAI-Bench), without requiring additional multimodal preference annotations. Our code is available at: https://github.com/wangclnlp/MSRL.

---

## 论文详细总结（自动生成）

这是一份关于论文《MSRL: Scaling Generative Multimodal Reward Modeling via Multi-Stage Reinforcement Learning》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心挑战**：多模态奖励模型（MRM）是实现多模态大模型（MLLM）与人类偏好对齐的关键。然而，现有的强化学习训练（如 RLVR）高度依赖高质量、人工标注的多模态偏好数据。这类数据获取成本极高且难以大规模扩展，形成了“数据瓶颈”，限制了 MRM 推理能力的进一步提升。
*   **研究动机**：借鉴纯文本大模型通过大规模 RL 提升推理能力的经验，探索是否可以利用充足的**纯文本偏好数据**来增强多模态奖励模型的推理能力，并将其有效地迁移至视觉任务中。

### 2. 方法论：多阶段强化学习（MSRL）
MSRL 提出了一种课程学习范式，将训练分为三个递进阶段：
*   **阶段 1：纯文本强化学习（Textual RL）**
    *   **核心思想**：在大规模纯文本偏好数据上训练模型，使其掌握复杂的奖励推理（Chain-of-Thought, CoT）能力。
    *   **技术细节**：使用可验证奖励（格式奖励 + 准确性奖励），通过 GRPO 算法优化模型。此阶段冻结视觉编码器，专注于逻辑推理。
*   **阶段 2：偏好泛化（Preference Generalization）**
    *   **基于描述的 RL**：将多模态数据的图像/视频替换为详细的文本描述（Caption），在保持纯文本输入的同时引入多模态场景语义。
    *   **跨模态知识蒸馏（CMKD）**：利用阶段 2 训练好的“文本专家”模型生成高质量推理链作为教师信号，引导模型在输入真实图像时产生一致的推理逻辑。
    *   **任务识别奖励**：引入 $r_{task}$，鼓励模型识别当前任务类型（如图像理解 vs. 视频生成）。
*   **阶段 3：全多模态强化学习（Multimodal RL）**
    *   **核心思想**：在真实的图像/视频偏好数据上进行最终适配。由于前两阶段已打下基础，此阶段仅需少量（约 20k）多模态数据即可实现高性能。

### 3. 实验设计
*   **数据集与场景**：
    *   **视觉理解**：VL-RewardBench, Multimodal RewardBench。
    *   **视觉生成**：GenAI-Bench (图像), VideoGen-RewardBench (视频)。
    *   **视频理解**：ShareGPTVideo。
*   **基准（Benchmark）与对比方法**：
    *   **闭源模型**：GPT-4o, Gemini-1.5-Pro, Claude-3.7-Sonnet。
    *   **开源模型**：InternVL3.5 系列, R1-Reward, LLaVA-Critic, UnifiedReward。
    *   **消融基准**：在相同多模态数据下训练的判别式（Discriminative）和生成式（Generative）MRM。

### 4. 资源与算力
*   **框架与参数**：使用 `ms-swift` 训练框架。
*   **超参数**：学习率 1e-6，Batch Size 128，GRPO 采样组大小为 8。
*   **算力说明**：论文**未明确提及**具体的 GPU 型号（如 A100 或 H100）及总训练时长，但提到模型规模涵盖了 1B、4B、8B 到 14B 多个量级。

### 5. 实验数量与充分性
*   **实验规模**：
    *   测试了 4 种不同参数规模的 Backbone（1B, 4B, 8B, 14B），验证了方法的扩展性（Scaling Behavior）。
    *   涵盖了图像理解、图像生成、视频理解、视频生成四大类任务。
*   **充分性与客观性**：
    *   **消融实验**：详细分析了三个阶段及 CMKD 技术的贡献，证明了每一阶段的必要性。
    *   **公平对比**：通过在相同多模态数据集上训练基准模型，排除了数据量差异带来的干扰，实验设计严谨、客观。

### 6. 主要结论与发现
*   **性能飞跃**：MSRL 在不增加多模态标注的情况下，显著提升了 MRM 性能。例如，8B 模型在 VL-RewardBench 上从 66.6% 提升至 75.9%。
*   **Scaling Law 效应**：随着模型参数增加，MSRL 带来的增益更加显著，证明了该方法在大规模模型上的潜力。
*   **低数据依赖**：实验显示，在完成文本 RL 后，仅需 5k 个多模态样本即可达到甚至超过传统方法使用大量数据训练的效果。

### 7. 优点（亮点）
*   **创新路径**：打破了“提升多模态模型必须靠多模态数据”的思维定式，成功实现了跨模态的偏好推理迁移。
*   **通用性强**：一个统一的奖励模型即可同时处理理解和生成任务，且支持图像和视频。
*   **高效扩展**：利用廉价且海量的文本数据解决了昂贵的多模态数据瓶颈问题。

### 8. 不足与局限
*   **描述依赖性**：阶段 2 的效果高度依赖于 Caption 的质量。如果 Caption 无法准确捕捉图像中的关键偏好特征（如细微的伪影或构图偏差），迁移效果可能受限。
*   **计算开销**：虽然节省了标注成本，但多阶段 RL 训练（尤其是 GRPO 采样）对计算资源的需求依然较高。
*   **视频时序建模**：尽管在视频任务上有所提升，但对于极长视频或复杂时序逻辑的奖励建模仍有待进一步探索。

（完）
