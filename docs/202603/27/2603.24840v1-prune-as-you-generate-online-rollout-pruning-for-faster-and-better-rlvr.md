---
title: "Prune as You Generate: Online Rollout Pruning for Faster and Better RLVR"
title_zh: 边生成边剪枝：用于实现更快、更优 RLVR 的在线 Rollout 剪枝
authors: "Haobo Xu, Sirui Chen, Ruizhong Qiu, Yuchen Yan, Chen Luo, Monica Cheng, Jingrui He, Hanghang Tong"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24840v1"
tags: ["query:rl-tl"]
score: 10.0
evidence: 具有可验证奖励的强化学习 (RLVR)
tldr: 针对强化学习可验证奖励（RLVR）计算成本高且学习信号稀疏的问题，本文提出 arrol 方法。该方法在生成过程中利用在线训练的轻量级质量头预测部分序列的成功概率，动态剪枝低价值样本并平衡正确性分布。实验表明，arrol 在提升训练速度达 1.7 倍的同时，显著提高了模型在推理和测试时缩放的准确率。
selection_source: fresh_fetch
motivation: 现有的 RLVR 方法因需大量采样导致计算开销巨大，且样本奖励方差低导致学习信号微弱。
method: 提出在线剪枝框架 arrol，通过实时训练质量头预测部分生成序列的成功率，实现动态剪枝与样本重组。
result: "在 Qwen 和 LLaMA 模型上，该方法提升了约 2.5% 的平均准确率，加速训练达 1.7 倍，并显著增强了测试时缩放效果。"
conclusion: arrol 证明了在生成过程中进行在线剪枝不仅能大幅降低 RL 训练成本，还能通过优化样本质量提升模型推理能力。
---

## 摘要
具有可验证奖励的强化学习 (RLVR) 显著提升了大语言模型 (LLMs) 的推理能力。然而，GRPO 和 DAPO 等方法由于依赖于为每个提示词采样大量 rollout，面临着巨大的计算开销。此外，在 RLVR 中，相对优势往往是稀疏的：许多样本趋向于全对或全错，导致组内奖励方差较低，从而产生微弱的学习信号。在本文中，我们提出了 arrol (Accelerating RLVR via online Rollout Pruning)，这是一种在线 rollout 剪枝方法，它在生成过程中对 rollout 进行剪枝，同时显式地引导幸存的样本在正确性上更加平衡，以增强学习信号。具体而言，arrol 实时训练一个轻量级的质量头 (quality head) 来预测部分 rollout 的成功概率，并利用其做出早期剪枝决策。学习到的质量头还可以进一步对候选样本进行加权，以在测试时缩放 (test-time scaling) 期间提高推理准确率。为了提高效率，我们提出了一种系统设计，在推理引擎内部对 rollout 进行剪枝，并对剩余样本进行重新批处理，以进行对数概率计算和策略更新。在 Qwen-3 和 LLaMA-3.2 模型 (1B-8B) 上的 GRPO 和 DAPO 实验表明，arrol 将平均准确率提高了 +2.30 至 +2.99，同时实现了高达 1.7 倍的训练加速，并在测试时缩放中带来了高达 +8.33 的额外平均准确率提升。代码可在 https://github.com/Hsu1023/ARRoL 获取。

## Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) has significantly advanced the reasoning capabilities of Large Language Models (LLMs). However, methods such as GRPO and DAPO suffer from substantial computational cost, since they rely on sampling many rollouts for each prompt. Moreover, in RLVR the relative advantage is often sparse: many samples become nearly all-correct or all-incorrect, yielding low within-group reward variance and thus weak learning signals. In this paper, we introduce arrol (Accelerating RLVR via online Rollout Pruning), an online rollout pruning method that prunes rollouts during generation while explicitly steering the surviving ones more correctness-balanced to enhance learning signals. Specifically, arrol trains a lightweight quality head on-the-fly to predict the success probability of partial rollouts and uses it to make early pruning decisions. The learned quality head can further weigh candidates to improve inference accuracy during test-time scaling. To improve efficiency, we present a system design that prunes rollouts inside the inference engine and re-batches the remaining ones for log-probability computation and policy updates. Across GRPO and DAPO on Qwen-3 and LLaMA-3.2 models (1B-8B), arrol improves average accuracy by +2.30 to +2.99 while achieving up to 1.7x training speedup, and yielding up to +8.33 additional gains in average accuracy in test-time scaling. The code is available at https://github.com/Hsu1023/ARRoL.

---

## 论文详细总结（自动生成）

以下是对论文《Prune as You Generate: Online Rollout Pruning for Faster and Better RLVR》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：具有可验证奖励的强化学习（RLVR，如 GRPO、DAPO）在提升大模型推理能力方面效果显著，但面临两大瓶颈：
    1.  **计算成本极高**：为了估计优势函数，每个 Prompt 需要采样大量 Rollout（生成序列），导致训练极其缓慢。
    2.  **学习信号稀疏/弱**：在二元奖励（0/1）场景下，如果一组样本全部正确或全部错误，组内奖励方差为零，导致策略梯度消失或信号微弱。
*   **研究动机**：探索是否能在生成过程中通过“在线剪枝”减少计算量，同时通过筛选样本来增强学习信号（提高奖励方差）。

### 2. 方法论
**ARRoL (Accelerating RLVR via online Rollout Pruning)** 的核心思想是在生成过程中实时预测样本质量，并据此进行动态剪枝。
*   **质量预测头 (Quality Head)**：在模型骨干网络上添加一个轻量级的 2 层 MLP 探针。它利用中间层的隐藏状态预测当前部分序列最终成功的概率。该头在训练过程中利用最终的真实奖励（0/1）进行在线训练（梯度与主模型解耦）。
*   **在线剪枝策略**：
    *   当生成达到预设长度（如 512 token）时，质量头对所有样本打分。
    *   **概率校准**：使用在线分箱（Binned）估计器将原始分数映射为后验成功概率 $q_i$。
    *   **平衡引导**：不只是剪掉差的，而是通过调整生存概率，使保留下来的样本组在正确性上趋向于平衡（目标比例 $\rho=0.5$），从而最大化组内方差，提供更强的学习信号。
*   **系统设计**：
    *   **后端 (vLLM)**：在推理引擎内部执行剪枝，立即释放被剪枝请求的显存和计算资源，重新调度活跃序列。
    *   **前端 (verl)**：接收幸存样本并重新批处理（Re-batching），仅对幸存者计算对数概率和更新策略。
*   **测试时缩放 (Test-time Scaling)**：训练好的质量头在推理阶段可作为“投票权重”，替代传统的多数投票法（Majority Vote），提升答案聚合的准确率。

### 3. 实验设计
*   **数据集**：使用 Dapo-Math-17K 进行训练。评估涵盖了 Math500、MinervaMath、OlympiadBench 以及竞赛级数据集 AMC’23、AIME’24、AIME’25。
*   **模型**：Qwen-3 系列 (1.7B, 4B, 8B) 和 LLaMA-3.2 (1.7B)。
*   **基准方法 (Benchmarks)**：
    *   **训练对比**：Vanilla GRPO 和 DAPO。
    *   **推理对比**：Majority Vote 和 DeepConf（基于 Log-prob 的置信度方法）。
    *   **消融对比**：随机剪枝 (Random Pruning)。

### 4. 资源与算力
*   **硬件**：实验在 **NVIDIA GH200 GPU** 上运行。
*   **训练细节**：学习率 $1 \times 10^{-6}$，组大小 (Group Size) 为 16，最大序列长度 8192，剪枝检测长度 $L_{detect}=512$。
*   **算力说明**：文中未明确给出总的 GPU 数量和总训练时长，但强调了相对于基准方法的 **1.6x 到 1.7x 的端到端加速**。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 1B 到 8B 不同规模的模型，并在两个主流 RLVR 算法（GRPO/DAPO）上验证。
*   **消融实验**：包括剪枝比例 $\kappa$ 的敏感度分析、不同剪枝策略的对比、训练阶段的耗时分解、以及收敛曲线分析。
*   **充分性评价**：实验设计较为充分且客观。通过在多个难度不一的数学竞赛数据集上测试，证明了该方法在不同难度任务下的鲁棒性。特别是对比了随机剪枝，有力证明了“平衡样本分布”对提升性能的关键作用。

### 6. 主要结论与发现
*   **性能提升**：ARRoL 在减少样本量的情况下，平均准确率反而提升了 **+2.30% 至 +2.99%**。
*   **训练加速**：实现了高达 **1.7 倍** 的端到端训练加速。
*   **推理增强**：在测试时缩放场景下，利用质量头加权投票比 DeepConf 提升了高达 **+8.33%** 的准确率。
*   **信号优化**：实验证实，通过剪枝将组内正负样本比例引导至 0.5 附近，能显著增强学习信号，解决梯度消失问题。

### 7. 优点
*   **“少即是多”**：打破了“采样越多越好”的直觉，证明了高质量、高方差的样本子集对 RL 训练更有利。
*   **系统级优化**：不仅是算法创新，还通过 vLLM 的后端集成实现了真正的墙钟时间（Wall-clock time）加速。
*   **多功能性**：质量头既能用于训练加速，又能用于推理增强，具有极高的实用价值。

### 8. 不足与局限
*   **领域局限性**：目前主要集中在数学等具有“可验证奖励”（Verifiable Rewards）的推理任务，在主观性较强的任务（如创意写作）中是否适用尚待验证。
*   **冷启动依赖**：需要一小段冷启动时间（如 20 步）来初始化质量头和校准器。
*   **检测长度开销**：必须生成到一定长度（如 512 token）才能进行剪枝，对于极短的推理任务，加速效果可能不明显。
*   **偏差风险**：如果质量头在训练早期产生严重偏差，可能会误剪掉潜在的正确路径，影响最终收敛。

（完）
