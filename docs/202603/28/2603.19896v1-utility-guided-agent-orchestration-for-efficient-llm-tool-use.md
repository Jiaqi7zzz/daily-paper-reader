---
title: Utility-Guided Agent Orchestration for Efficient LLM Tool Use
title_zh: 效用引导的智能体编排，旨在实现高效的大语言模型工具使用
authors: "Boyan Liu, Gongming Zhao, Hongli Xu"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19896v1"
tags: ["query:rl-tl"]
score: 9.0
evidence: 包含验证的大模型工具使用效用引导编排
tldr: 本研究针对大语言模型智能体在工具使用中面临的质量与成本权衡问题，提出了一种效用引导的编排策略。该方法将智能体决策视为显式的决策问题，通过平衡预估收益、步骤成本、不确定性和冗余度，在响应、检索、工具调用等操作间进行动态选择。实验证明，这种显式编排信号能有效控制智能体行为，在保证任务性能的同时优化资源消耗，为工具型智能体提供了一个可控且实用的管理框架。
selection_source: fresh_fetch
motivation: 现有的 LLM 智能体工作流在灵活性与执行成本之间难以平衡，缺乏显式的决策机制来优化质量与成本的权衡。
method: 提出一种效用引导的编排策略，通过综合评估收益、成本、不确定性和冗余度，动态选择最优的智能体动作。
result: 实验表明，该策略能通过显式信号显著调节智能体行为，在不同成本约束下实现了比固定工作流和 ReAct 更优的性能表现。
conclusion: 显式的效用引导编排为 LLM 智能体提供了一种实用且可控的机制，能够有效实现工具使用过程中的质量与成本优化。
---

## 摘要
使用工具的大语言模型（LLM）智能体通常在回答质量与执行成本之间面临根本性的权衡。固定的工作流虽然稳定但缺乏灵活性，而像 ReAct 这样自由形式的多步推理方法虽然可能提高任务性能，但代价是过多的工具调用、更长的轨迹、更高的 Token 消耗以及增加的延迟。在本文中，我们将智能体编排视为一个显式的决策问题，而不是完全将其留给提示词层面的行为。我们提出了一种效用引导的编排策略，通过平衡预估收益、步骤成本、不确定性和冗余，在响应、检索、工具调用、验证和停止等动作中进行选择。我们的目标并非追求普适的最佳任务性能，而是提供一个可控且可分析的策略框架，用于研究使用工具的 LLM 智能体在质量与成本之间的权衡。在直接回答、阈值控制、固定工作流、ReAct 以及几种策略变体上的实验表明，显式的编排信号会显著影响智能体的行为。对成本定义、工作流公平性和冗余控制的进一步分析表明，轻量级的效用设计可以为智能体控制提供一种合理且实用的机制。

## Abstract
Tool-using large language model (LLM) agents often face a fundamental tension between answer quality and execution cost. Fixed workflows are stable but inflexible, while free-form multi-step reasoning methods such as ReAct may improve task performance at the expense of excessive tool calls, longer trajectories, higher token consumption, and increased latency. In this paper, we study agent orchestration as an explicit decision problem rather than leaving it entirely to prompt-level behavior. We propose a utility-guided orchestration policy that selects among actions such as respond, retrieve, tool call, verify, and stop by balancing estimated gain, step cost, uncertainty, and redundancy. Our goal is not to claim universally best task performance, but to provide a controllable and analyzable policy framework for studying quality-cost trade-offs in tool-using LLM agents. Experiments across direct answering, threshold control, fixed workflows, ReAct, and several policy variants show that explicit orchestration signals substantially affect agent behavior. Additional analyses on cost definitions, workflow fairness, and redundancy control further demonstrate that lightweight utility design can provide a defensible and practical mechanism for agent control.

---

## 论文详细总结（自动生成）

以下是对论文《Utility-Guided Agent Orchestration for Efficient LLM Tool Use》的结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在使用工具的大语言模型（LLM）智能体中，存在“回答质量”与“执行成本”之间的根本矛盾。
*   **背景问题**：
    *   **固定工作流**：稳定但僵化，无法根据任务难度或中间证据动态调整。
    *   **自由形式推理（如 ReAct）**：灵活但往往导致过度执行，产生冗余的工具调用、长轨迹、高 Token 消耗和高延迟。
*   **核心目标**：将智能体编排（Orchestration）从一种隐含的提示词行为转变为一个**显式的决策问题**，通过效用函数来量化“下一步行动是否值得”，从而实现质量与成本的可控权衡。

### 2. 论文提出的方法论
*   **核心思想**：引入一个轻量级的效用引导编排层，在每个步骤中评估候选动作的综合效用，选择效用最高的动作。
*   **动作空间**：定义了五个核心动作：`respond`（直接回答）、`retrieve`（检索）、`tool_call`（调用工具）、`verify`（验证证据）、`stop`（停止）。
*   **效用公式**：
    $$U(a | s_t) = Gain(a | s_t) - \lambda_1 StepCost(a | s_t) - \lambda_2 Uncertainty(a | s_t) - \lambda_3 Redundancy(a | s_t)$$
    *   **Estimated Gain (预估收益)**：自评该动作对改善最终答案的潜在贡献。
    *   **Step Cost (步骤成本)**：对执行成本的归一化代理指标，用于抑制无意义的轨迹扩张。
    *   **Uncertainty (不确定性)**：评估当前证据是否充足，高不确定性会促使更多检索或验证。
    *   **Redundancy (冗余度)**：惩罚重复或语义相似的动作，以压缩执行轨迹。
*   **执行流程**：构建状态表示 -> 计算效用得分 -> 选择动作 -> 更新状态并观察环境 -> 循环直至触发停止动作或达到预算上限。

### 3. 实验设计
*   **数据集与场景**：使用 **HotpotQA** 开发集的 200 个随机样本进行评估。
*   **Benchmark 与对比方法**：
    *   **Direct**：直接回答。
    *   **Fixed Workflows**：包括 minimal、search-twice、search-verify 等不同复杂度的固定流程。
    *   **Threshold Control**：基于阈值的简单控制器。
    *   **ReAct**：经典的推理-行动交替模式。
    *   **Policy Variants**：本文提出的策略在不同成本定义（Token、延迟、步骤）下的变体。
*   **评估指标**：F1 分数（质量）、Token 消耗、墙钟时间（Wall-clock Time）、效率得分（F1/Tokens）。

### 4. 资源与算力
*   **模型使用**：实验采用了 **Llama-3.1-8B** 作为基础模型。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或训练/推理的总时长。由于该方法主要基于推理时的启发式策略而非重度强化学习训练，其算力需求主要集中在 LLM 的推理调用上。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了主实验对比（质量-成本帕累托前沿分析）。
    *   **消融实验**：分别移除收益、不确定性、冗余和停止控制，验证各组件必要性。
    *   **深度分析**：包括推理深度对 F1 的影响、不同成本定义（Token vs 延迟）的有效性分析、工作流公平性分析、以及语义冗余控制的效果。
*   **充分性评价**：实验设计较为全面，不仅关注最终性能，还深入探讨了内部启发式信号（如 Gain 与 Continue Rate）的相关性，证明了策略的非随机性。对比基准涵盖了从简单到复杂的多种模式，具有较好的客观性和公平性。

### 6. 论文的主要结论与发现
*   **显式编排的有效性**：效用引导的策略能有效调节智能体行为，在质量与成本之间找到比固定工作流更优的平衡点。
*   **成本代理的合理性**：使用简单的“步骤成本”作为代理指标，在行为导向上与真实的 Token 成本和延迟成本高度一致。
*   **冗余控制的价值**：引入语义冗余惩罚能显著压缩 Token 消耗和工具调用次数，而几乎不损失 F1 分数。
*   **自评信号的局限**：LLM 生成的“预估收益”与最终表现有正相关性（0.1479），但“不确定性”信号的校准度较低，仍有改进空间。

### 7. 优点
*   **解耦控制与推理**：将编排逻辑从复杂的 Prompt 中抽离，使智能体行为更具可解释性和可调优性。
*   **轻量级且实用**：不需要复杂的强化学习训练，通过启发式效用设计即可在预算敏感的场景中部署。
*   **多维度权衡**：提供了分析质量、Token、延迟和冗余之间复杂关系的框架。

### 8. 不足与局限
*   **启发式依赖**：效用组件（Gain, Uncertainty）依赖于 LLM 的自评，若模型自评能力差（如小模型），策略效果会受限。
*   **性能上限**：在绝对 F1 分数上，该策略并未全面超越不受限的 ReAct，说明在追求效率时确实牺牲了一定的性能上限。
*   **延迟开销**：引入语义冗余检查等复杂效用计算时，虽然节省了 Token，但可能增加额外的计算延迟。
*   **泛化性验证**：实验仅限于 HotpotQA 这一种任务类型，在更长程、更复杂的任务环境中的表现尚待验证。

（完）
