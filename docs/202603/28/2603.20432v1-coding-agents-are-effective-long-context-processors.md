---
title: Coding Agents are Effective Long-Context Processors
title_zh: 编程智能体是有效的长上下文处理器
authors: "Weili Cao, Xunjian Yin, Bhuwan Dhingra, Shuyan Zhou"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20432v1"
tags: ["query:rl-tl"]
score: 8.0
evidence: 编码智能体使用原生工具进行长文本处理
tldr: "针对大语言模型在处理超长上下文时性能下降且注意力机制不可解释的问题，本文提出将长文本处理从隐式的注意力机制转变为显式的编码代理交互。通过让编码代理利用文件系统组织文本并使用原生工具（如代码和终端命令）进行操作，该方法在长文本推理、RAG和大规模问答任务中表现优异，平均超越现有最先进水平17.3%，为长文本处理提供了除扩展上下文窗口之外的新路径。"
selection_source: fresh_fetch
motivation: 现有的LLM在处理长上下文时依赖不可解释的注意力机制，且随着长度增加性能显著下降。
method: 将编码代理作为通用接口，利用文件系统管理文本并调用可执行代码和终端工具进行显式处理。
result: "在多个基准测试中，该方法平均超越现有最先进技术17.3%，并能处理高达三万亿token的大规模语料。"
conclusion: 将长文本处理委托给具备工具使用和文件系统导航能力的编码代理，是替代语义搜索或单纯扩展上下文窗口的有效方案。
---

## 摘要
大语言模型（LLMs）在扩展以访问海量上下文方面取得了显著进展。然而，这种访问是通过潜在且不可解释的注意力机制实现的，且 LLMs 无法有效处理长上下文，随着上下文长度的增加，性能会出现显著下降。在这项工作中，我们研究了是否可以通过允许编程智能体在文件系统中组织文本并使用其原生工具进行操作，将长上下文处理从潜在注意力外部化为显式的、可执行的交互。我们评估了现成的尖端编程智能体作为处理长上下文任务的通用接口，包括长上下文推理、检索增强生成以及包含高达三万亿 token 的大规模语料库的开放域问答。在多个基准测试中，这些智能体的表现平均优于已发表的当前最优水平（SOTA）17.3%。我们将这种效能归因于两个关键因素：原生工具熟练度，使智能体能够利用可执行代码和终端命令而非被动的语义查询；以及文件系统熟悉度，使其能够将海量文本语料库作为目录结构进行导航。这些发现表明，将长上下文处理委托给编程智能体为语义搜索或上下文窗口扩展提供了一种有效的替代方案，为 LLMs 的长上下文处理开辟了新方向。

## Abstract
Large Language Models (LLMs) have demonstrated remarkable progress in scaling to access massive contexts. However, the access is via the latent and uninterpretable attention mechanisms, and LLMs fail to effective process long context, exhibiting significant performance degradation as context length increases. In this work, we study whether long-context processing can be externalized from latent attention into explicit, executable interactions, by allowing coding agents to organize text in file systems and manipulate it using its native tools. We evaluate off-the-shelf frontier coding agents as the general interface for tasks that require processing long contexts, including long-context reasoning, retrieval-augmented generation, and open-domain question answering with large-scale corpus contains up to three trillion tokens. Across multiple benchmarks, these agents outperform published state-of-the-art by 17.3% on average. We attribute this efficacy to two key factors: native tool proficiency, which enables agents to leverage executable code and terminal commands rather than passive semantic queries, and file system familiarity, which allows them to navigate massive text corpora as directory structures. These findings suggest that delegating long-context processing to coding agents offers an effective alternative to semantic search or context window scaling, opening new directions for long-context processing in LLMs.

---

## 论文详细总结（自动生成）

### 论文总结：编程智能体是有效的长上下文处理器

#### 1. 核心问题与整体含义（研究动机和背景）
现代应用要求大语言模型（LLM）处理海量语料（如科学档案或网页级文本）。尽管 LLM 的上下文窗口已扩展至百万级，但仍面临两大挑战：
*   **性能衰减（Context Rot）**：随着上下文长度增加，模型处理信息的有效性显著下降，且注意力机制具有潜在性和不可解释性。
*   **检索局限性**：传统的检索增强生成（RAG）依赖固定的、浅层的检索机制，难以支持复杂的多跳推理。
**核心动机**：研究是否可以将长上下文处理从“隐式注意力”转向“显式交互”，利用编程智能体（Coding Agents）在文件系统中组织和操作文本的能力，来解决超长文本的处理难题。

#### 2. 方法论：核心思想与技术细节
论文提出将**长文本处理重新定义为文件系统导航与操作任务**：
*   **核心思想**：利用编程智能体在处理大型代码库时积累的经验（如处理长文件、理解层级结构），将其迁移到通用文本处理中。
*   **语料库格式化**：
    *   对于大规模语料（>100M tokens），将每个文档存为独立的 `.txt` 文件，并组织在目录层级中。
    *   对于单篇长文档，将其存为一个 `.txt` 文件。
*   **智能体接口**：智能体仅接收文件/目录路径和查询。它被允许自由使用原生工具：
    *   **终端命令**：如 `grep`（搜索）、`sed`（提取特定行）、`nl`（行号索引）、`head/tail`。
    *   **Python 脚本**：编写自定义脚本进行程序化搜索、正则匹配和数据聚合。
    *   **迭代执行**：根据中间发现的结果，自主决定下一步操作（如修正查询、深入读取）。
*   **自主性**：不预设 RAG 流程，由智能体自主决定是顺序扫描、关键词搜索还是编写解析脚本。

#### 3. 实验设计
*   **数据集与场景**：
    *   **BrowseComp-Plus (750M tokens)**：网页浏览基准，需在 10 万个文档中进行多跳推理。
    *   **LongBench-v2 (188K tokens)**：涵盖单/多文档 QA、代码仓库理解等。
    *   **Oolong (Real/Synthetic, 385K-536K tokens)**：评估长上下文推理和信息聚合能力。
    *   **Natural Questions (NQ, 3T tokens)**：超大规模 Wikipedia 语料库的开放域问答。
*   **对比方法**：
    *   **GPT-5 Full Context**：直接将全文（或滑动窗口）输入模型。
    *   **标准 RAG**：使用 Gemini embeddings 进行检索。
    *   **ReAct Agent**：受限在预定义工具 API（检索、获取文档）中的智能体。
    *   **RLM (Recursive Language Model)**：通过递归调用自身处理文本片段的方法。
*   **评估指标**：准确率（Accuracy）、精确匹配（EM）及 Oolong 专用评分。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **原因**：该研究主要评估的是“现成（off-the-shelf）”的尖端模型 API（如 OpenAI 的 GPT-5 驱动的 Codex、Anthropic 的 Claude 3.5 Sonnet 驱动的 Claude Code）。
*   **成本分析**：文中详细列出了每条查询的平均推理成本（Table 5），显示编程智能体的成本高于 RAG，但通常低于或等同于其他强力基线（如 RLM 或全上下文 GPT-5）。

#### 5. 实验数量与充分性
*   **实验规模**：在 5 个不同尺度的基准测试上进行了实验，上下文跨度从 18.8 万到 3 万亿 token。
*   **消融实验**：
    *   **文件系统结构**：对比了“层级文件夹”与“单 JSON 文件”的影响，证明了文件系统熟悉度的重要性。
    *   **检索工具影响**：测试了加入 BM25 或向量检索是否能增强智能体。
    *   **策略分析**：量化分析了智能体在不同任务中的搜索强度、读取量和代码生成量。
*   **充分性评价**：实验设计较为全面，涵盖了不同任务类型（检索密集型 vs. 聚合密集型），并对比了多种主流长文本处理范式，结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **性能卓越**：编程智能体在 4/5 个基准上刷新了 SOTA，平均性能提升 **17.3%**。
*   **关键成功因素**：
    1.  **原生工具熟练度**：能够使用可执行代码进行精确交互，而非模糊的语义查询。
    2.  **文件系统熟悉度**：将语料库视为目录结构，利用“索引+切片（index and slice）”策略高效定位信息。
*   **涌现的自适应策略**：
    *   **多跳任务**：表现出“迭代查询优化”行为。
    *   **分析聚合任务**：自动转向“程序化聚合”（编写 Python 脚本进行正则匹配和统计）。
*   **反直觉发现**：给编程智能体配备标准的检索工具（如向量检索）并不总能提升性能，有时反而会抑制其原生的探索行为。

#### 7. 优点
*   **通用性强**：无需针对特定任务进行微调或架构修改，直接利用现有的编程能力。
*   **可解释性高**：处理过程通过显式的终端命令和代码体现，比注意力机制更易追踪。
*   **灵活性**：能够根据任务需求动态调整策略（搜索 vs. 编写代码 vs. 直接阅读）。
*   **突破尺度限制**：成功处理了高达 3 万亿 token 的语料，远超当前 LLM 的原生上下文窗口。

#### 8. 不足与局限
*   **成本与延迟**：相比轻量级的 RAG，编程智能体需要多次迭代，推理成本和响应时间较高。
*   **工具干扰风险**：实验显示，集成外部检索工具可能导致智能体过度依赖检索器而忽略更深层的原生探索。
*   **对模型能力依赖度高**：该方法高度依赖于模型本身的编程和工具调用水平，对于非编程优化的模型效果可能有限。
*   **环境限制**：需要一个安全的可执行环境（沙箱）来运行智能体生成的代码和命令。

（完）
