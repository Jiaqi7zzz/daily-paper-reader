---
title: "DyJR: Preserving Diversity in Reinforcement Learning with Verifiable Rewards via Dynamic Jensen-Shannon Replay"
title_zh: DyJR：通过动态 Jensen-Shannon 重放保持具有可验证奖励的强化学习中的多样性
authors: "Long Li, Zhijian Zhou, Tianyi Wang, Weidi Xu, Zuming Huang, Wei Chu, Zhe Wang, Shirui Pan, Chao Qu, Yuan Qi"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16157v1"
tags: ["query:rl-tl"]
score: 9.0
evidence: 在推理中使用动态回放和可验证奖励的强化学习
tldr: 针对强化学习（如GRPO）在提升大模型推理能力时存在的样本效率低和多样性缺失问题，本文提出DyJR框架。该框架通过动态Jensen-Shannon回放机制，利用时间敏感动态缓冲区保留近期样本，并结合JS散度正则化约束分布，在提高训练效率的同时防止模式崩溃。实验证明DyJR在数学推理和Text-to-SQL任务上显著优于现有基准，有效维持了生成多样性。
selection_source: fresh_fetch
motivation: 现有的经验回放方法在提升强化学习效率时，往往因过度拟合准确样本而导致模型出现模式崩溃和多样性丧失。
method: 提出DyJR框架，利用时间敏感动态缓冲区同步模型演化，并通过Jensen-Shannon散度正则化代替直接梯度更新以维持分布多样性。
result: 在数学推理和Text-to-SQL基准测试中，DyJR在性能上显著优于GRPO及RLEP等基准，且训练效率与原始GRPO相当。
conclusion: DyJR通过动态参考分布约束，成功解决了强化学习中准确性与多样性的平衡问题，有效缓解了模型对高概率Token的过度依赖。
---

## 摘要
虽然强化学习（RL）增强了大语言模型的推理能力，但像 GRPO 这样的同策略算法由于丢弃了过去的采样，导致样本效率低下。现有的经验重放方法通过重用准确的样本进行直接策略更新来解决这一问题，但这通常会带来高昂的计算成本，并因过拟合导致模式崩溃。我们认为，历史数据应优先用于维持多样性，而非仅仅是强化准确性。为此，我们提出了动态 Jensen-Shannon 重放（DyJR），这是一个简单且有效的正则化框架，它利用来自近期轨迹的动态参考分布。DyJR 引入了两项创新：（1）时间敏感型动态缓冲区，采用先进先出（FIFO）和自适应大小调整，仅保留时间上邻近的样本，从而与模型演进同步；（2）Jensen-Shannon 散度正则化，用分布约束取代直接梯度更新，以防止多样性崩溃。在数学推理和 Text-to-SQL 基准测试上的实验表明，DyJR 显著优于 GRPO 以及 RLEP 和 Ex-GRPO 等基准方法，同时保持了与原始 GRPO 相当的训练效率。此外，从 Rank-k 词元概率演进的角度来看，我们展示了 DyJR 增强了多样性并减轻了对 Rank-1 词元的过度依赖，阐明了 DyJR 的特定子模块如何影响训练动态。

## Abstract
While Reinforcement Learning (RL) enhances Large Language Model reasoning, on-policy algorithms like GRPO are sample-inefficient as they discard past rollouts. Existing experience replay methods address this by reusing accurate samples for direct policy updates, but this often incurs high computational costs and causes mode collapse via overfitting. We argue that historical data should prioritize sustaining diversity rather than simply reinforcing accuracy. To this end, we propose Dynamic Jensen-Shannon Replay (DyJR), a simple yet effective regularization framework using a dynamic reference distribution from recent trajectories. DyJR introduces two innovations: (1) A Time-Sensitive Dynamic Buffer that uses FIFO and adaptive sizing to retain only temporally proximal samples, synchronizing with model evolution; and (2) Jensen-Shannon Divergence Regularization, which replaces direct gradient updates with a distributional constraint to prevent diversity collapse. Experiments on mathematical reasoning and Text-to-SQL benchmarks demonstrate that DyJR significantly outperforms GRPO as well as baselines such as RLEP and Ex-GRPO, while maintaining training efficiency comparable to the original GRPO. Furthermore, from the perspective of Rank-$k$ token probability evolution, we show that DyJR enhances diversity and mitigates over-reliance on Rank-1 tokens, elucidating how specific sub-modules of DyJR influence the training dynamics.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《DyJR: Preserving Diversity in Reinforcement Learning with Verifiable Rewards via Dynamic Jensen-Shannon Replay》** 的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
在利用强化学习（RL）提升大语言模型（LLM）推理能力的过程中，现有的同策略（On-policy）算法（如 GRPO）存在**样本效率低下**的问题，因为它们在每次更新后都会丢弃昂贵的采样数据。虽然现有的“经验回放”（Experience Replay）技术尝试通过重用历史成功样本来解决此问题，但作者指出这些方法存在两个核心误区：
*   **过度强化准确性导致模式崩溃**：直接最大化历史轨迹的似然概率会使模型过拟合特定的解题路径，迅速丧失探索能力。
*   **存储开销巨大**：传统方法往往需要维护海量缓冲区，造成极高的显存负担。
**核心动机**：重新定义经验回放在推理任务中的角色——历史数据不应仅用于强化准确性，而应作为**维持生成多样性**的正则化约束。

### 2. 方法论：核心思想与关键技术
作者提出了 **DyJR (Dynamic Jensen-Shannon Replay)** 框架，其核心思想是利用动态参考分布来约束模型，防止其在训练早期陷入单一模式。

*   **时间敏感型动态缓冲区 (Time-Sensitive Dynamic Buffer)**：
    *   **FIFO 机制**：仅保留最近 $M$ 步（默认 $M=8$）生成的正确样本，确保参考分布与当前模型状态同步。
    *   **自适应数据选择**：采用“由高到低”的置信度筛选策略，优先录取高置信度样本，但在任务困难时自动放宽标准以防止数据饥饿。
    *   **初期多样性保护**：在训练前 20 步（模型熵值剧烈波动的阶段），将采样率从 5% 临时提升至 20%，以捕获更多高熵的探索模式。
*   **Jensen-Shannon (JS) 散度正则化**：
    *   **核心公式**：总损失函数定义为 $L_{total} = L_{GRPO} + \alpha_{JS} \cdot L_{JS}$。
    *   **技术细节**：不同于直接的梯度更新，DyJR 计算当前策略与缓冲区样本分布之间的 JS 散度。JS 散度具有对称性和有界性，相比 Forward KL 散度，它能更稳健地处理非平稳的历史数据，避免模型过度平滑或产生训练不稳定性。

### 3. 实验设计
*   **任务场景**：
    1.  **长链数学推理**：使用 Qwen3-4B-Base 模型，在 Reinforce-Ada-Hard 数据集上训练。
    2.  **SQL 代码生成**：使用 Llama-3.1-8B-Instruct 模型，在 BIRD 数据集上训练。
*   **Benchmark (评估基准)**：
    *   数学类：AIME25, AMC23, Beyond AIME, BRUMO25, HMMT25, Minerva。
    *   SQL 类：BIRD, Spider。
*   **对比方法**：
    *   基础算法：GRPO。
    *   变体/竞争对手：DAPO, DPH-RL (静态 JS 约束), RLEP (传统回放), Ex-GRPO (基于熵的回放)。

### 4. 资源与算力
*   **硬件配置**：论文明确指出实验使用了 **32 张 NVIDIA A800 GPU**。
*   **并行策略**：采用了张量并行（TP）和序列并行（SP）均为 1 的配置。
*   **训练效率**：DyJR 的显存开销极低（缓冲区索引占用显存小于 1GB），且由于回放样本量远小于在线采样量，其训练吞吐量（ms/token）与原始 GRPO 几乎持平。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个不同领域（数学与 SQL）、两种不同架构（Qwen 与 Llama）的模型。
*   **消融实验**：非常详尽，包括对正则化系数 $\alpha_{JS}$、缓冲区最大寿命 $M$、散度类型（JS vs. Forward KL）以及数据选择策略的深入对比。
*   **动态分析**：作者通过追踪 **Rank-k Token 概率演化**（Rank-1 到 Rank-3），直观展示了 DyJR 如何在训练后期将概率质量从 Rank-1 重新分配到 Rank-2/3，从而证明了其维持多样性的能力。
*   **客观性**：实验采用了多次独立运行取均值和标准差的方式，对比基准涵盖了最新的同类研究，具有较高的说服力。

### 6. 主要结论与发现
*   **性能突破**：DyJR 在数学任务上的平均准确率达到 34.1%，显著超过 GRPO 基准（29.8%）和其他回放方法。
*   **多样性是关键**：实验证明，历史数据的价值在于其包含的早期探索模式。如果只保留后期高准确率但低熵的数据，模型性能反而会下降。
*   **JS 散度的优越性**：在处理来自快速演化模型的混合数据时，JS 散度比 Forward KL 更能有效防止模式覆盖（Mode-covering）带来的过平滑问题。
*   **可扩展性**：DyJR 在 Pass@k 评估中表现出更强的扩展性，随着采样预算 $k$ 的增加，性能提升幅度远超基准。

### 7. 优点
*   **理念新颖**：将经验回放的目标从“强化准确性”转向“保护多样性”，为 RLVR 训练提供了新视角。
*   **极高效率**：通过动态收缩缓冲区和低比例回放，在几乎不增加计算成本和显存负担的前提下，实现了显著的性能提升。
*   **理论与实践结合**：通过 Rank-k 概率演化图深入解释了模型内部的训练动力学，使结论不仅停留在指标提升上。

### 8. 不足与局限
*   **任务局限性**：该方法主要针对具有**可验证奖励（Verifiable Rewards）**的任务（如数学、代码），对于奖励信号模糊或主观性强的任务（如创意写作、开放域对话）是否有效尚未验证。
*   **超参数敏感性**：实验显示性能对 $\alpha_{JS}$ 和 $M$ 具有一定的敏感性（呈倒 U 型趋势），在不同规模的模型上可能需要重新调优。
*   **初期依赖**：方法高度依赖训练初期的探索质量，如果基础模型在初期完全无法生成正确样本，该机制可能失效。

（完）
