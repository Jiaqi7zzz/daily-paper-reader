---
title: Offline Exploration-Aware Fine-Tuning for Long-Chain Mathematical Reasoning
title_zh: 面向长链数学推理的离线探索感知微调
authors: "Yongyu Mu, Jiali Zeng, Fandong Meng, JingBo Zhu, Tong Xiao"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16206v1"
tags: ["query:rl-tl"]
score: 9.0
evidence: 将具有可验证奖励的强化学习 (RLVR) 用于数学推理
tldr: 针对大语言模型在长链数学推理中强化学习初始化不足的问题，本文提出了一种离线探索感知微调方法（OXA）。该方法通过强化低置信度的正确教师数据并抑制高置信度的错误自生成数据，优化了监督微调阶段的探索能力。实验证明，OXA显著提升了模型在多个基准测试中的推理性能，并为后续强化学习阶段提供了更优的策略起点。
selection_source: fresh_fetch
motivation: 现有的数学推理研究多关注强化学习阶段的探索，而忽视了监督微调阶段对后续探索空间的塑造作用。
method: 提出OXA方法，通过促进低置信度的正确教师数据以吸收新模式，并抑制高置信度的错误自生成数据以重定向概率分布。
result: "在Qwen2.5-1.5B-Math模型上，OXA相比传统SFT在Pass@1和Pass@k指标上分别平均提升了6个和5个百分点。"
conclusion: OXA通过提高初始策略熵和优化探索景观，为长链数学推理任务提供了持久且显著的性能增益。
---

## 摘要
通过鼓励自我探索，基于可验证奖励的强化学习 (RLVR) 显著提升了大语言模型的数学推理能力。作为 RLVR 的起点，有监督微调 (SFT) 记忆新思维链轨迹的能力提供了一个关键的初始化，塑造了后续的探索格局。然而，现有研究主要集中在促进 RLVR 训练期间的探索，而对探索感知型 SFT 的研究尚不充分。为了填补这一空白，我们提出了离线探索感知 (OXA) 微调。具体而言，OXA 优化了两个目标：提升低置信度的已验证教师蒸馏数据，以内部化先前未捕获的推理模式；抑制高置信度的错误自我蒸馏数据，从而将错误模式的概率质量重新分配给潜在的正确候选。在 6 个基准测试上的实验结果表明，OXA 持续提升了数学推理性能，特别是在 Qwen2.5-1.5B-Math 上，与传统 SFT 相比，平均获得了 +6 Pass@1 和 +5 Pass@k 的提升。至关重要的是，OXA 提高了初始策略熵，且性能增益在广泛的 RLVR 训练中持续存在，证明了 OXA 的长期价值。

## Abstract
Through encouraging self-exploration, reinforcement learning from verifiable rewards (RLVR) has significantly advanced the mathematical reasoning capabilities of large language models. As the starting point for RLVR, the capacity of supervised fine-tuning (SFT) to memorize new chain-of-thought trajectories provides a crucial initialization that shapes the subsequent exploration landscape. However, existing research primarily focuses on facilitating exploration during RLVR training, leaving exploration-aware SFT under-explored. To bridge this gap, we propose Offline eXploration-Aware (OXA) fine-tuning. Specifically, OXA optimizes two objectives: promoting low-confidence verified teacher-distillation data to internalize previously uncaptured reasoning patterns, and suppressing high-confidence incorrect self-distillation data to redistribute probability mass of incorrect patterns toward potentially correct candidates. Experimental results across 6 benchmarks show that OXA consistently improves mathematical reasoning performance, especially achieving an average gain of $+6$ Pass@1 and $+5$ Pass@$k$ points compared to conventional SFT on the Qwen2.5-1.5B-Math. Crucially, OXA elevates initial policy entropy, and performance gains persist throughout extensive RLVR training, demonstrating the long-term value of OXA.

---

## 论文详细总结（自动生成）

这是一份关于论文《Offline Exploration-Aware Fine-Tuning for Long-Chain Mathematical Reasoning》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究背景**：大语言模型（LLM）在长链数学推理上的突破通常依赖“监督微调（SFT）+ 强化学习（RLVR）”的两阶段流程。
*   **核心问题**：现有的研究大多集中在如何在 RL 阶段促进探索，而忽视了 SFT 阶段的作用。传统的 SFT 往往会导致“熵崩溃（Entropy Collapse）”——模型会过度拟合训练数据，导致概率分布变得极其尖锐（过于自信），从而收窄了后续 RL 阶段的探索空间。
*   **研究动机**：作者认为理想的 RL 初始化（即 SFT 后的模型）应具备两个特征：**高初始准确率**和**高初始策略熵**。本文旨在通过改进 SFT 策略，为 RL 阶段塑造一个更有利于探索的“景观”。

### 2. 方法论：OXA 微调
作者提出了 **OXA (Offline eXploration-Aware)** 微调算法，核心思想是“反向对抗熵崩溃”，具体包含两个解耦的目标：

*   **促进低置信度的正确教师数据 (Promote Low-Confidence Truths)**：
    *   **逻辑**：利用教师模型生成的正确推理轨迹，但优先选择当前模型感到“困惑”（高困惑度 PPL）的样本。
    *   **技术细节**：设计了**高斯引导的 PPL 采样算法**。它不是简单选择最难的样本（防止模型无法收敛），而是根据预设的高斯分布在特定 PPL 区间内采样，确保模型内部化那些处于分布边缘、此前未掌握的推理模式。
    *   **损失函数**：使用标准的最大似然估计（MLE）交叉熵损失 $L_{CE}$。
*   **抑制高置信度的错误自生成数据 (Suppress High-Confidence Errors)**：
    *   **逻辑**：让模型生成推理轨迹，识别出那些模型非常自信（低 PPL）但结果错误的样本。
    *   **技术细节**：使用**非似然损失 (Unlikelihood Loss, UL)** 来惩罚这些错误的“高峰”，迫使模型将概率质量重新分配给其他潜在正确的候选路径。
    *   **损失函数**：$L_{UL} = -\log(1 - p(s_t|s_{<t}))$。
*   **全局目标**：$L = L_{CE} + \alpha \cdot L_{UL}$。其中 $\alpha$ 设为极小值（如 $10^{-4}$），以防止梯度爆炸。

### 3. 实验设计
*   **模型**：Qwen2.5-1.5B/7B-Math（主实验）、LLaMA3.2-3B-Base、Qwen3-1.7B-Base。
*   **数据集**：
    *   **SFT 阶段**：AceReason-1.1-SFT（260 万样本中筛选出 5 万正确样本和 5 万错误样本）。
    *   **RL 阶段**：DeepScaleR-40K 的子集。
*   **Benchmark**：AIME24、AIME25、BRUMO25、CMIMC25、HMMT25、Minerva（涵盖竞赛级难度）。
*   **对比方法**：
    *   **Base**：原始预训练模型。
    *   **SFT**：常规随机采样 SFT。
    *   **SFT LP**：优先选择低 PPL（模型已掌握）数据的 SFT（作为 OXA 的反面参考）。

### 4. 资源与算力
*   **软件框架**：使用 LLaMA-Factory 进行微调，Verl 进行强化学习，vLLM/SGLang 进行推理加速。
*   **算力细节**：文中**未明确列出具体的 GPU 型号和数量**。
*   **训练参数**：SFT 阶段 Batch Size 为 128，训练 6 个 Epoch；RL 阶段 Batch Size 为 64，更新 1200-1600 步，最大输出长度 16,384 标记。

### 5. 实验数量与充分性
*   **实验规模**：
    *   跨越了 4 个不同架构和规模的模型（1.5B 到 7B）。
    *   在 6 个主流数学基准上进行了测试。
    *   进行了 RL 全流程验证，证明了 SFT 阶段的增益可以持续到 RL 结束。
*   **消融实验**：非常充分。包括 PPL 采样区间的影响、$\alpha$ 权重的敏感性分析、数据规模扩展性分析（从 5 万扩展到 15 万）、以及对 OOD（领域外）任务（如 GPQA 博士级科学问题）的泛化测试。
*   **公平性**：所有对比方法在相同的数据量（50k）和超参数下进行，确保了结果的客观性。

### 6. 主要结论与发现
*   **性能飞跃**：在 1.5B 模型上，OXA 相比传统 SFT 平均提升了 **+6 Pass@1** 和 **+5 Pass@k**。
*   **RL 潜力释放**：OXA 模型在 RL 训练初期拥有更高的策略熵，且在整个 RL 过程中始终保持领先，证明了“好的开始是成功的一半”。
*   **数据效率**：仅使用 15 万精选数据的 OXA 模型，性能接近在数百万数据上训练的强力基准（如 ARN-1.1-SFT）。
*   **泛化能力**：OXA 不仅提升了数学能力，在 GPQA 等复杂推理任务上也表现出更强的泛化性。

### 7. 优点
*   **视角独特**：首次系统探讨了如何通过 SFT 阶段的离线数据选择来优化 RL 的探索空间。
*   **算法优雅**：通过 PPL 采样和 Unlikelihood Loss 巧妙地解决了熵崩溃问题，且不改变 RL 框架，具有很强的通用性。
*   **正交性强**：实验证明 OXA 可以与现有的 RL 增强方法（如 Clip-Cov）叠加使用，产生累积增益。

### 8. 不足与局限
*   **计算开销**：相比普通 SFT，OXA 需要额外的自蒸馏过程（模型生成数据并计算 PPL），增加了离线数据准备的时间和算力成本。
*   **模型规模限制**：实验最高仅验证到 7B 规模，虽然作者推测在更大模型上效果会更明显，但缺乏 30B 或 70B 级别模型的实证数据。
*   **领域局限**：目前仅在数学推理领域验证，尚未在代码生成、逻辑推理等其他长链推理任务中测试。

（完）
