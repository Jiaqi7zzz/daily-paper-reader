---
title: "TRUST-SQL: Tool-Integrated Multi-Turn Reinforcement Learning for Text-to-SQL over Unknown Schemas"
title_zh: TRUST-SQL：面向未知模式的工具集成多轮强化学习 Text-to-SQL 框架
authors: "Ai Jian, Xiaoyun Zhang, Wanrou Du, Jingqing Ruan, Jiangbo Pei, Weipeng Zhang, Ke Zeng, Xunliang Cai"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16448v1"
tags: ["query:rl-tl"]
score: 9.0
evidence: 利用验证元数据进行工具集成推理的多轮强化学习
tldr: 针对现实中数据库模式庞大且未知的挑战，本文提出TRUST-SQL框架。该框架将Text-to-SQL建模为部分可观测马尔可夫决策过程，通过四阶段协议引导智能体主动探索元数据。结合创新的双轨GRPO强化学习策略，利用令牌级掩码优势有效解决了信用分配问题。实验表明，该方法在无需预加载模式的情况下，性能显著优于基础模型，并能媲美甚至超越依赖模式预填的强基准模型。
selection_source: fresh_fetch
motivation: 传统的Text-to-SQL研究假设已知完整模式，但在处理包含海量噪声元数据的真实企业级数据库时，这种假设并不成立。
method: 提出一种集成工具的四阶段协议，并采用双轨GRPO策略，通过令牌级掩码优势将探索奖励与执行结果分离以优化强化学习。
result: "在五个基准测试中，4B和8B模型变体分别实现了30.6%和16.6%的平均绝对性能提升，且表现优于依赖模式预填的基准。"
conclusion: TRUST-SQL证明了通过结构化工具集成和改进的强化学习策略，智能体可以在不预知数据库模式的情况下实现高效且准确的SQL生成。
---

## 摘要
Text-to-SQL 解析在“全模式假设”下取得了显著进展。然而，这一前提在现实世界的企业环境中往往失效，因为那里的数据库包含数百张表以及海量的噪声元数据。智能体必须主动识别并验证相关的子集，而不是预先注入完整的模式，这便产生了我们在本研究中探讨的“未知模式”场景。为此，我们提出了 TRUST-SQL（通过工具在未知模式下进行真实推理）。我们将该任务建模为一个部分可观测马尔可夫决策过程（POMDP），其中我们的自主智能体采用结构化的四阶段协议，将推理建立在经过验证的元数据之上。至关重要的是，该协议为我们新颖的双轨 GRPO（Dual-Track GRPO）策略提供了结构边界。通过应用 Token 级的掩码优势，该策略将探索奖励与执行结果隔离开来，以解决信用分配问题，相比标准 GRPO 实现了 9.9% 的相对提升。在五个基准测试上的广泛实验表明，TRUST-SQL 的 4B 和 8B 变体相较于其基础模型，平均绝对提升分别达到了 30.6% 和 16.6%。值得注意的是，尽管在完全没有预加载元数据的情况下运行，我们的框架仍能持续匹配或超越依赖模式预填充的强基准模型。

## Abstract
Text-to-SQL parsing has achieved remarkable progress under the Full Schema Assumption. However, this premise fails in real-world enterprise environments where databases contain hundreds of tables with massive noisy metadata. Rather than injecting the full schema upfront, an agent must actively identify and verify only the relevant subset, giving rise to the Unknown Schema scenario we study in this work. To address this, we propose TRUST-SQL (Truthful Reasoning with Unknown Schema via Tools). We formulate the task as a Partially Observable Markov Decision Process where our autonomous agent employs a structured four-phase protocol to ground reasoning in verified metadata. Crucially, this protocol provides a structural boundary for our novel Dual-Track GRPO strategy. By applying token-level masked advantages, this strategy isolates exploration rewards from execution outcomes to resolve credit assignment, yielding a 9.9% relative improvement over standard GRPO. Extensive experiments across five benchmarks demonstrate that TRUST-SQL achieves an average absolute improvement of 30.6% and 16.6% for the 4B and 8B variants respectively over their base models. Remarkably, despite operating entirely without pre-loaded metadata, our framework consistently matches or surpasses strong baselines that rely on schema prefilling.

---

## 论文详细总结（自动生成）

### 论文总结：TRUST-SQL 框架

#### 1. 核心问题与研究动机
传统的 Text-to-SQL 研究大多基于**“全模式假设”（Full Schema Assumption）**，即假设数据库的所有表结构和元数据都能预先加载到模型的上下文窗口中。然而，在真实的工业场景中，数据库往往包含数百张表和海量的噪声元数据，直接注入完整模式会导致上下文溢出或引入干扰信息。
本文提出了**“未知模式”（Unknown Schema）**场景，要求智能体像人类专家一样，通过主动探索、验证元数据来构建 SQL。核心挑战在于：如何防止模型在长路径交互中产生幻觉，以及如何将最终的执行结果准确归因到早期的模式探索行为（即信用分配问题）。

#### 2. 方法论
TRUST-SQL 将任务建模为**部分可观测马尔可夫决策过程（POMDP）**，核心包含两个部分：
*   **四阶段交互协议（EPGC）**：
    1.  **Explore（探索）**：调用工具查询数据库元数据。
    2.  **Propose（提议）**：强制性的认知检查点，要求智能体提交已验证的模式片段，以此抑制幻觉。
    3.  **Generate（生成）**：基于提议的模式生成候选 SQL。
    4.  **Confirm（确认）**：执行并提交最终答案。
*   **双轨 GRPO 策略（Dual-Track GRPO）**：
    *   基于 DeepSeek 的组相对策略优化（GRPO）进行改进。
    *   **双轨设计**：将轨迹分为“模式轨道”（优化元数据检索准确性）和“全轨道”（优化 SQL 执行正确性）。
    *   **Token 级掩码优势**：通过掩码技术，确保探索奖励仅影响探索阶段的 Token，执行奖励影响后续生成 Token，从而解决了长轨迹中的信用分配难题。

#### 3. 实验设计
*   **数据集**：涵盖了 5 个主流基准测试，包括 **BIRD-Dev**（大规模模式落地）、**Spider-Test**（组合泛化）以及三个鲁棒性变体（**Spider-Syn** 同义词替换、**Spider-DK** 隐性知识、**Spider-Realistic** 歧义消除）。
*   **对比方法**：对比了单轮模型（如 OmniSQL, SQL-R1）和多轮强化学习模型（如 MTIR-SQL, SQL-Trail）。
*   **实验设置**：TRUST-SQL 在完全不预加载模式（Unknown Schema）下运行，而基准模型通常拥有预填充完整模式的特权。

#### 4. 资源与算力
论文详细列出了训练配置：
*   **硬件**：使用了 NVIDIA **A100 GPU**。
*   **4B 模型**：SFT 阶段使用 16 张 A100（约 6.5 小时），RL 阶段使用 8 张 A100（约 60 小时）。
*   **8B 模型**：SFT 阶段使用 16 张 A100（约 12 小时），RL 阶段使用 32 张 A100（异步模式，约 40 小时）。
*   **数据量**：采用高效的数据配方，包含 9.2k SFT 样本和 11.6k RL 样本。

#### 5. 实验数量与充分性
实验设计非常充分且具有客观性：
*   **消融实验**：针对奖励权重 $\lambda$、模式奖励的耦合方式（是否与执行结果挂钩）、SFT 预热的必要性等进行了深入探讨。
*   **测试时缩放（Test-time Scaling）**：分析了训练步数、推理轮数限制以及采样数量（Pass@K）对性能的影响。
*   **多样性验证**：在 Spider 2.0 等更复杂的企业级数据库上进行了额外验证，证明了框架的泛化能力。

#### 6. 主要结论与发现
*   **性能飞跃**：TRUST-SQL 使 4B 和 8B 模型在未知模式下的性能分别提升了 **30.6%** 和 **16.6%**。
*   **超越特权基准**：即使不提供任何预加载模式，TRUST-SQL 的表现依然持平或优于那些拥有完整模式信息的强基准模型。
*   **模式预填的冗余性**：实验发现，对于具备主动探索能力的 TRUST-SQL 而言，预填模式反而可能引入噪声，降低鲁棒性。
*   **信用分配的有效性**：双轨 GRPO 相比标准 GRPO 实现了 9.9% 的相对性能提升，证明了分离探索与执行信号的必要性。

#### 7. 优点（亮点）
*   **结构化约束**：通过 Propose 阶段强制模型“承诺”已知信息，极大地降低了幻觉率（从 26.4% 降至 2.8%）。
*   **算法创新**：双轨 GRPO 为多轮工具集成任务提供了一种优雅的信用分配解决方案。
*   **现实意义**：直接挑战了学术界常用的“全模式假设”，更符合企业级私有化部署的实际需求。

#### 8. 不足与局限
*   **推理开销**：多轮交互必然带来更高的延迟和 Token 消耗（尽管论文证明其比 CHESS 等框架高效得多）。
*   **方言限制**：目前主要针对 **SQLite** 进行优化和评估，尚未广泛测试 PostgreSQL 或 MySQL 等其他 SQL 方言。
*   **固定轮数预算**：训练时使用了固定的最大轮数（如 10 轮），对于极端复杂的数据库模式，可能需要更动态的轮数分配机制。

（完）
