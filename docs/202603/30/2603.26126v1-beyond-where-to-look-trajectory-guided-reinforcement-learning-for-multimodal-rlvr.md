---
title: "Beyond Where to Look: Trajectory-Guided Reinforcement Learning for Multimodal RLVR"
title_zh: 超越“看哪里”：面向多模态 RLVR 的轨迹引导强化学习
authors: "Jinda Lu, Junkang Wu, Jinghan Li, Kexin Huang, Shuo Yang, Mingzhu Chen, Jiancan Wu, Kuien Liu, Xiang Wang"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26126v1"
tags: ["query:rl-tl"]
score: 9.0
evidence: 多模态大语言模型中具有可验证奖励的强化学习 (RLVR)
tldr: 针对多模态大语言模型在强化学习中存在的“能定位视觉区域但难以将其转化为有效推理”的问题，本文提出了轨迹引导强化学习（TGRL）。该方法通过引入强模型的专家推理轨迹，引导策略模型在细粒度推理过程中深度整合视觉证据。结合标记级重加权和轨迹过滤技术，TGRL 在多个多模态推理基准上显著提升了性能，有效弥合了视觉感知与逻辑推理之间的断层。
selection_source: fresh_fetch
motivation: 尽管模型能关注到相关视觉区域，但往往无法将视觉证据有效融入后续推理链，导致推理过程与视觉事实脱节。
method: 提出 TGRL 框架，利用专家推理轨迹引导策略优化，并引入标记级重加权和轨迹过滤机制以确保训练的稳定性。
result: 在多个多模态推理基准测试中，TGRL 显著且持续地提高了模型的推理准确率和逻辑一致性。
conclusion: TGRL 有效解决了多模态推理中的感知与逻辑融合难题，为提升 MLLM 的复杂推理能力提供了新路径。
---

## 摘要
多模态大语言模型（MLLM）中具有可验证奖励的强化学习（RLVR）的最新进展主要集中在提高最终答案的正确性和增强视觉定位。然而，一个关键瓶颈仍然存在：尽管模型能够关注相关的视觉区域，但它们往往无法有效地将视觉证据整合到后续的推理中，导致推理链与视觉事实的关联较弱。为了解决这一问题，我们提出了轨迹引导强化学习（TGRL），该方法利用来自更强模型的专家推理轨迹，引导策略模型将视觉证据整合到细粒度的推理过程中。我们进一步引入了标记级重加权和轨迹过滤，以确保稳定且有效的策略优化。在多个多模态推理基准上的广泛实验表明，TGRL 持续提升了推理性能，并有效地弥合了视觉感知与逻辑推理之间的差距。

## Abstract
Recent advances in Reinforcement Learning with Verifiable Rewards (RLVR) for multimodal large language models (MLLMs) have mainly focused on improving final answer correctness and strengthening visual grounding. However, a critical bottleneck remains: although models can attend to relevant visual regions, they often fail to effectively incorporate visual evidence into subsequent reasoning, leading to reasoning chains that are weakly grounded in visual facts. To address this issue, we propose Trajectory-Guided Reinforcement Learning (TGRL), which guides the policy model to integrate visual evidence into fine-grained reasoning processes using expert reasoning trajectories from stronger models. We further introduce token-level reweighting and trajectory filtering to ensure stable and effective policy optimization. Extensive experiments on multiple multimodal reasoning benchmarks demonstrate that TGRL consistently improves reasoning performance and effectively bridges the gap between visual perception and logical reasoning.

---

## 论文详细总结（自动生成）

以下是对论文《Beyond Where to Look: Trajectory-Guided Reinforcement Learning for Multimodal RLVR》的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：在多模态大语言模型（MLLM）中，具有可验证奖励的强化学习（RLVR）已成为提升推理能力（如思维链 CoT）的关键技术。
*   **核心问题**：现有的多模态 RLVR 方法主要关注“最终答案正确性”或“视觉定位（看哪里）”。然而，模型往往存在**感知与推理的断层**：即使模型关注到了正确的视觉区域，也无法将其有效地转化为逻辑推理证据，导致推理链与视觉事实脱节（例如：看到了8个瓶子，但在计算时却用了7个）。
*   **研究动机**：通过引入专家推理轨迹，显式地监督从“感知”到“推理”的转换过程，而不仅仅是监督结果。

### 2. 方法论
核心思想是提出**轨迹引导强化学习（TGRL）**，将专家模型的推理路径整合进强化学习框架。
*   **轨迹整合（Trajectory Integration）**：在采样阶段，将当前策略模型生成的“在线轨迹”与强专家模型（如 Qwen2.5-VL-32B）生成的“专家轨迹”混合，共同组成 Rollout 组。
*   **标记级重加权（Token-level Reweighting）**：
    *   **重要性采样修正**：针对专家轨迹属于离线分布的问题，引入重要性比率修正。
    *   **自适应权重**：计算学生模型与专家模型在每个 Token 上的概率差异（Discrepancy），通过 Sigmoid 函数动态调整权重，让模型重点学习那些“学生模型低估了专家逻辑”的关键步骤。
*   **轨迹过滤（Trajectory Filtering）**：仅保留最终答案正确的专家轨迹，剔除失败的推理路径，以减少梯度噪声。
*   **优化目标**：将上述机制无缝集成到 GRPO 和 DAPO 等主流 RL 算法中，通过组内奖励归一化实现优势估计。

### 3. 实验设计
*   **训练数据集**：
    *   **Geo3K**：包含 2.1K 几何推理样本。
    *   **ViRL39K**：包含 39K 大规模多模态推理样本。
*   **评估基准（Benchmarks）**：
    *   **推理能力**：MathVista, WeMath, MathVision, MathVerse。
    *   **感知/幻觉**：HallusionBench。
*   **对比方法**：包括原始 Qwen2.5-VL 基座、标准的 GRPO/DAPO、以及 Perception-R1、NoisyRollout、OpenVLThinker 等前沿多模态推理模型。

### 4. 资源与算力
*   **模型配置**：使用 Qwen2.5-VL-3B/7B 作为学生策略模型，Qwen2.5-VL-32B 作为专家模型。
*   **训练细节**：Geo3K 训练 15 个 Epoch，ViRL39K 训练 2 个 Epoch。
*   **算力说明**：论文中**未明确列出**具体的 GPU 型号、数量或总训练时长，但提到了使用 EasyR1 训练框架进行分布式训练。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 2.1K 到 39K 不同量级的训练数据，并在 5 个主流基准上进行了测试。
*   **消融实验**：详细对比了“无过滤”、“无重加权”以及“无专家轨迹”的情况，验证了各组件的必要性。
*   **参数敏感性**：针对重加权系数 $\beta$ 做了多组对比实验（$\beta$ 从 1 到 15）。
*   **评价**：实验设计较为全面，涵盖了感知与推理两个维度，对比基准线清晰，结果具有客观性。

### 6. 主要结论与发现
*   **性能提升**：TGRL 在所有基准上均显著优于原始 RL 算法（如 GRPO/DAPO），在 39K 数据规模下，TGRL-DAPO 达到了 60.68 的平均分，刷新了 SOTA。
*   **弥合断层**：实验证明，轨迹引导能有效减少“感知正确但推理错误”的情况，增强了模型对视觉证据的利用率。
*   **专家模型门槛**：即使使用开源的 32B 模型作为专家，其效果也优于某些依赖闭源最强模型（如 Gemini）进行感知奖励的方法。

### 7. 优点
*   **切中痛点**：识别并解决了多模态推理中“感知-推理转换”这一关键瓶颈。
*   **算法优雅**：通过 Token 级别的重加权机制，巧妙地平衡了“在线探索”与“离线引导”，避免了简单的行为克隆导致的分布偏移。
*   **通用性强**：可以作为插件集成到多种基于组优化的 RL 算法中。

### 8. 不足与局限
*   **依赖专家模型**：性能上限受限于专家轨迹的质量，且需要额外的推理开销来获取专家数据。
*   **任务局限**：目前主要集中在具有“可验证奖励”（如数学、几何）的任务上，对于开放式、主观性的多模态任务（如创意写作、图像描述）适用性待验证。
*   **离线偏差**：尽管有修正机制，但大规模离线轨迹引入时仍可能存在训练不稳定的风险。

（完）
