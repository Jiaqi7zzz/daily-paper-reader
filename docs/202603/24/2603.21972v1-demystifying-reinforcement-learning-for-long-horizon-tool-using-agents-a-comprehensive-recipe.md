---
title: "Demystifying Reinforcement Learning for Long-Horizon Tool-Using Agents: A Comprehensive Recipe"
title_zh: 揭秘长程工具使用智能体的强化学习：一份全面指南
authors: "Xixi Wu, Qianguo Sun, Ruiyang Zhang, Chao Song, Junlong Wu, Yiyan Qi, Hong Cheng"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21972v1"
tags: ["query:rl-tl"]
score: 10.0
evidence: 具有奖励塑造的长程工具使用智能体强化学习
tldr: 本研究针对大语言模型在长程规划和工具使用中的强化学习（RL）应用，系统性地探索了奖励设计、模型规模、数据组成、算法选择和环境稳定性五个维度。通过在TravelPlanner基准上的实验，总结出7条关键经验，如小模型更依赖阶段性奖励，而大模型在简单稠密奖励下收敛更快。最终训练出的模型在TravelPlanner上达到了SOTA水平，显著优于现有领先模型，为构建自主智能体提供了实践指南。
selection_source: fresh_fetch
motivation: 尽管强化学习对提升LLM智能体的长程规划能力至关重要，但在复杂多轮环境下的实际应用方案仍缺乏系统性研究。
method: 在TravelPlanner基准上，从奖励塑造、模型缩放、数据构成、算法选择和环境稳定性五个维度进行了系统的实证研究。
result: 实验得出了7项关键结论，包括约1000个样本是性能平衡点，以及环境稳定性对防止策略退化的重要性。
conclusion: 本研究提供的RL训练方案使模型在TravelPlanner上取得了SOTA性能，为开发长程工具使用智能体提供了全面的技术路径。
---

## 摘要
强化学习 (RL) 对于将大语言模型 (LLM) 演进为具备长程规划能力的自主智能体至关重要，但在复杂的、多轮交互的环境中扩展强化学习的实用方案仍然难以捉摸。本文利用 TravelPlanner 开展了一项系统的实证研究，TravelPlanner 是一个具有挑战性的测试基准，要求通过工具编排来满足多方面的约束条件。我们从五个维度分解了智能体强化学习的设计空间：奖励塑造、模型缩放、数据组成、算法选择和环境稳定性。我们的受控实验得出了 7 个关键结论，例如：(1) 奖励和算法的选择具有规模依赖性，较小的模型受益于阶段性奖励和增强的探索，而较大的模型则能通过更简单的稠密奖励高效收敛；(2) 约 1000 个具有平衡难度混合的训练样本是域内和域外性能的最佳平衡点；(3) 环境稳定性对于防止策略退化至关重要。基于我们提炼的方案，我们训练的强化学习模型在 TravelPlanner 上实现了最先进的性能，显著优于领先的大语言模型。

## Abstract
Reinforcement Learning (RL) is essential for evolving Large Language Models (LLMs) into autonomous agents capable of long-horizon planning, yet a practical recipe for scaling RL in complex, multi-turn environments remains elusive. This paper presents a systematic empirical study using TravelPlanner, a challenging testbed requiring tool orchestration to satisfy multifaceted constraints. We decompose the agentic RL design space along 5 axes: reward shaping, model scaling, data composition, algorithm selection, and environmental stability. Our controlled experiments yield 7 key takeaways, e.g., (1) reward and algorithm choices are scale-dependent as smaller models benefit from staged rewards and enhanced exploration, whereas larger models converge efficiently with simpler dense rewards, (2) ~ 1K training samples with a balanced difficulty mixture mark a sweet spot for both in-domain and out-of-domain performance, and (3) environmental stability is critical to prevent policy degradation. Based on our distilled recipe, our RL-trained models achieve state-of-the-art performance on TravelPlanner, significantly outperforming leading LLMs.

---

## 论文详细总结（自动生成）

这篇论文《Demystifying Reinforcement Learning for Long-Horizon Tool-Using Agents: A Comprehensive Recipe》对长程工具使用智能体的强化学习（RL）进行了系统性的实证研究。以下是详细总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何有效地利用强化学习（RL）来提升大语言模型（LLM）在复杂、多轮交互环境下的长程规划（Long-Horizon Planning）和工具使用能力。
*   **研究动机**：虽然 RL 是提升智能体能力的关键，但现有的研究多集中在短程任务或单步推理上。对于需要数十次工具调用、满足多重约束的长程任务，社区缺乏一套关于奖励设计、数据组成、模型缩放和算法选择的完整“配方”（Recipe）。
*   **背景**：研究采用了 **TravelPlanner** 作为主要实验平台，这是一个模拟真实旅游规划的基准，要求智能体在预算、偏好、常识等复杂约束下编排多种工具。

### 2. 论文提出的方法论
论文提出了 **STAR (Synthesis, Training, And Reinforcement)** 统一后训练框架，包含三个阶段：
*   **数据合成 (Synthesis)**：从沙盒中采样原子元素，验证可行性后，利用强模型（如 GPT-OSS-120B）生成具有难度控制（简单、中等、困难）的自然语言查询。
*   **监督微调 (SFT)**：采用拒绝采样（Rejection Sampling），利用强教师模型（DeepSeek-V3.2-Exp-Thinking）生成高质量轨迹，筛选出成功的样本对基础模型进行微调，解决 RL 的冷启动问题。
*   **强化学习 (RL)**：
    *   **核心算法**：主要使用 **GRPO**（组相对策略优化），并对比了 DAPO（轨迹过滤）和 ARPO（自适应探索）。
    *   **奖励塑造 (Reward Shaping)**：设计了从稠密到稀疏的四种奖励：`SUM`（所有指标聚合）、`MACRO`（宏观约束满足）、`SUCCESS`（纯稀疏二元奖励）和 `CURRICULUM`（从稠密到稀疏的阶段性过渡）。
    *   **技术优化**：取消 KL 惩罚、增加裁剪上限（Clip-high）、严格执行格式检查、处理超长序列。

### 3. 实验设计
*   **数据集/场景**：
    *   **域内 (In-domain)**：TravelPlanner 测试集（1,000 个实例）。
    *   **域外 (OOD)**：7 个知识密集型 QA 基准（NQ, TriviaQA, PopQA, HotpotQA, 2Wiki, Musique, Bamboogle），仅提供 Wikipedia 搜索工具。
*   **Benchmark**：对比了包括 GPT-4o、GPT-5、Gemini-Pro、Kimi-K2.5、Qwen3.5 等顶级商业模型，以及专门针对 TravelPlanner 优化的 Planner-R1。
*   **对比维度**：奖励函数类型、模型规模（1.5B, 3B, 7B）、训练数据量（100 到 2000 个）、数据难度比例、RL 算法变体、环境稳定性（注入随机工具失败）。

### 4. 资源与算力
*   **硬件**：
    *   1.5B 和 3B 模型：使用 1 个节点（8 × NVIDIA A100-80G）。
    *   7B 模型：使用 2 个节点（16 × NVIDIA A100-80G）。
*   **训练时长**：论文提供了具体的 GPU 小时数（见表 4）。例如，3B 模型的 GRPO 训练约需 176 GPU 小时，7B 模型约需 368 GPU 小时。
*   **框架**：基于 `rLLM` 和 `LLaMA-Factory`。

### 5. 实验数量与充分性
*   **实验规模**：合成了超过 10,000 个查询，RL 训练通常使用 1,000 个样本。
*   **充分性**：实验设计非常严谨，采用了**单变量控制法**（每次只改变一个因素）。
*   **客观性**：为了公平对比，所有模型（包括商业模型）都使用了相同的格式化模型（DeepSeek-V3.2-Exp）将自然语言输出转为 JSON 进行自动评估，消除了评估偏差。

### 6. 主要结论与发现
1.  **规模依赖性**：小模型（1.5B）极度依赖阶段性奖励（Curriculum）和重探索算法（ARPO）；大模型（7B）则能直接从简单稠密奖励（SUM）和标准 GRPO 中高效学习。
2.  **数据甜点位**：约 **1,000 个训练样本**是性能平衡点。超过此数量会导致模型过度拟合特定格式，从而损害域外（OOD）泛化能力。
3.  **难度混合**：纯困难数据会导致奖励过稀疏而无法收敛；混合难度的效果最好。
4.  **泛化税**：过于稠密的任务特定奖励（如 SUM）会显著降低模型在通用 QA 任务上的表现，而半稀疏奖励（MACRO）能更好地平衡性能与泛化。
5.  **环境稳定性**：智能体对轻微噪声有韧性，但当工具失败率达到 10% 时，性能会剧烈下降。

### 7. 优点
*   **系统性强**：不仅提出了模型，还系统地拆解了 RL 训练的每一个关键变量，提供了可量化的指导建议。
*   **实用性高**：为开源社区提供了在有限算力下训练高性能智能体的“配方”。
*   **性能卓越**：STAR 训练的 7B 模型在 TravelPlanner 上超越了参数量大得多的商业模型（如 Kimi-K2.5, GPT-4o）。

### 8. 不足与局限
*   **环境局限**：TravelPlanner 虽然复杂，但仍是模拟沙盒，无法完全代表具有不可预测动态的真实世界 API 环境。
*   **泛化评估范围**：OOD 评估仅限于知识密集型 QA，未涵盖代码生成、数学推理等其他智能体领域。
*   **模型规模上限**：实验最高只做到 7B，更大规模模型（如 70B+）的行为是否符合该配方尚待验证。
*   **缺乏全局回溯**：案例分析显示，模型在局部纠错上表现良好，但在需要全局回溯（发现路径不通后更换目标城市）时仍存在困难。

（完）
