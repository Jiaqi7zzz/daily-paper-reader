---
title: Solver-Aided Verification of Policy Compliance in Tool-Augmented LLM Agents
title_zh: 工具增强型大语言模型智能体中策略合规性的求解器辅助验证
authors: "Cailin Winston, Claris Winston, René Just"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20449v1"
tags: ["query:rl-tl"]
score: 9.0
evidence: 使用SMT求解器辅助框架强制执行工具增强大模型代理的工具使用策略合规性
tldr: 针对工具增强型大语言模型（TaLLM）在执行任务时难以严格遵守领域策略的问题，本文提出一种基于SMT求解器的验证框架。该方法通过LLM辅助将自然语言策略转化为形式化逻辑约束，并在运行时利用Z3求解器对工具调用进行前置检查。实验表明，该框架能有效拦截违规操作，在保证任务准确率的同时显著提升了智能体的合规性与可靠性。
selection_source: fresh_fetch
motivation: 现有的TaLLM主要依靠提示词引导策略合规，缺乏强制性的逻辑保障，难以满足敏感应用场景的安全性需求。
method: 采用LLM辅助与人工引导相结合的方式将策略转化为形式化逻辑约束，并在运行时使用Z3求解器对工具调用进行拦截验证。
result: 在TauBench基准测试中，该方法在保持任务准确率的同时显著减少了策略违规行为。
conclusion: 将形式化推理集成到TaLLM执行流程中，是提升智能体工具调用合规性和系统整体可靠性的有效途径。
---

## 摘要
工具增强型大语言模型 (TaLLMs) 通过赋予大语言模型调用外部工具的能力，使其能够与现实世界环境进行交互。然而，在客户服务和业务流程自动化等敏感应用中部署 TaLLMs 的一个主要限制是，在工具使用和智能体行为方面，缺乏对特定领域操作策略的可靠合规性。目前的方法仅通过在 LLM 上下文中包含策略描述来引导 LLM 遵守策略，但这无法保证能够防止违规行为。在本文中，我们引入了一个 SMT 求解器辅助框架，用于在 TaLLM 智能体中强制执行工具使用策略合规性。具体而言，我们采用 LLM 辅助、人工引导的方法，将以自然语言指定的工具使用策略转换为针对智能体可观测状态和工具参数的形式逻辑 (SMT-LIB-2.0) 约束。在运行时，计划中的工具调用会被拦截，并使用 Z3 求解器根据约束条件进行检查，作为工具调用的前提条件。违反策略的工具调用将被阻止。我们在 TauBench 基准测试上进行了评估，结果表明，求解器辅助的策略检查在减少策略违规的同时，保持了整体任务的准确性。这些结果表明，将形式化推理集成到 TaLLM 的执行中可以提高工具调用策略的合规性和整体可靠性。

## Abstract
Tool-augmented Large Language Models (TaLLMs) extend LLMs with the ability to invoke external tools, enabling them to interact with real-world environments. However, a major limitation in deploying TaLLMs in sensitive applications such as customer service and business process automation is a lack of reliable compliance with domain-specific operational policies regarding tool-use and agent behavior. Current approaches merely steer LLMs to adhere to policies by including policy descriptions in the LLM context, but these provide no guarantees that policy violations will be prevented. In this paper, we introduce an SMT solver-aided framework to enforce tool-use policy compliance in TaLLM agents. Specifically, we use an LLM-assisted, human-guided approach to translate natural-language-specified tool-use policies into formal logic (SMT-LIB-2.0) constraints over agent-observable state and tool arguments. At runtime, planned tool calls are intercepted and checked against the constraints using the Z3 solver as a pre-condition to the tool call. Tool invocations that violate the policy are blocked. We evaluated on the TauBench benchmark and demonstrate that solver-aided policy checking reduces policy violations while maintaining overall task accuracy. These results suggest that integrating formal reasoning into TaLLM execution can improve tool-call policy compliance and overall reliability.

---

## 论文详细总结（自动生成）

### 论文总结：工具增强型 LLM 智能体中策略合规性的求解器辅助验证

#### 1. 核心问题与整体含义（研究动机和背景）
随着工具增强型大语言模型（TaLLMs）在客户服务、业务自动化等敏感领域的广泛应用，如何确保智能体严格遵守领域特定的操作策略（如退改签规则、权限控制）成为关键挑战。
*   **现状与痛点**：现有方法主要依靠在提示词（Prompt）中加入策略说明或使用启发式规则过滤，但这仅能“引导”而非“保证”合规。LLM 在面对复杂、长篇幅的策略文档时，容易出现遗忘、逻辑混淆或在多轮对话中失效。
*   **核心目标**：引入形式化验证方法，将自然语言策略转化为逻辑约束，通过 SMT（可满足性模理论）求解器在工具执行前进行强制性合规检查，从而提供确定性的安全保障。

#### 2. 方法论：核心思想与技术流程
该论文提出了一种集成 SMT 求解器的运行时验证框架，核心流程如下：
*   **策略逻辑编码（Policy Encoding）**：利用 LLM 辅助并结合人工引导，将自然语言策略文档转化为 SMT-LIB-2.0 格式的逻辑公式。这包括定义常量（状态、参数）、排序（枚举类型）和谓词（合规条件）。
*   **工具验证模式（Tool Validation Schema）**：为每个工具定义验证架构，明确评估该工具合规性所需的变量（如用户身份、时间戳、会员等级）以及对应的策略谓词。
*   **运行时拦截与验证**：
    1.  **拦截**：在智能体计划调用工具时进行拦截。
    2.  **事实提取**：使用 LLM 从对话历史和工具参数中提取具体数值（Facts）。
    3.  **公式构建**：将提取的事实与静态策略编码结合，构建完整的 SMT 实例。
    4.  **求解器检查**：调用 **Z3 求解器**。若结果为 `SAT`（可满足），则允许执行；若为 `UNSAT`（不可满足），则拦截调用。
*   **反馈循环**：当调用被拦截时，利用求解器生成的“最小不可满足核心”（Unsat Core）作为解释反馈给智能体，引导其重新规划路径。

#### 3. 实验设计
*   **数据集/场景**：使用了 **$\tau$-bench (Tau-bench)** 基准测试中的**航空领域（Airline Domain）**。
*   **场景复杂度**：包含 50 个任务、13 种工具以及一份约 1242 词的复杂策略文档（涉及取消、重订票、补偿资格等规则）。
*   **对比方法**：基准方法（Baseline）是仅通过提示词提供策略说明的标准 TaLLM 智能体。
*   **评估指标**：
    *   **pass^k**：衡量在 $k$ 次独立试验中任务全部成功的概率（评估一致性与准确率）。
    *   **精确率与召回率**：针对工具调用的合规性进行统计。

#### 4. 资源与算力
*   **模型使用**：智能体（Customer & Service Agents）使用 **GPT-4** 实例化；运行时的事实提取器使用 **GPT-4o**。
*   **求解器**：使用 **Z3 Solver**。
*   **算力细节**：文中**未明确说明**具体的硬件 GPU 型号、数量或训练时长。由于该方法主要基于现成的 API 模型和符号求解器，不涉及大规模模型训练，更多关注推理侧的集成。

#### 5. 实验数量与充分性
*   **实验规模**：针对航空领域的 50 个任务，每个任务进行了 $k=4$ 次独立试验。
*   **充分性评价**：
    *   **客观性**：使用了标准化的第三方基准测试 $\tau$-bench，具有较好的客观性。
    *   **局限性**：实验主要集中在单一的航空领域，虽然该领域策略复杂，但尚未在更多样化的领域（如金融、医疗）进行横向对比。
    *   **消融/探索**：论文详细探讨了四种不同的策略翻译设计方案（从纯 LLM 生成到人工微调），证明了目前全自动翻译的局限性。

#### 6. 主要结论与发现
*   **违规显著减少**：求解器辅助框架将无效工具调用的比例从基准测试中的约 50% 降低到了 **29%**。
*   **准确率与一致性提升**：在保持整体任务准确率的同时，该方法显著提升了 `pass^k` 分数。随着试验次数 $k$ 增加，性能下降幅度（26%）远小于基准方法（40%），证明了系统更加稳健。
*   **翻译挑战**：全自动将自然语言策略转化为 SMT 约束仍极具挑战，容易出现语法错误、语义缺失和约束过松（Underconstrained）的问题。

#### 7. 优点（亮点）
*   **硬性保障**：打破了以往仅靠提示词“软引导”的局限，为 LLM 行为提供了基于逻辑的硬性约束。
*   **可解释性**：通过 Unsat Core 提供拦截原因，使智能体的重新规划更具针对性，而非盲目重试。
*   **解耦设计**：验证逻辑与智能体推理逻辑解耦，不影响 LLM 本身的模型架构，具有较好的通用性。

#### 8. 不足与局限
*   **人工依赖**：目前仍需要人工参与策略的逻辑编码和微调，难以实现从文档到形式化约束的全自动端到端转化。
*   **性能权衡**：实验显示，虽然拦截了违规调用（提升精确率），但也导致了正确工具调用的减少（降低了召回率），说明约束可能存在过紧的情况。
*   **延迟风险**：在运行时增加 LLM 事实提取和 SMT 求解步骤，可能会增加端到端的响应延迟（文中未详细量化延迟数据）。
*   **覆盖范围**：仅验证了工具调用参数的合规性，对于智能体纯文本回复中的策略违规（如口头承诺补偿）尚无法完全覆盖。

（完）
