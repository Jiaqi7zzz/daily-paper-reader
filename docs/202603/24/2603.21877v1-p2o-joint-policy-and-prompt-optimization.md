---
title: "P^2O: Joint Policy and Prompt Optimization"
title_zh: P^2O：联合策略与提示优化
authors: "Xinyu Lu, Kaiqi Zhang, Jinglin Yang, Boxi Cao, Yaojie Lu, Hongyu Lin, Min He, Xianpei Han, Le Sun"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21877v1"
tags: ["query:rl-tl"]
score: 9.0
evidence: 在RLVR中协同提示词优化与策略优化
tldr: 针对大语言模型在强化学习（RLVR）中面临的“硬样本”探索效率低、奖励稀疏问题，本文提出了 P^2O 框架。该框架将提示词优化与策略优化相结合，利用 GeneticPareto 算法为硬样本进化出更优提示词，引导模型发现成功路径，并将这些推理增益蒸馏至模型参数中。实验证明，P^2O 在分布内和分布外基准测试中均显著提升了模型性能和收敛速度。
selection_source: fresh_fetch
motivation: 传统的 RLVR 在面对成功率接近零的硬样本时，由于奖励稀疏导致模型缺乏有效的监督信号，限制了推理能力的进一步提升。
method: 提出 P^2O 框架，通过 GeneticPareto 算法动态优化硬样本的提示词，并将优化后的推理路径作为密集监督信号蒸馏到模型策略中。
result: "实验显示 P^2O 不仅在分布内任务表现优异，在分布外（OOD）基准测试中也实现了平均 4.7% 的显著性能提升。"
conclusion: P^2O 通过协同提示词与策略优化，有效解决了强化学习中的探索难题，增强了语言模型的复杂推理能力和泛化性。
---

## 摘要
具有可验证奖励的强化学习（RLVR）已成为增强大语言模型（LLM）推理能力的强大范式。然而，原生 RLVR 面临探索效率低下的问题，特别是在面对成功率接近于零的“困难样本”时。在此类场景中，对稀疏结果奖励的依赖通常会导致零优势估计，尽管这些实例具有很高的信息价值，但实际上却使模型缺乏监督信号。为解决这一问题，我们提出了 P^2O，这是一个协同提示优化与策略优化的新颖框架。P^2O 在训练迭代过程中识别困难样本，并利用 GeneticPareto (GEPA) 提示优化算法来演化提示模板，从而引导模型发现成功的轨迹。关键在于，与依赖输入增强的传统提示工程方法不同，P^2O 将这些优化提示所带来的推理增益直接蒸馏到模型参数中。该机制为困难样本提供了更密集的正向监督信号，并加速了收敛。广泛的实验表明，P^2O 不仅在分布内数据集上取得了卓越的性能，还表现出强大的泛化能力，在分布外基准测试中实现了显著提升（平均提升 4.7%）。

## Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) has emerged as a powerful paradigm for enhancing the reasoning capabilities of Large Language Models (LLMs). However, vanilla RLVR suffers from inefficient exploration, particularly when confronting "hard samples" that yield nearzero success rates. In such scenarios, the reliance on sparse outcome rewards typically results in zero-advantage estimates, effectively starving the model of supervision signals despite the high informational value of these instances. To address this, we propose P^2O, a novel framework that synergizes Prompt Optimization with Policy Optimization. P^2O identifies hard samples during training iterations and leverages the GeneticPareto (GEPA) prompt optimization algorithm to evolve prompt templates that guide the model toward discovering successful trajectories. Crucially, unlike traditional prompt engineering methods that rely on input augmentation, P^2O distills the reasoning gains induced by these optimized prompts directly into the model parameters. This mechanism provides denser positive supervision signals for hard samples and accelerates convergence. Extensive experiments demonstrate that P^2O not only achieves superior performance on in-distribution datasets but also exhibits strong generalization, yielding substantial improvements on out-of-distribution benchmarks (+4.7% avg.).

---

## 论文详细总结（自动生成）

这是一份关于论文《P^2O: Joint Policy and Prompt Optimization》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：在大语言模型（LLM）的推理能力对齐中，带可验证奖励的强化学习（RLVR）已成为主流。然而，原生 RLVR 存在**探索效率低下**的问题。
*   **核心问题**：当训练数据中包含“硬样本”（模型在多次尝试中成功率接近零的题目）时，由于奖励极其稀疏，模型无法获得有效的梯度信号（优势估计为零）。这导致模型在简单样本上过拟合，而在复杂推理任务上停滞不前，陷入局部最优。
*   **研究动机**：作者观察到，即使模型无法直接解决硬样本，通过精心设计的提示词（Prompt）往往能激发其潜在的推理路径。因此，本文旨在通过**联合优化提示词与策略参数**，利用提示词作为“桥梁”跨越奖励稀疏的鸿沟。

### 2. 论文提出的方法论
P^2O 框架采用**交替最大化**的流程，包含两个核心阶段：

*   **阶段 1：带上下文蒸馏的策略优化（Policy Optimization with Context Distillation）**
    *   **硬样本挖掘**：在训练中动态识别成功率低于阈值 $\tau$ 的样本。
    *   **上下文蒸馏**：这是关键技术。对于硬样本，使用优化后的提示词 $z$ 引导模型生成成功轨迹，但在计算策略梯度更新参数时，输入端使用的是**原始查询 $x$** 而非带提示词的输入。
    *   **目的**：强制模型将提示词诱导出的推理能力“内化”到模型参数中，使其在推理阶段无需依赖复杂的提示词也能解决难题。
*   **阶段 2：进化提示优化（Evolutionary Prompt Optimization）**
    *   **核心算法**：使用 **GEPA (Genetic-Pareto)** 算法。
    *   **反射进化**：利用一个“反射 LLM”（如基座模型本身或更强的教师模型）分析失败案例，生成针对性的语义变异提示词。
    *   **Pareto 选择**：维护一个多样化的提示词群体，通过 Pareto 前沿选择在准确性和简洁性等多个维度上表现优异的提示词，确保覆盖不同的错误模式。

### 3. 实验设计
*   **数据集**：从 DeepScaler 和 DeepMath 中各抽取 5,000 个样本（DeepMath 选取难度 $\ge 7$ 的样本）。
*   **Benchmark（基准测试）**：涵盖 6 个极具挑战性的数学竞赛数据集：AIME24、AIME25、AMC、MATH500、MINERVA、OLYMPIAD。
*   **对比方法**：
    *   基座模型：Qwen3-0.6B/1.7B/4B。
    *   强化学习基线：**GRPO**（Group Relative Policy Optimization）。
    *   P^2O 变体：P^2O$_{Self-Ref}$（自反思）和 P^2O$_{Teacher-Ref}$（教师模型反思，使用 Kimi-K2）。

### 4. 资源与算力
*   **训练配置**：模型使用 Qwen3-4B 作为骨干，训练 5 个 epoch，全局 Batch Size 为 128，最大生成长度 12k 序列。
*   **算力说明**：论文**未明确提及具体的 GPU 型号、数量或总训练时长**。但提到在提示词优化阶段，通过设置 Beam Size $W=16$ 来并行化 Pareto 搜索以提高计算效率。

### 5. 实验数量与充分性
*   **实验规模**：在两个不同的 5K 训练集上进行了完整训练，并在 6 个主流基准上验证。
*   **消融实验**：
    1.  **上下文蒸馏的必要性**：对比了直接在增强输入上训练（导致依赖提示词）与蒸馏训练的效果。
    2.  **提示词多样性**：验证了在 Rollout 组内使用多样化提示词与单一提示词的差异。
*   **客观性**：使用了开源的 Qwen 评估套件，并报告了 Pass@1 和 Pass@6 等指标。通过展示硬样本数量随训练轮数下降的曲线，直观证明了方法的有效性。整体实验设计较为充分且逻辑严密。

### 6. 论文的主要结论与发现
*   **性能飞跃**：P^2O 在所有基准上均显著优于 GRPO。在最难的 AIME24/25 任务上，相比 GRPO 实现了 **12.3% 以上的平均提升**。
*   **打破瓶颈**：实验证明提示词优化确实能为硬样本提供“冷启动”信号，使模型能够探索到原本无法触及的高奖励区域。
*   **内化效应**：通过上下文蒸馏，模型在不使用任何额外提示词的情况下，其基座推理能力得到了本质增强。
*   **反思源的影响**：在某些任务中，自反思（Self-Ref）甚至优于教师反思，说明模型有时更易于吸收符合自身推理逻辑的改进建议。

### 7. 优点
*   **创新性地结合了离散优化与连续优化**：将提示词视为隐变量，解决了强化学习中长期存在的稀疏奖励探索难题。
*   **蒸馏机制巧妙**：解决了“提示词依赖”问题，保证了模型在推理时的简洁性和高效性。
*   **动态演化**：提示词随模型能力的提升而进化，始终针对模型当前的“弱点”进行干预。

### 8. 不足与局限
*   **领域局限性**：实验主要集中在数学推理任务上，在代码编写、创意写作或通用对话等非确定性奖励场景下的表现尚未验证。
*   **计算开销**：虽然论文强调了效率，但引入 GEPA 进化过程和多次 Rollout 相比纯 GRPO 仍增加了额外的计算负担。
*   **依赖反射模型**：提示词优化的质量高度依赖于“反射 LLM”的反馈质量，如果反射模型存在偏见或能力不足，可能会引导错误的优化方向。

（完）
