---
title: "CCTU: A Benchmark for Tool Use under Complex Constraints"
title_zh: CCTU：复杂约束下的工具使用基准测试
authors: "Junjie Ye, Guoqiang Zhang, Wenjie Fu, Tao Gui, Qi Zhang, Xuanjing Huang"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15309v1"
tags: ["query:rl-tl"]
score: 8.0
evidence: 评估LLM在约束下使用工具的基准
tldr: "本研究针对大语言模型在复杂约束下使用工具的挑战，提出了CCTU基准测试。该基准涵盖资源、行为、工具集和响应四个维度的12类约束，包含200个高难度测试案例。通过开发可执行的约束验证模块，研究评估了9种主流模型，发现即使是顶尖模型在严格约束下的任务完成率也低于20%，揭示了模型在遵循复杂指令和自我修正方面的显著局限。"
selection_source: fresh_fetch
motivation: 现有研究缺乏针对大语言模型在多重显式约束下进行工具调用能力的专门评估。
method: 提出了包含12类约束的CCTU基准，并开发了支持多轮交互和步骤级验证的可执行约束校验模块。
result: "实验显示主流模型在严格约束下的任务成功率不足20%，且在收到违规反馈后仍表现出有限的自我修正能力。"
conclusion: 研究揭示了当前大模型在复杂约束工具使用中的瓶颈，为开发更鲁棒的智能体提供了数据和评估支持。
---

## 摘要
在显式约束下通过工具使用来解决问题，是大型语言模型（LLM）面临的一个极具挑战性且不可避免的场景，这要求模型具备函数调用、指令遵循和自我修正等能力。然而，由于缺乏专门的评估手段，这一领域的研究进展受到了阻碍。为此，我们推出了 CCTU，这是一个用于评估 LLM 在复杂约束下工具使用能力的基准测试。CCTU 基于包含 12 个约束类别的分类法，涵盖四个维度（即资源、行为、工具集和响应）。该基准测试包含 200 个精心策划且具有挑战性的测试用例，涵盖多种工具使用场景，每个用例平均涉及七种约束类型，平均提示词长度超过 4,700 个 token。为了实现可靠的评估，我们开发了一个可执行的约束验证模块，该模块执行步骤级验证，并在模型与环境的多轮交互过程中强制执行合规性。我们在思考和非思考模式下评估了九种最先进的 LLM。结果表明，当要求严格遵守所有约束时，没有模型的任务完成率超过 20%。进一步分析显示，模型在超过 50% 的案例中违反了约束，特别是在资源和响应维度上。此外，即使在收到关于违反约束的详细反馈后，LLM 的自我修正能力仍然有限，这凸显了开发鲁棒工具使用智能体的一个关键瓶颈。为了促进未来的研究，我们发布了数据和代码。

## Abstract
Solving problems through tool use under explicit constraints constitutes a highly challenging yet unavoidable scenario for large language models (LLMs), requiring capabilities such as function calling, instruction following, and self-refinement. However, progress has been hindered by the absence of dedicated evaluations. To address this, we introduce CCTU, a benchmark for evaluating LLM tool use under complex constraints. CCTU is grounded in a taxonomy of 12 constraint categories spanning four dimensions (i.e., resource, behavior, toolset, and response). The benchmark comprises 200 carefully curated and challenging test cases across diverse tool-use scenarios, each involving an average of seven constraint types and an average prompt length exceeding 4,700 tokens. To enable reliable evaluation, we develop an executable constraint validation module that performs step-level validation and enforces compliance during multi-turn interactions between models and their environments. We evaluate nine state-of-the-art LLMs in both thinking and non-thinking modes. Results indicate that when strict adherence to all constraints is required, no model achieves a task completion rate above 20%. Further analysis reveals that models violate constraints in over 50% of cases, particularly in the resource and response dimensions. Moreover, LLMs demonstrate limited capacity for self-refinement even after receiving detailed feedback on constraint violations, highlighting a critical bottleneck in the development of robust tool-use agents. To facilitate future research, we release the data and code.

---

## 论文详细总结（自动生成）

### 论文总结：CCTU - 复杂约束下的工具使用基准测试

#### 1. 核心问题与研究动机
论文关注的核心问题是 **大语言模型（LLM）在存在显式、复杂约束的场景下使用工具的能力**。
*   **研究背景**：在实际应用中，LLM 驱动的智能体不仅需要正确调用工具，还必须遵循各种现实约束（如调用频率限制、特定响应格式、操作顺序等）。
*   **研究动机**：现有的基准测试通常将“工具调用能力”与“指令遵循能力”分开评估，缺乏一个将两者结合、并在动态多轮交互中进行实时约束验证的系统性框架。

#### 2. 方法论：CCTU 框架设计
CCTU 的核心思想是构建一个包含多维约束的工具使用评估环境，并引入**可执行的约束验证模块**。
*   **约束分类法（Taxonomy）**：提出了涵盖 4 个维度、12 个类别的约束体系：
    *   **资源（Resource）**：交互轮数、总工具调用次数、特定工具调用次数。
    *   **行为（Behavior）**：顺序依赖、并行依赖、并行调用数量。
    *   **工具集（Toolset）**：可用工具及参数范围、必填参数、参数类型。
    *   **响应（Response）**：长度限制、格式要求、内容要求。
*   **构建流程**：
    1.  **数据源**：基于 FTRL 数据集，涵盖单步、并行、多跳等复杂查询结构。
    2.  **约束集成**：利用 LLM（如 Qwen3-32B）根据参考轨迹自动注入约束。
    3.  **验证代码生成**：为每个约束生成对应的 Python 验证代码，实现步骤级（Step-level）的合规性检查。
    4.  **质量控制**：通过两轮独立的人工校验（计算机专业研究生执行），确保数据逻辑和验证代码的准确性。

#### 3. 实验设计
*   **数据集与场景**：包含 200 个测试案例，覆盖 28 个领域（如政治、医疗、计算机科学等）。平均每个案例包含 7 种约束，提示词平均长度超过 4,700 tokens。
*   **评估对象**：选取了 9 种顶尖 LLM，包括 GPT-5.2、GPT-5.1、Claude Opus 4.6、OpenAI o3、DeepSeek-V3.2、Gemini 3 Pro、Kimi 2.5、Qwen3.5-Plus 和 Seed-2.0-Pro。
*   **评估模式**：对比了**思考模式（Thinking Mode）**与**非思考模式**。
*   **评估指标**：
    *   **SR (Solve Rate)**：解决所有子查询且最终满足约束（允许中间违规后修正）。
    *   **PSR (Perfect Solve Rate)**：解决所有查询且全程未违反任何约束。

#### 4. 资源与算力
*   论文**未明确说明**评估过程的具体硬件配置（如 GPU 型号或数量）。
*   文中提到在数据构建流水线中使用了 **Qwen3-32B** 模型来生成轨迹和约束，但未提及该过程的训练时长或具体算力消耗。

#### 5. 实验数量与充分性
*   **实验规模**：对 9 个模型在 2 种模式下进行了测试，每个实验独立运行 3 次以减少随机性影响，并报告了均值和标准差。
*   **充分性评价**：实验设计较为充分。它不仅对比了不同模型，还深入分析了 12 种约束类别的违规分布，并探讨了“思考模式”对性能的双重影响。虽然样本量（200例）不算巨大，但由于每个案例涉及多轮交互和多重约束，评估的深度和复杂度较高。

#### 6. 主要结论与发现
*   **性能瓶颈**：在严格约束下（PSR），所有模型的成功率均**低于 20%**，大多数低于 15%，说明现有模型在复杂约束工具使用方面存在严重缺陷。
*   **违规分布**：模型在**资源维度**（如调用次数限制）和**响应维度**（如特定内容要求）的违规率最高（超过 50%）。
*   **自我修正能力不足**：即使验证模块提供了详细的违规反馈，模型的修正成功率依然有限（最高仅约 65%），OpenAI o3 甚至低至 18.57%。
*   **思考模式的副作用**：虽然思考模式通常能提升性能，但在某些模型（如 Claude 和 Kimi）中会导致“过度思考（Overthinking）”，反而因修改了原本正确的参数而导致失败。

#### 7. 优点与亮点
*   **动态验证机制**：开发了可执行的 Python 验证模块，实现了多轮交互中的实时反馈，比静态文本匹配更客观、更接近真实应用。
*   **高难度设计**：长文本（>4700 tokens）与多重约束（平均 7 种）的结合，极大地拉开了模型间的差距。
*   **人工校验严谨**：采用双人共识机制进行数据和代码校验，保证了 Benchmark 的可靠性。

#### 8. 不足与局限
*   **样本规模有限**：200 个案例虽然质量高，但在覆盖更广泛的极端边缘情况方面可能仍有局限。
*   **数据源单一**：主要基于 FTRL 数据集构建，可能继承了原数据集的分布偏差。
*   **应用限制**：目前的约束主要针对逻辑和格式，尚未涵盖实时性（如毫秒级延迟）或动态变化的外部环境约束。

（完）
