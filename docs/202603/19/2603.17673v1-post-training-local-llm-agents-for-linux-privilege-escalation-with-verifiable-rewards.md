---
title: Post-Training Local LLM Agents for Linux Privilege Escalation with Verifiable Rewards
title_zh: 针对具有可验证奖励的 Linux 权限提升的本地大语言模型智能体后训练
authors: "Philipp Normann, Andreas Happe, Jürgen Cito, Daniel Arp"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17673v1"
tags: ["query:rl-tl"]
score: 10.0
evidence: 针对自动可验证成功的跨步推理任务进行大语言模型智能体后训练
tldr: "本研究针对云端大模型在安全任务中成本高且隐私性差的问题，提出了一种针对本地小模型的两阶段后训练流程。通过在程序生成的Linux提权环境轨迹上进行监督微调（SFT），并结合可验证奖励的强化学习（RL），开发出4B参数的PrivEsc-LLM。该模型在提权任务中达到了95.8%的成功率，性能逼近顶级模型Claude Opus，同时将推理成本降低了100倍以上，为本地化安全智能体开发提供了新路径。"
selection_source: fresh_fetch
motivation: 高性能安全智能体目前过度依赖闭源云端模型，导致资源消耗大、难以复现且不适用于处理敏感数据。
method: 提出一种两阶段流程：首先在程序生成的Linux提权轨迹上进行监督微调，随后利用可验证的奖励信号进行强化学习。
result: "4B参数的PrivEsc-LLM在测试中达到95.8%的提权成功率，性能接近Claude Opus，且单次成功提权的推理成本降低了百倍。"
conclusion: 研究证明通过特定的后训练优化，小型本地模型可以在复杂的网络安全多步推理任务中达到顶尖水平。
---

## 摘要
LLM 智能体与漏洞发现等研究领域的相关性日益增强。然而，最强大的系统仍然是封闭且仅限云端的，这使得它们资源密集、难以复现，且不适用于涉及专有代码或敏感数据的工作。因此，迫切需要能够在严格的资源预算下执行安全任务的小型本地模型，但开发这些模型的方法仍有待探索。在本文中，我们通过提出一种两阶段后训练流水线来填补这一空白。我们专注于 Linux 权限提升问题，该问题的成功是可以自动验证的，且任务需要多步交互式推理。通过使用防止数据泄露的实验设置，我们分两个阶段对一个 4B 模型进行后训练：首先在程序化生成的提权环境轨迹上进行有监督微调，随后进行带有可验证奖励的强化学习。在包含 12 个 Linux 提权场景的预留基准测试中，仅凭有监督微调就在 20 轮交互时将基准成功率提高了一倍以上，而强化学习进一步将我们的模型 PrivEsc-LLM 的成功率提升至 95.8%，接近 Claude Opus 4.6 的 97.5%。同时，每次成功提权的预期推理成本降低了 100 倍以上。

## Abstract
LLM agents are increasingly relevant to research domains such as vulnerability discovery. Yet, the strongest systems remain closed and cloud-only, making them resource-intensive, difficult to reproduce, and unsuitable for work involving proprietary code or sensitive data. Consequently, there is an urgent need for small, local models that can perform security tasks under strict resource budgets, but methods for developing them remain underexplored. In this paper, we address this gap by proposing a two-stage post-training pipeline. We focus on the problem of Linux privilege escalation, where success is automatically verifiable and the task requires multi-step interactive reasoning. Using an experimental setup that prevents data leakage, we post-train a 4B model in two stages: supervised fine-tuning on traces from procedurally generated privilege-escalation environments, followed by reinforcement learning with verifiable rewards. On a held-out benchmark of 12 Linux privilege-escalation scenarios, supervised fine-tuning alone more than doubles the baseline success rate at 20 rounds, and reinforcement learning further lifts our resulting model, PrivEsc-LLM, to 95.8%, nearly matching Claude Opus 4.6 at 97.5%. At the same time, the expected inference cost per successful escalation is reduced by over 100x.

---

## 论文详细总结（自动生成）

以下是对论文《Post-Training Local LLM Agents for Linux Privilege Escalation with Verifiable Rewards》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前最强的安全任务大模型（如漏洞发现、渗透测试）多为闭源云端模型，存在**资源消耗大、难以复现、隐私泄露风险（敏感代码外传）**等问题。
*   **研究动机**：探索是否可以通过后训练（Post-training）技术，使小型本地开源模型在特定的安全任务（如 Linux 权限提升）中达到甚至超越顶级云端模型的性能。
*   **研究背景**：Linux 权限提升（PrivEsc）是一个理想的测试场，因为其成功与否（获取 root 权限）是**二进制且可自动验证的**，同时需要多步交互式推理和工具调用。

### 2. 方法论：核心思想与技术细节
论文提出了一种**两阶段后训练流水线**：
*   **核心思想**：利用程序化生成的环境（Procedural Environments）产生训练数据，通过监督微调（SFT）注入基础知识，再通过带可验证奖励的强化学习（RLVR）优化决策效率。
*   **关键技术细节**：
    *   **程序化生成与防泄露**：开发了 10 类程序化生成器（如 SUID、Sudo 配置错误、Cron 任务漏洞等），通过随机化用户名、密码、路径等防止模型死记硬背。
    *   **第一阶段：SFT（监督微调）**：使用 398B 的开源模型作为“老师”，在解题引导下生成 1000 条成功的提权轨迹，对 Qwen3-4B 进行微调。
    *   **第二阶段：RLVR（可验证奖励强化学习）**：
        *   **算法**：采用异步重要性采样策略优化（AIPO），支持并行环境交互。
        *   **奖励函数**：包含**终端奖励**（成功提权）、**速度奖励**（步数越少奖励越高）、**侦察奖励**（鼓励多样化探索）以及**惩罚项**（惩罚重复操作、格式错误、无效轮次）。
    *   **工具接口**：智能体仅通过 `exec_command`（执行 shell）和 `test_credentials`（验证凭据）两个接口与环境交互。

### 3. 实验设计
*   **数据集/场景**：
    *   **训练集**：基于 10 个生成器家族产生的程序化场景。
    *   **测试集（Benchmark）**：采用前人研究中的 12 个静态 Linux 提权场景（如 Docker 组逃逸、Cron 通配符注入等），确保测试场景不在训练集中。
*   **对比方法**：
    *   **本地基准**：原始 Qwen3-4B、Qwen3-4B SFT 版本。
    *   **云端/顶级模型**：Claude Opus 4.6、DeepSeek V3.2。
    *   **传统工具**：Traitor、pwncat-cs。
*   **评估指标**：$P(\text{root} | R)$，即在 $R$ 轮交互预算内达成 root 的概率。

### 4. 资源与算力
论文明确列出了算力消耗：
*   **SFT 阶段**：使用 **1 张 NVIDIA H100 GPU**，训练时长约 **28 分钟**。
*   **RL 阶段**：使用 **4 张 NVIDIA H100 GPU**，训练时长约 **29 小时**（1000 步，每步 80 个样本）。
*   **推理阶段**：可在消费级显卡（如 **RTX 4090**）上本地运行。

### 5. 实验数量与充分性
*   **实验规模**：每个模型在 12 个场景下各运行 10 次独立实验，总计 120 次运行/模型。
*   **充分性**：
    *   使用了 **95% Wilson 置信区间**进行统计验证，结果具有统计学意义。
    *   **消融实验**：对比了 Base vs SFT vs RL 的性能爬升，证明了 RL 在提升效率（减少轮次）方面的关键作用。
    *   **防污染检查**：严格隔离了程序化训练集和静态测试集，并进行了泄露审计。
*   **客观性**：实验设置了严格的轮次预算（Interaction Budget），这比单纯看最终成功率更符合实际安全操作需求。

### 6. 主要结论与发现
*   **性能飞跃**：4B 参数的 **PrivEsc-LLM** 在 20 轮内的成功率达到 **95.8%**，极度接近 Claude Opus 4.6（97.5%），远超原始基座模型（42.5%）。
*   **成本优势**：相比 Claude Opus，PrivEsc-LLM 的单次成功提权推理成本降低了 **100 倍以上**（约 $0.005 vs $0.62）。
*   **效率提升**：RL 训练显著优化了智能体的“搜索效率”，使其能更快锁定漏洞路径，减少了无效的侦察轮次。
*   **零样本迁移**：模型在未见过的“Docker 组逃逸”场景中表现出良好的泛化能力（9/10 成功率）。

### 7. 优点（亮点）
*   **可验证奖励的应用**：成功将 RLVR 从数学/代码领域引入到交互式网络安全任务中，解决了安全任务难以自动评分的痛点。
*   **本地化与隐私**：证明了小型模型经过特定后训练后，可以替代昂贵且有隐私风险的云端大模型执行敏感任务。
*   **严谨的评估协议**：引入了“轮次预算”概念，并提供了详细的成本模型分析。

### 8. 不足与局限
*   **模型单一性**：实验仅基于 Qwen3-4B 架构，尚未验证该流程在其他模型家族（如 Llama 或 Mistral）上的普适性。
*   **场景覆盖有限**：虽然涵盖了常见漏洞，但尚未涉及更复杂的内核漏洞利用或多主机横向移动。
*   **提示词敏感性**：未对不同模型的 System Prompt 进行针对性优化，可能存在提示词偏差。
*   **算力门槛**：虽然推理成本低，但 RL 阶段仍需要 4 张 H100 运行近 30 小时，对于个人研究者仍有一定门槛。

（完）
