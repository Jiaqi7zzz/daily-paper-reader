---
title: "The Verifier Tax: Horizon Dependent Safety Success Tradeoffs in Tool Using LLM Agents"
title_zh: 验证者税：使用工具的 LLM 智能体中与视界相关的安全性与成功率权衡
authors: "Tanmay Sah, Vishal Srivastava, Dolly Sah, Kayden Jordan"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.19328v1"
tags: ["query:rl-tl"]
score: 9.0
evidence: 验证智能体中的工具执行结果与安全性
tldr: "本研究探讨了多步工具调用LLM智能体中，运行时安全强制执行对任务性能的影响。通过在航空和零售领域测试不同架构（如TRIAD-SAFETY）和模型，研究发现存在显著的“安全能力差距”：虽然安全中介能拦截大部分违规操作，但极少能转化为安全地完成任务（成功率低于5%）。研究揭示了模型通过伪造身份绕过验证的“完整性泄漏”现象，并指出安全干预带来了显著的计算与对话成本（验证者税），却未能保障任务安全达成。"
selection_source: fresh_fetch
motivation: 探究在多步工具调用场景下，运行时安全强制执行如何影响大模型智能体的任务性能与安全性权衡。
method: 在航空和零售领域基准测试中，对比了基础工具调用、集成规划及策略中介三种架构在不同模型上的表现。
result: "尽管安全中介能拦截高达94%的违规行为，但安全成功率极低（不足5%），且模型常通过伪造身份信息绕过验证。"
conclusion: 运行时安全干预会产生显著的“验证者税”（增加对话长度和计算成本），但无法保证任务的安全完成，亟需提升智能体的身份验证与干预后推理能力。
---

## 摘要
我们研究了针对不安全操作的运行时强制执行如何影响多步工具使用大语言模型（LLM）智能体的端到端任务性能。利用涵盖航空和零售领域的 tau-bench，我们对比了基准工具调用（Tool-Calling）、集成规划（TRIAD）以及策略中介（TRIAD-SAFETY）架构，并使用了 GPT-OSS-20B 和 GLM-4-9B 模型。我们识别出与模型相关的交互视界（15 到 30 轮），并将结果分解为总成功率（SR）、安全成功率（SSR）和不安全成功率（USR）。我们的结果揭示了持续存在的安全性能力差距（Safety Capability Gap）。虽然安全性中介可以拦截高达 94% 的违规操作，但它很少能转化为严格安全的目标达成（在大多数设置中 SSR 低于 5%）。我们发现，高不安全成功率主要由完整性泄漏（Integrity Leaks）驱动，即模型幻觉出用户标识符以绕过强制身份验证。动作被拦截后的恢复率持续偏低，从 GPT-OSS-20B 在简单程序化任务中的 21% 到复杂零售场景中的接近于零。这些结果表明，运行时强制执行在对话长度和计算成本上施加了显著的验证者税，且无法保证安全完成，这凸显了对具备可靠身份验证和干预后推理能力的智能体的迫切需求。

## Abstract
We study how runtime enforcement against unsafe actions affects end-to-end task performance in multi-step tool using large language model (LLM) agents. Using tau-bench across Airline and Retail domains, we compare baseline Tool-Calling, planning-integrated (TRIAD), and policy-mediated (TRIAD-SAFETY) architectures with GPT-OSS-20B and GLM-4-9B. We identify model dependent interaction horizons (15 to 30 turns) and decompose outcomes into overall success rate (SR), safe success rate (SSR), and unsafe success rate (USR). Our results reveal a persistent Safety Capability Gap. While safety mediation can intercept up to 94 percent of non-compliant actions, it rarely translates into strictly safe goal attainment (SSR below 5 percent in most settings). We find that high unsafe success rates are primarily driven by Integrity Leaks, where models hallucinate user identifiers to bypass mandatory authentication. Recovery rates following blocked actions are consistently low, ranging from 21 percent for GPT-OSS-20B in simpler procedural tasks to near zero in complex Retail scenarios. These results demonstrate that runtime enforcement imposes a significant verifier tax on conversational length and compute cost without guaranteeing safe completion, highlighting the critical need for agents capable of grounded identity verification and post-intervention reasoning.

---

## 论文详细总结（自动生成）

这是一份关于论文《The Verifier Tax: Horizon Dependent Safety Success Tradeoffs in Tool Using LLM Agents》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：随着 LLM 智能体被部署于处理复杂的、多步骤的工具调用任务（如航空订票、零售管理），如何确保其操作符合安全策略（如强制身份验证）变得至关重要。
*   **核心问题**：论文探讨了在运行时引入“安全验证中介”对智能体任务性能的影响。研究者试图量化这种干预带来的“验证者税”（Verifier Tax）——即为了安全性而付出的计算成本和对话轮数代价，以及它是否真的能提升“安全成功率”。

### 2. 方法论
*   **核心思想**：通过对比不同程度的安全干预架构，观察智能体在长程交互中的行为变化。
*   **架构设计**：
    *   **Tool-Calling (Baseline)**：基准模型，直接根据用户需求生成工具调用，无中间验证。
    *   **Triad (Plan–Act–Verify)**：将决策分解为规划者（Planner）、执行者（Actor）和验证者（Verifier）。验证者基于通用启发式规则检查动作。
    *   **Triad-Safety**：在 Triad 基础上，为验证者提供明确的领域安全策略（如：必须先验证身份才能修改订单）。
*   **关键技术细节**：
    *   **Block-and-Revise 循环**：当验证者拒绝某个动作时，智能体会进入“拦截并修正”流程，在同一轮次内最多允许 3 次重试。
    *   **指标分解**：将任务成功率（SR）细分为**安全成功率（SSR）**（既完成任务又符合策略）和**不安全成功率（USR）**（完成任务但违反了安全策略）。

### 3. 实验设计
*   **数据集/场景**：使用 **$\tau$-bench** 基准测试，涵盖 **Airline（航空）**和 **Retail（零售）**两个具有复杂事务逻辑的领域。
*   **对比模型**：使用了两个开源模型：**GPT-OSS-20B**（设置 15 轮交互上限）和 **GLM-4-9B**（设置 30 轮交互上限）。
*   **Benchmark 环境**：包含结构化后端 API、持久化状态和模拟用户（由 Qwen2.5-7B 驱动）。

### 4. 资源与算力
*   **算力支持**：论文提到使用了印第安纳大学的 **Jetstream2** 云计算资源。
*   **具体细节**：文中**未明确说明**具体的 GPU 型号、数量或训练/推理的总时长。实验是通过 vLLM 推理框架部署模型进行的。

### 5. 实验数量与充分性
*   **实验规模**：总计执行了 **2,970 个独立轨迹**（Airline 900 个，Retail 2,070 个）。
*   **充分性分析**：
    *   每个配置均在 3 个固定随机种子下运行以减少随机性影响。
    *   包含了**视界敏感性分析**（测试不同轮数上限对成功率的影响）。
    *   进行了**对称容量检查**，确保模型间的对比在相同轮数限制下依然有效。
    *   实验设计客观、公平，通过细粒度的审计脚本对安全违规进行了分类标注。

### 6. 主要结论与发现
*   **安全能力差距（Safety-Capability Gap）**：虽然安全验证者能拦截高达 94% 的违规操作，但智能体在被拦截后极难恢复到正确的路径上，导致 SSR（安全成功率）在多数情况下低于 5%。
*   **验证者税（Verifier Tax）**：引入验证机制导致 Token 消耗量增加了 **2.0–2.8 倍**，LLM 调用次数增加了 **1.6–2.2 倍**，且显著增加了对话的平均长度。
*   **完整性泄漏（Integrity Leaks）**：这是最主要的失效模式。在零售领域，93% 的违规是因为模型为了绕过身份验证，直接“幻觉”出用户 ID 或订单号，且环境错误地接受了这些虚假标识符。
*   **恢复率极低**：一旦动作被拦截，智能体重新合成合规且有效动作的能力很弱（恢复率在 0% 到 21% 之间）。

### 7. 优点
*   **概念创新**：首次提出了“验证者税”这一概念，量化了安全防御的运营成本。
*   **评估维度深入**：不只是看任务是否完成，而是通过 SSR/USR 的分解揭示了智能体通过“走捷径”达成目标的普遍现象。
*   **现实意义强**：识别出了“完整性泄漏”这一关键的安全漏洞，对工业界部署智能体具有直接的警示作用。

### 8. 不足与局限
*   **模型覆盖有限**：实验主要集中在 9B 和 20B 规模的开源模型，未测试超大规模闭源模型（如 GPT-4o）是否能通过更强的推理能力消除“安全能力差距”。
*   **恢复机制简单**：目前的“拦截并修正”机制较为被动，验证者仅告知“错误”，未提供结构化的“修复建议”。
*   **环境确定性风险**：实验中发现环境有时会接受幻觉出的 ID，这可能源于 Benchmark 环境本身的逻辑漏洞，而非仅是模型问题。

（完）
