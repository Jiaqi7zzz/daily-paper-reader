---
title: "One Supervisor, Many Modalities: Adaptive Tool Orchestration for Autonomous Queries"
title_zh: 一个主管，多种模态：面向自主查询的自适应工具编排
authors: "Mayank Saini, Arit Kumar Bishwas"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11545v2"
tags: ["query:rl-tl"]
score: 8.0
evidence: 跨多种模态的自主工具编排智能体AI框架
tldr: 本研究提出一种用于自主多模态查询处理的代理 AI 框架，通过中央主管（Supervisor）协调文本、图像、音频和视频等多种工具。该框架摒弃了预设决策树，采用 RouteLLM 和 SLM 辅助的自适应路由策略进行任务分解与调度。实验表明，该方法在保持准确率的同时，显著降低了响应时间、对话返工率和运营成本，为多模态 AI 的高效部署提供了新方案。
selection_source: carryover_cache
motivation: 旨在解决多模态 AI 系统在处理跨模态复杂查询时，因缺乏灵活调度机制而导致的效率低、成本高和交互冗余问题。
method: 开发了一个基于中央主管的框架，利用自适应路由动态分解查询并调用特定模态工具（如 OCR、语音转录等）进行处理。
result: "实验显示，该框架在 15 个任务类别中实现了 72% 的耗时缩减、85% 的返工减少以及 67% 的成本降低。"
conclusion: 智能化的集中编排是优化多模态 AI 部署经济性并提升自主查询处理性能的关键。
---

## 摘要
我们提出了一种用于自主多模态查询处理的智能体 AI 框架，该框架协调了涵盖文本、图像、音频、视频和文档模态的专业工具。一个核心主管（Supervisor）动态分解用户查询，将子任务委派给相应的模态工具（例如目标检测、OCR、语音转录），并通过自适应路由策略而非预定义的决策树来综合结果。对于纯文本查询，该框架通过 RouteLLM 使用学习型路由；而对于非文本路径，则使用 SLM 辅助的模态分解。在涵盖 15 个任务类别的 2,847 个查询上的评估结果显示，与匹配的分层基准相比，我们的框架在保持准确率相当的情况下，获得准确答案的时间缩短了 72%，对话返工减少了 85%，成本降低了 67%。这些结果表明，智能化的集中编排从根本上改善了多模态 AI 部署的经济性。

## Abstract
We present an agentic AI framework for autonomous multimodal query processing that coordinates specialized tools across text, image, audio, video, and document modalities. A central Supervisor dynamically decomposes user queries, delegates subtasks to modality-appropriate tools (e.g., object detection, OCR, speech transcription), and synthesizes results through adaptive routing strategies rather than predetermined decision trees. For text-only queries, the framework uses learned routing via RouteLLM, while non-text paths use SLM-assisted modality decomposition. Evaluated on 2,847 queries across 15 task categories, our framework achieves 72% reduction in time-to-accurate-answer, 85% reduction in conversational rework, and 67% cost reduction compared to the matched hierarchical baseline while maintaining accuracy parity. These results demonstrate that intelligent centralized orchestration fundamentally improves multimodal AI deployment economics.

---

## 论文详细总结（自动生成）

### 论文总结：One Supervisor, Many Modalities: Adaptive Tool Orchestration for Autonomous Queries

#### 1. 核心问题与整体含义（研究动机和背景）
现代 AI 部署面临一个核心矛盾：用户需要系统能够自主处理从简单文本到复杂多模态（图像、音频、视频、文档）的各类查询，同时要求系统具备高成本效益和实时响应能力。
*   **现有方案的缺陷**：
    *   **单体大模型（Monolithic LLMs）**：如 GPT-4 或 Gemini Ultra，虽然能力强，但统一处理所有任务会导致极高的运营成本，且大材小用。
    *   **分层路由系统（Hierarchical Routing）**：依赖预设的决策树，极其脆弱。一旦查询偏离预期模式，系统往往需要完全重启，导致高延迟和糟糕的用户体验。
*   **研究动机**：开发一种能够动态分解任务、自主协调专业工具、并具备局部故障恢复能力的集中式编排框架，以打破“高性能即高成本”和“预设逻辑即脆弱”的僵局。

#### 2. 论文提出的方法论
该论文引入了一个**中央主管（Supervisor）驱动的自适应编排架构**，核心组件包括：
*   **中央主管（Supervisor）**：作为核心大脑，读取工具的类型化接口规范（前置/后置条件、延迟先验），根据查询特征和历史记忆动态构建执行路径。
*   **Couplet Framework（耦合框架）**：这是处理非文本模态的关键。它将领域优化的传统模型（如 YOLO 用于检测、CLIP 用于检索、Tesseract 用于 OCR）与轻量级小语言模型（SLM，如 Phi-3.5）配对。SLM 负责将自然语言指令转化为模型输入，并将原始输出语境化。
*   **自适应路由策略**：
    *   **文本查询**：集成 **RouteLLM**，通过学习型分类器预测查询复杂度，在强模型（GPT-4o）和弱模型（LLaMA-3/Phi-3.5）之间切换。
    *   **非文本查询**：利用 SLM 进行模态分解，直接调用特定的感知工具。
*   **五层记忆架构**：包括短期记忆、全量历史、模态特定记忆、相关查询上下文（基于向量检索）和压缩上下文，确保跨模态信息的持久性和一致性。
*   **状态管理（StateGraph）**：基于 LangGraph 构建，支持并行执行、动态分支和局部修复（即某个工具失败时仅修复该节点，无需重启整个流水线）。

#### 3. 实验设计
*   **数据集与场景**：评估涵盖了 2,847 个查询，分为 15 个任务类别，包括文本推理（MMLU）、文档问答、视觉问答（VQA-v2）、音频处理、视频分析及混合检索任务。
*   **Benchmark（基准）**：
    *   **Monolithic LLM**：全量使用 GPT-4 处理。
    *   **分层路由（Hierarchical Routing）**：基于预设决策树的匹配系统。
    *   **多智能体框架**：如 AutoGen 和 LangGraph 的标准实现。
*   **评估指标**：获得准确答案的时间（TTA）、对话返工率（Rework Rate）、每查询成本、吞吐量（q/s）以及任务准确率。

#### 4. 资源与算力
*   **模型使用**：文中提到了多种模型，包括 Azure OpenAI 的 GPT-4o、LLaMA-3-70B、Mixtral-8x7B、Phi-3.5-mini-instruct，以及感知模型 YOLO、CLIP、Whisper 等。
*   **算力细节**：论文**未明确说明**具体的硬件训练资源（如 GPU 型号、数量或具体训练时长）。实验主要侧重于推理阶段的编排效率，而非从头训练大模型。

#### 5. 实验数量与充分性
*   **实验规模**：2,847 组实验涵盖了广泛的模态组合，样本量足以支撑统计显著性分析（文中给出了 p < 0.001 的显著性测试结果）。
*   **消融实验**：作者针对记忆层、验证机制、并行执行和 Couplet Framework 分别做了消融分析，量化了各组件对 TTA 和返工率的贡献。
*   **公平性**：所有基准系统被赋予了相同的工具集和内存后端，确保了性能差异仅源于编排策略的不同。

#### 6. 论文的主要结论与发现
*   **效率大幅提升**：相比分层基准，该框架实现了 **72% 的 TTA 缩减**和 **85% 的对话返工减少**。
*   **成本显著降低**：通过智能路由和感知任务下放，**成本降低了 67%**。
*   **性能保持**：在大幅优化效率的同时，准确率与单体大模型保持在 ±1% 的统计等价水平。
*   **感知任务优势**：使用专门的感知模型（如 YOLO）处理图像帧仅需 180ms，而端到端多模态 LLM 需要 2.4s，证明了“专业工具+轻量协调”的优越性。

#### 7. 优点
*   **动态性与鲁棒性**：摒弃了死板的决策树，能够处理未见过的查询模式和复杂的跨模态组合。
*   **局部修复能力**：引入了局部故障恢复机制，显著提升了系统在真实环境中的可用性。
*   **经济性**：通过“成本旋钮（Cost Knob）”和多级路由，实现了极佳的性能-成本平衡。
*   **模块化**：工具接口标准化，支持“即插即用”，易于扩展新功能。

#### 8. 不足与局限
*   **编排开销**：Supervisor 本身的推理（尤其是使用 SLM 进行分解时）会引入额外的基础延迟，对于极高频、极简单的任务可能不如硬编码逻辑快。
*   **工具集成成本**：虽然支持即插即用，但目前 Couplet Framework 仍需对新工具进行一定程度的手动适配（如定义前置/后置条件）。
*   **过度升级风险**：自主路由有时会表现出过度保守，将本可以用简单模型解决的任务升级到昂贵模型。
*   **硬件细节缺失**：缺乏具体的算力消耗和环境配置说明，不利于完全复现其性能基准。

（完）
