---
title: "One Supervisor, Many Modalities: Adaptive Tool Orchestration for Autonomous Queries"
title_zh: 一个主管，多种模态：面向自主查询的自适应工具编排
authors: Mayank Saini Arit Kumar Bishwas
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11545v1"
tags: ["query:rl-tl"]
score: 8.0
evidence: 自主多模态查询的自适应工具编排
tldr: 本研究提出一种用于自主多模态查询处理的代理 AI 框架，通过中央主管（Supervisor）协调文本、图像、音频和视频等多种工具。该框架摒弃了预设决策树，采用 RouteLLM 和 SLM 辅助的自适应路由策略进行任务分解与调度。实验表明，该方法在保持准确率的同时，显著降低了响应时间、对话返工率和运营成本，为多模态 AI 的高效部署提供了新方案。
selection_source: carryover_cache
motivation: 旨在解决多模态 AI 系统在处理跨模态复杂查询时，因缺乏灵活调度机制而导致的效率低、成本高和用户交互冗余问题。
method: 开发了一个基于中央主管的框架，利用自适应路由动态分解查询并调用特定模态工具（如 OCR、语音转录等）进行处理。
result: "实验显示该框架在 2,847 个查询中实现了 72% 的响应时间缩短、85% 的返工减少以及 67% 的成本降低。"
conclusion: 智能化的集中编排是优化多模态 AI 部署经济性并提升自主查询处理性能的关键。
---

## 摘要
我们提出了一种用于自主多模态查询处理的智能体 AI 框架，该框架协调了涵盖文本、图像、音频、视频和文档模态的专业工具。一个核心主管（Supervisor）动态分解用户查询，将子任务委派给相应的模态工具（例如目标检测、OCR、语音转录），并通过自适应路由策略而非预定义的决策树来综合结果。对于纯文本查询，该框架通过 RouteLLM 使用学习型路由；而对于非文本路径，则使用 SLM 辅助的模态分解。在涵盖 15 个任务类别的 2,847 个查询上的评估结果显示，与匹配的分层基准相比，我们的框架在保持准确率相当的情况下，获得准确答案的时间缩短了 72%，对话返工减少了 85%，成本降低了 67%。这些结果表明，智能化的集中编排从根本上改善了多模态 AI 部署的经济性。

## Abstract
We present an agentic AI framework for autonomous multimodal query processing that coordinates specialized tools across text, image, audio, video, and document modalities. A central Supervisor dynamically decomposes user queries, delegates subtasks to modality-appropriate tools (e.g., object detection, OCR, speech transcription), and synthesizes results through adaptive routing strategies rather than predetermined decision trees. For text-only queries, the framework uses learned routing via RouteLLM, while non-text paths use SLM-assisted modality decomposition. Evaluated on 2,847 queries across 15 task categories, our framework achieves 72% reduction in time-to-accurate-answer, 85% reduction in conversational rework, and 67% cost reduction compared to the matched hierarchical baseline while maintaining accuracy parity. These results demonstrate that intelligent centralized orchestration fundamentally improves multimodal AI deployment economics.

---

## 论文详细总结（自动生成）

这是一份关于论文《One Supervisor, Many Modalities: Adaptive Tool Orchestration for Autonomous Queries》的结构化深入总结：

### 1. 核心问题与研究背景
现代 AI 部署面临一个核心矛盾：用户需要系统能自主处理从简单文本到复杂多模态（图像、音频、视频、文档）的各类查询，同时要求系统具备高成本效益和实时响应能力。
*   **现有方案的缺陷**：
    *   **单体大模型（Monolithic LLMs）**：如 GPT-4 或 Gemini Ultra，虽然能力强但全量调用成本极高，且大多数简单查询并不需要如此高的推理能力。
    *   **分层路由系统（Hierarchical Routing）**：依赖预设的决策树，极其脆弱。一旦查询偏离预期模式，系统往往会崩溃或需要完全重启，导致高延迟和糟糕的用户体验。
*   **研究动机**：开发一种能够根据查询特征动态分解任务、委派专业工具并自适应路由的中央编排框架，以打破“高性能-高成本”的僵局。

### 2. 方法论
该论文提出了一种基于**中央主管（Supervisor）**的代理 AI 框架，核心思想是将编排逻辑从硬编码的决策树转变为基于上下文的自主推理。

*   **核心组件与技术细节**：
    *   **中央主管（Supervisor）**：读取具有形式化类型签名（前置/后置条件、延迟先验）的工具规范，动态构建执行图。
    *   **两阶段查询分解**：
        1.  **启发式分析**：检测附件（URL 或文件）的 MIME 类型和扩展名。
        2.  **语义分析**：利用轻量级 SLM（如 Phi-3.5-mini）为查询分配执行标签（如 audio, vision, complex 等）。
    *   **自适应路由策略**：
        1.  **文本路径**：集成 **RouteLLM**，通过“胜率预测模型”在强模型（GPT-4o）和弱模型（LLaMA-3, Mixtral 等）之间切换。
        2.  **非文本路径（Couplet Framework）**：将感知任务委派给领域优化模型（如 YOLO 用于检测、Whisper 用于音频），再由 SLM 进行结果语境化，避免昂贵的多模态 LLM 推理。
    *   **五层记忆架构**：包括短期窗口、全历史、模态特定记忆、语义相关记忆和压缩记忆，通过加权评分实现跨模态上下文检索。
    *   **状态管理**：基于 LangGraph 的 StateGraph 模型，支持并行执行、动态分支和局部错误修复（无需全局重启）。

### 3. 实验设计
*   **数据集与场景**：评估涵盖了 2,847 个查询，分为 15 个任务类别（包括 MMLU 文本推理、VQA-v2 视觉问答、PDF 文档分析、视频/音频处理等）。
*   **基准测试（Benchmark）**：
    *   **Monolithic LLM**：全量使用 GPT-4 处理所有查询。
    *   **分层路由（Hierarchical）**：使用预设决策树的匹配基准。
    *   **多智能体框架**：如 AutoGen 和 LangGraph。
*   **核心指标**：准确答案时间（TTA）、返工率（Rework Rate）、每查询成本、吞吐量和准确率。

### 4. 资源与算力
*   **模型使用**：文中提到了多种模型，包括 GPT-4o、Claude-3.5、LLaMA-3-70B、Mixtral-8x7B、Phi-3.5-mini，以及传统模型 YOLO、CLIP、Tesseract。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。由于该框架侧重于推理侧的编排而非从头训练大模型，其算力消耗主要体现在推理 API 调用和轻量级 SLM 的本地部署上。

### 5. 实验数量与充分性
*   **实验规模**：2,847 组实验涵盖了 15 个细分领域，样本量较大。
*   **消融实验**：进行了多项消融研究，分别验证了记忆层（TTA 退化 28%）、验证机制（返工率增加 58%）和并行执行对系统性能的影响。
*   **充分性评价**：实验设计较为充分且客观。通过引入 Wilcoxon 符号秩检验和 McNemar 检验（p < 0.001）来验证统计显著性，确保了结果的公平性。对比了多种主流架构，证明了其在复杂边缘案例下的鲁棒性。

### 6. 主要结论与发现
*   **效率提升**：相比分层基准，获得准确答案的时间（TTA）中位数缩短了 **72%**。
*   **成本优化**：昂贵模型调用减少，整体成本降低了 **67%**。
*   **交互优化**：由于具备局部修复和自主澄清能力，对话返工率降低了 **85%**。
*   **性能保持**：在大幅降低成本和延迟的同时，准确率与单体大模型保持在 ±1% 的统计等价水平。
*   **吞吐量**：系统吞吐量提升了 20%（从 45 提升至 54 q/s）。

### 7. 优点
*   **自适应性强**：摒弃了死板的决策树，能够处理未预见的模态组合和模糊查询。
*   **局部修复机制**：当某个工具失败时，主管能动态选择替代方案，而非让整个流水线崩溃。
*   **经济性极佳**：通过 RouteLLM 和 Couplet Framework 实现了极细粒度的成本控制。
*   **模块化设计**：工具采用类型化接口，支持“插拔式”扩展。

### 8. 不足与局限
*   **编排延迟**：虽然整体 TTA 降低，但对于极高吞吐量的简单场景，LLM 驱动的编排层本身会引入额外的毫秒级延迟。
*   **工具集成开销**：Couplet Framework 目前支持的模型库（YOLO, CLIP 等）仍需一定程度的手动集成，尚未实现完全自动化的新工具发现。
*   **记忆压力**：在极长对话会话中，五层记忆的检索和压缩可能会变得计算密集。
*   **路由保守性**：在某些情况下，路由策略可能会为了保险而过度升级到昂贵模型（Over-conservative escalation）。

（完）
