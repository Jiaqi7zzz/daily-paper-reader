---
title: "Try, Check and Retry: A Divide-and-Conquer Framework for Boosting Long-context Tool-Calling Performance of LLMs"
title_zh: 尝试、检查与重试：一种提升大语言模型长上下文工具调用性能的分而治之框架
authors: "Kunfeng Chen, Qihuang Zhong, Juhua Liu, Bo Du, Dacheng Tao"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11495v1"
tags: ["query:rl-tl"]
score: 9.0
evidence: 用于提升大语言模型工具调用性能的分治框架
tldr: 针对大语言模型在长上下文工具调用中面临的海量噪声干扰问题，本文提出了一种名为 Tool-DC 的分治框架。该框架采用“尝试-检查-重试”范式，通过降低推理难度并利用模型自省能力来提升性能。Tool-DC 包含免训练（TF）和基于训练（TB）两个版本，实验证明其在多个基准测试上显著优于现有方法，甚至使小参数模型达到顶尖闭源模型的水平。
selection_source: fresh_fetch
motivation: 现有大语言模型在处理长上下文中海量且含噪声的候选工具时表现不佳，限制了其实际应用。
method: 提出 Tool-DC 框架，通过“尝试-检查-重试”的策略将复杂任务分解，并提供免训练和基于训练两种灵活变体。
result: "Tool-DC (TF) 在 BFCL 和 ACEBench 上平均提升达 25.10%，而 Tool-DC (TB) 使 Qwen2.5-7B 性能媲美 OpenAI o3 等闭源模型。"
conclusion: 该分治框架有效解决了长上下文工具调用的难题，显著增强了开源模型在复杂工具调用场景下的竞争力。
---

## 摘要
工具调用使大语言模型（LLMs）能够与外部环境进行交互。然而，现有方法在长上下文工具调用任务中往往难以处理海量且含噪声的候选工具，限制了其在现实世界中的应用。为此，我们提出了 Tool-DC，一种用于提升 LLMs 工具调用性能的分而治之框架。Tool-DC 的核心是通过“尝试-检查-重试”范式降低推理难度，并充分利用 LLMs 的自我反思能力。具体而言，Tool-DC 包含两个变体：1）免训练的 Tool-DC (TF)，具有即插即用和灵活的特点；2）基于训练的 Tool-DC (TB)，具有更高的推理效率。大量实验表明，两种 Tool-DC 方法均显著优于同类方法。在 BFCL 和 ACEBench 基准测试中，Tool-DC (TF) 相比基线模型带来了高达 +25.10% 的平均增益，而 Tool-DC (TB) 使 Qwen2.5-7B 能够达到甚至超越闭源 LLMs（如 OpenAI o3 和 Claude-Haiku-4.5）的性能。

## Abstract
Tool-calling empowers Large Language Models (LLMs) to interact with external environments. However, current methods often struggle to handle massive and noisy candidate tools in long-context tool-calling tasks, limiting their real-world application. To this end, we propose Tool-DC, a Divide-and-Conquer framework for boosting tool-calling performance of LLMs. The core of Tool-DC is to reduce the reasoning difficulty and make full use of self-reflection ability of LLMs via a "Try-Check-Retry" paradigm. Specifically, Tool-DC involves two variants: 1) the training-free Tool-DC (TF), which is plug-and-play and flexible; 2) the training-based Tool-DC (TB), which is more inference-efficient. Extensive experiments show that both Tool-DC methods outperform their counterparts by a clear margin. Tool-DC (TF) brings up to +25.10% average gains against the baseline on BFCL and ACEBench benchmarks, while Tool-DC (TB) enables Qwen2.5-7B to achieve comparable or even better performance than proprietary LLMs, e.g., OpenAI o3 and Claude-Haiku-4.5.

---

## 论文详细总结（自动生成）

### 论文总结：Try, Check and Retry (Tool-DC) 框架

#### 1. 核心问题与整体含义
论文关注的是大语言模型（LLMs）在**长上下文工具调用（Tool-Calling）**场景下的性能瓶颈。随着候选工具数量的增加（海量 API）以及工具间语义相似性的干扰（噪声），LLMs 往往难以准确选择工具并填充参数。研究动机在于：现有的检索增强方法过度依赖检索器精度，而基于提示词的方法在处理复杂长上下文时灵活性不足。本文旨在通过一种“分而治之”的策略，降低推理难度并激活模型的自省能力。

#### 2. 方法论：Tool-DC 框架
论文提出了 **Tool-DC** 框架，核心思想是 **“尝试-检查-重试”（Try-Check-Retry）** 范式，并提供两个版本：

*   **Tool-DC (TF) - 免训练版本**：
    1.  **Try（尝试）**：利用“战略锚点分组”策略，将海量工具库拆分为多个子空间。每个小组包含一个检索到的高相关工具（锚点）和若干干扰工具，模型在各小组内并行进行局部推理。
    2.  **Check（检查）**：使用规则引擎进行“模式一致性验证”，从函数名、参数键、数据类型三个维度过滤掉幻觉生成的无效调用。
    3.  **Retry（重试）**：将通过验证的候选工具重新聚合，交给模型进行最终的全局决策和精细化参数填充。
*   **Tool-DC (TB) - 基于训练版本**：
    *   为了解决 TF 版本多次调用带来的推理延迟，该方法将“尝试-检查-重试”的思维轨迹（CoT）提取出来，构建高质量数据集，通过微调（SFT）将这种分治决策能力内化到模型参数中，实现单次前向传播完成高质量调用。

#### 3. 实验设计
*   **数据集/Benchmark**：
    *   **BFCL (Berkeley Function-Calling Leaderboard)**：包含合成与手工构建的测试集。
    *   **ACEBench**：涵盖 8 大领域、68 个子领域的综合工具调用基准。
*   **实验设置**：
    *   **标准设置**：原始基准测试（通常 <10 个工具）。
    *   **扩展设置**：通过注入随机干扰工具，将候选工具扩展至 20-50 个，模拟真实世界的长上下文噪声环境。
*   **对比方法**：
    *   **免训练基线**：GT_Funs（仅给正确工具）、All_Funs（全量工具）、Top-K 检索、HiTEC-ICL（错误清单引导）、ToolGT（结构化模板）。
    *   **训练/闭源模型**：OpenAI o3、Claude-Haiku-4.5、DeepSeek-V3.2、Gemini-3-Pro 以及专门的工具调用模型（如 xLAM, ToolACE）。

#### 4. 资源与算力
*   **训练数据**：使用 `xlam-function-calling-60k` 作为原始语料，构建了 46,897 条带有 CoT 轨迹的训练样本。
*   **硬件资源**：使用了 **2 张 NVIDIA A800 80GB GPU**。
*   **训练细节**：采用 LoRA 微调技术，训练 2 个 Epoch，7B 模型的微调时长约为 **10 小时**。
*   **推理引擎**：使用 vLLM 引擎加速生成。

#### 5. 实验数量与充分性
论文进行了多维度的充分实验：
*   **跨模型验证**：测试了 Qwen2.5 (1.5B/3B/7B)、Llama-3.1/3.2、Gemma-3、Qwen3 等多个开源系列，以及 GPT-4o-mini 等闭源模型。
*   **消融实验**：分别移除了 Try、Check、Retry 阶段，验证了每个环节对最终性能的贡献（结果显示 Retry 阶段对性能至关重要）。
*   **参数敏感性分析**：研究了分组数量 $K$ 对结果的影响。
*   **可扩展性分析**：测试了工具数量从 10 增加到 50 时的性能趋势，证明了框架在极端长上下文下的稳健性。
实验设计客观、公平，通过引入“扩展设置”有效模拟了真实应用中的痛点。

#### 6. 主要结论与发现
*   **性能飞跃**：Tool-DC (TF) 在扩展设置下为小模型（如 Qwen2.5-1.5B）带来了高达 **25.10%** 的平均增益。
*   **超越闭源**：经过 Tool-DC (TB) 微调的 **Qwen2.5-7B** 在 BFCL 上的总分达到 83.16%，**超越了 OpenAI o3、DeepSeek-V3.2 和 Claude-Haiku-4.5**。
*   **长上下文瓶颈**：实验发现，即使是支持 1M 上下文的模型（如 InternLM2.5-7B-1M），在面对海量工具干扰时性能依然会大幅下降，证明了“分治策略”比单纯增加上下文长度更有效。

#### 7. 优点
*   **范式创新**：提出的“尝试-检查-重试”流程符合人类解决复杂问题的逻辑，有效激活了模型的自我修正能力。
*   **灵活性高**：TF 版本无需训练即可部署，TB 版本则兼顾了效率与性能。
*   **抗噪性强**：在工具数量大幅增加时，性能衰减远小于传统方法，具有极强的实用价值。

#### 8. 不足与局限
*   **任务类型限制**：目前实验主要集中在**单步工具调用**，尚未在多步嵌套或复杂 Agent 规划场景中进行验证。
*   **数据多样性**：训练 TB 版本所用的种子数据集（xLAM）虽然规模大，但领域分布可能仍有局限。
*   **推理开销**：TF 版本由于涉及多次并行调用和重试，在没有并行加速的情况下会增加首字延迟（Latency）。

（完）
