---
title: "DIVE: Scaling Diversity in Agentic Task Synthesis for Generalizable Tool Use"
title_zh: DIVE：在智能体任务合成中扩展多样性以实现可泛化的工具使用
authors: "Aili Chen, Chi Zhang, Junteng Liu, Jiangjie Chen, Chengyu Du, Yunji Li, Ming Zhong, Qin Wang, Zhengmao Zhu, Jiayuan Song, Ke Ji, Junxian He, Pengyu Zhao, Yanghua Xiao"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.11076v1"
tags: ["query:rl-tl"]
score: 9.0
evidence: 可执行且可验证的代理任务用于通用工具使用
tldr: 本研究针对大语言模型在工具使用中泛化能力不足的问题，提出了DIVE框架。DIVE采用证据驱动的合成方法，通过先执行真实工具再反向推导任务，确保了任务的可执行性与多样性。该方法在373个工具上实现了结构化多样性扩展，显著提升了模型在分布外（OOD）基准测试中的表现，证明了多样性扩展在工具使用泛化中优于单纯的数据量扩展。
selection_source: fresh_fetch
motivation: 现有的合成任务缺乏多样性，导致大语言模型在面对新任务和新工具集时泛化能力较弱。
method: 提出DIVE框架，通过“先执行工具、后推导任务”的逆向合成流程，结合证据收集与任务衍生循环，在多领域扩展工具覆盖范围和组合多样性。
result: 在Qwen3-8B上的实验显示，DIVE在9个分布外基准测试中平均提升22分，且多样性扩展的效果在数据量仅为四分之一的情况下仍优于单纯的数量扩展。
conclusion: 提升合成任务的多样性是实现智能体工具使用泛化能力的关键，其重要性超过了单纯增加训练数据量。
---

## 摘要
近期的研究通过合成智能体任务来对使用工具的大语言模型（LLMs）进行后训练，但在任务和工具集发生偏移时，实现鲁棒的泛化仍然是一个公开的挑战。我们将这种脆弱性归因于合成任务的多样性不足。扩展多样性具有挑战性，因为训练要求任务保持可执行且可验证，而泛化则要求覆盖多样的工具类型、工具集组合以及异构的工具使用模式。我们提出了 DIVE，这是一种证据驱动的方法，它反转了合成顺序：首先执行多样的真实世界工具，然后根据生成的轨迹严格反向推导任务，从而通过构建过程提供具身性（grounding）。DIVE 沿着两个可控轴（工具池覆盖范围和单任务工具集多样性）扩展结构多样性，并通过“证据收集-任务推导”循环在五个领域的 373 个工具中诱导出丰富的多步工具使用模式。在 DIVE 数据（48k SFT + 3.2k RL）上训练 Qwen3-8B，在 9 个分布外（OOD）基准测试中平均提升了 22 个百分点，并比最强的 8B 基线模型高出 68 个百分点。值得注意的是，受控扩展分析表明，在 OOD 泛化方面，多样性扩展的效果始终优于数量扩展，即使数据量仅为后者的四分之一。

## 速览
**TLDR**：本研究针对大语言模型在工具使用中泛化能力不足的问题，提出了DIVE框架。DIVE采用证据驱动的合成方法，通过先执行真实工具再反向推导任务，确保了任务的可执行性与多样性。该方法在373个工具上实现了结构化多样性扩展，显著提升了模型在分布外（OOD）基准测试中的表现，证明了多样性扩展在工具使用泛化中优于单纯的数据量扩展。 \
**Motivation**：现有的合成任务缺乏多样性，导致大语言模型在面对新任务和新工具集时泛化能力较弱。 \
**Method**：提出DIVE框架，通过“先执行工具、后推导任务”的逆向合成流程，结合证据收集与任务衍生循环，在多领域扩展工具覆盖范围和组合多样性。 \
**Result**：在Qwen3-8B上的实验显示，DIVE在9个分布外基准测试中平均提升22分，且多样性扩展的效果在数据量仅为四分之一的情况下仍优于单纯的数量扩展。 \
**Conclusion**：提升合成任务的多样性是实现智能体工具使用泛化能力的关键，其重要性超过了单纯增加训练数据量。

---

## Abstract
Recent work synthesizes agentic tasks for post-training tool-using LLMs, yet robust generalization under shifts in tasks and toolsets remains an open challenge. We trace this brittleness to insufficient diversity in synthesized tasks. Scaling diversity is difficult because training requires tasks to remain executable and verifiable, while generalization demands coverage of diverse tool types, toolset combinations, and heterogeneous tool-use patterns. We propose DIVE, an evidence-driven recipe that inverts synthesis order, executing diverse, real-world tools first and reverse-deriving tasks strictly entailed by the resulting traces, thereby providing grounding by construction. DIVE scales structural diversity along two controllable axes, tool-pool coverage and per-task toolset variety, and an Evidence Collection--Task Derivation loop further induces rich multi-step tool-use patterns across 373 tools in five domains. Training Qwen3-8B on DIVE data (48k SFT + 3.2k RL) improves by +22 average points across 9 OOD benchmarks and outperforms the strongest 8B baseline by +68. Remarkably, controlled scaling analysis reveals that diversity scaling consistently outperforms quantity scaling for OOD generalization, even with 4x less data.

---

## 论文详细总结（自动生成）

以下是对论文《DIVE: Scaling Diversity in Agentic Task Synthesis for Generalizable Tool Use》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的智能体（Agent）后训练方法高度依赖合成任务，但这些模型在面对未见过的任务和工具集（OOD）时表现出极大的脆弱性。
*   **研究背景**：作者认为这种脆弱性源于合成任务的**多样性不足**。现有的合成方法要么局限于固定工具集（如仅网页搜索），要么在尝试扩展多样性时面临“不可执行”或“不可验证”的风险（即合成的任务模型无法通过给定工具解出，或缺乏客观的评判标准）。
*   **整体含义**：论文旨在探索如何在大规模合成任务的同时，确保任务的**可执行性、可验证性**以及**结构多样性**，从而提升智能体在真实世界复杂工具环境下的泛化能力。

### 2. 方法论：DIVE 框架
DIVE 的核心思想是**“逆向合成” (Inverted Synthesis)**，即先执行工具获取证据，再根据证据推导任务。

*   **核心流程**：
    1.  **资源准备**：构建三个解耦的资源池：
        *   **工具池**：包含 373 个经过验证的真实工具，涵盖金融、生物、医学、学术和通用领域，并分为“检索型”和“处理型”。
        *   **种子池**：约 5000 个领域特定的实体种子，防止主题坍缩。
        *   **示例池**：提供不同任务形式的先验（如多步检索、检索后计算）。
    2.  **证据驱动合成循环**：
        *   **证据收集 (Evidence Collection)**：智能体在随机采样的工具集中，围绕种子实体进行多步真实工具调用，记录执行轨迹和返回结果。
        *   **任务衍生 (Task Derivation)**：根据上述真实的执行轨迹，反向推导出一个严格受证据支持的查询-答案对（Q, A）。
        *   **迭代优化**：通过多次循环，不断增加证据的深度和任务的复杂度。
*   **关键优势**：由于任务是基于已发生的工具执行轨迹生成的，因此**天然保证了可执行性**（有解）和**可验证性**（有确定的参考答案）。

### 3. 实验设计
*   **模型与训练**：
    *   **基座模型**：Qwen3-8B。
    *   **训练阶段**：包含 48k 轨迹的监督微调（SFT）和 3.2k 任务的强化学习（RL，使用 GRPO 算法）。
*   **基准测试 (Benchmark)**：构建了三层评估体系：
    *   **L1 (分布内)**：DIVE-Eval（相同工具池，不同种子）。
    *   **L2 (通用工具 OOD)**：GAIA, HLE, BrowseComp, XBench 等。
    *   **L3 (专业工具 OOD)**：FinanceAgent, MedAgentBench, SWE-bench, Toolathlon（涉及 MCP 协议和状态化环境）。
*   **对比方法**：
    *   **8B 基线**：WebExplorer-8B, EnvScaler-8B 等。
    *   **前沿模型**：Gemini-3-Pro, Claude-4-Sonnet, DeepSeek-V3.2-Exp, GPT-OSS-120B 等。

### 4. 资源与算力
*   **合成算力**：使用 Claude-4-Sonnet 作为证据收集器和任务生成器。
*   **训练细节**：
    *   **SFT**：300 步，Batch Size 64，学习率 1e-5，上下文长度 64k。
    *   **RL**：使用 GRPO 算法，100 步，Batch Size 512，学习率 5e-6，上下文长度 128k。
*   **硬件/时长**：文中提到使用了 SGLang 推理框架，并采用了张量并行（TP=4）和上下文并行（CP=4），但**未明确说明具体的 GPU 型号（如 H100/A100）数量及总训练时长**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 9 个不同的 OOD 基准测试上进行了验证。
    *   进行了 **24 条不同的领域扩展路径**的受控实验，以研究多样性如何影响泛化。
    *   对 48k SFT 轨迹进行了深入的结构化分析（包括工具调用序列、图拓扑等）。
*   **充分性与公平性**：实验设计非常严谨，通过控制变量法对比了“仅增加数据量”与“增加多样性”的区别。使用了 Pass@k 过滤和交叉验证确保合成数据的质量，实验覆盖了从软件工程到医学等多个异构领域，具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **多样性胜过数量**：在 OOD 泛化方面，**扩展工具多样性的效果远好于单纯增加训练数据量**。即使数据量少 4 倍，高多样性的模型依然表现更优。
*   **RL 的放大效应**：强化学习能够进一步放大多样性带来的收益，RL 后的模型在复杂工具组合上的表现显著提升。
*   **结构多样性是关键**：DIVE 合成的数据在工具调用拓扑（如检索-处理混合模式）上比传统方法丰富得多，这是其泛化的核心原因。
*   **小模型逆袭**：经过 DIVE 训练的 8B 模型在多个专业基准测试中达到了与千亿级参数模型（如 GPT-OSS-120B）相当甚至更好的水平。

### 7. 优点
*   **方法论创新**：通过“证据驱动”解决了合成数据中长期存在的“不可解”和“幻觉答案”问题。
*   **工具规模大**：涵盖 373 个真实 API，远超同类研究。
*   **分析深入**：引入了 R/P（检索/处理）拓扑分类法，从数学和结构层面量化了智能体任务的多样性。

### 8. 不足与局限
*   **真实工具成本**：依赖真实 API 调用进行合成，可能面临 API 访问受限、成本高昂或环境不稳定的问题。
*   **教师模型依赖**：合成过程依赖于强大的教师模型（如 Claude-4），如果教师模型本身在某些领域有偏见，可能会传递给合成数据。
*   **模型规模限制**：主要实验集中在 8B 模型上，尚未验证该方法在超大规模模型（如 70B+）上的扩展效应。
*   **实时性风险**：真实工具的数据（如金融股价）具有时效性，合成的 QA 对可能随时间失效，需要持续维护工具池。

（完）
