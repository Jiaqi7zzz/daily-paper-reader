---
title: "SWE-Fuse: Empowering Software Agents via Issue-free Trajectory Learning and Entropy-aware RLVR Training"
title_zh: SWE-Fuse：通过无问题描述轨迹学习与熵感知 RLVR 训练赋能软件智能体
authors: "Xin-Cheng Wen, Binbin Chen, Haoxuan Lan, Hang Yu, Peng Di, Cuiyun Gao"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.07927v1"
tags: ["query:rl-tl"]
score: 9.0
evidence: 用于软件代理修复问题的熵感知RLVR训练
tldr: 针对软件工程智能体在处理真实问题时面临的问题描述质量低、存在噪声等挑战，本文提出 SWE-Fuse 框架。该框架通过融合“无问题描述”轨迹学习和熵感知强化学习（RLVR），减少了误导性描述的影响并增强了模型的调试能力。实验表明，SWE-Fuse 在 SWE-bench Verified 基准上显著提升了 8B 和 32B 模型的解决率，结合推理时扩展技术效果更佳。
selection_source: fresh_fetch
motivation: 现有的软件工程智能体受限于低质量或误导性的问题描述，导致模型难以准确对齐问题与解决方案。
method: 提出 SWE-Fuse 框架，包含无问题描述驱动的轨迹学习模块以减少噪声干扰，以及熵感知 RLVR 训练模块以动态平衡探索与稳定性。
result: "在 SWE-bench Verified 上，SWE-Fuse 使 8B 和 32B 模型的解决率分别提升了 43.0% 和 60.2%，且在测试时扩展下表现更优。"
conclusion: SWE-Fuse 通过优化训练轨迹和强化学习策略，有效提升了轻量级模型解决复杂真实软件问题的能力。
---

## 摘要
大语言模型（LLMs）改变了软件工程的格局。最近，许多基于 LLM 的智能体被开发出来，用于解决现实世界的软件问题修复任务。尽管取得了最先进的性能，这些智能体仍面临一个重大挑战：高质量问题描述不足。现实世界的数据集通常在问题描述与其相应解决方案之间表现出不一致，从而引入了噪声和歧义，误导自动化智能体并限制其解决问题的有效性。我们提出了 SWE-Fuse，这是一个感知问题描述的训练框架，它融合了问题描述引导和无问题描述的样本来训练 SWE 智能体。它由两个关键模块组成：(1) 无问题描述驱动的轨迹学习模块，用于减轻潜在的误导性问题描述，同时使模型能够学习逐步调试过程；(2) 熵感知 RLVR 训练模块，通过熵驱动的裁剪自适应地调整训练动态。它在高熵下应用宽松的裁剪以鼓励探索，在低熵下应用更严格的裁剪以确保训练稳定性。我们在广泛研究的 SWE-bench Verified 基准测试上评估了 SWE-Fuse，展示了其在解决现实世界软件问题方面的有效性。具体而言，SWE-Fuse 在解决率上分别比最佳的 8B 和 32B 基准模型高出 43.0% 和 60.2%。此外，将 SWE-Fuse 与测试时扩展（TTS）相结合可以进一步提升性能，在 TTS@8 下，8B 和 32B 模型的解决率分别达到 49.8% 和 65.2%。

## Abstract
Large language models (LLMs) have transformed the software engineering landscape. Recently, numerous LLM-based agents have been developed to address real-world software issue fixing tasks. Despite their state-of-the-art performance, Despite achieving state-of-the-art performance, these agents face a significant challenge: \textbf{Insufficient high-quality issue descriptions.} Real-world datasets often exhibit misalignments between issue descriptions and their corresponding solutions, introducing noise and ambiguity that mislead automated agents and limit their problem-solving effectiveness. We propose \textbf{\textit{SWE-Fuse}}, an issue-description-aware training framework that fuses issue-description-guided and issue-free samples for training SWE agents. It consists of two key modules: (1) An issue-free-driven trajectory learning module for mitigating potentially misleading issue descriptions while enabling the model to learn step-by-step debugging processes; and (2) An entropy-aware RLVR training module, which adaptively adjusts training dynamics through entropy-driven clipping. It applies relaxed clipping under high entropy to encourage exploration, and stricter clipping under low entropy to ensure training stability. We evaluate SWE-Fuse on the widely studied SWE-bench Verified benchmark shows to demonstrate its effectiveness in solving real-world software problems. Specifically, SWE-Fuse outperforms the best 8B and 32B baselines by 43.0\% and 60.2\% in solve rate, respectively. Furthermore, integrating SWE-Fuse with test-time scaling (TTS) enables further performance improvements, achieving solve rates of 49.8\% and 65.2\% under TTS@8 for the 8B and 32B models, respectively.

---

## 论文详细总结（自动生成）

这是一份关于论文《SWE-Fuse: Empowering Software Agents via Issue-free Trajectory Learning and Entropy-aware RLVR Training》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的核心问题是**软件工程（SWE）智能体在处理真实世界 GitHub 问题时，高度依赖高质量的问题描述（Issue Description）**。
*   **研究动机**：作者发现现实世界的数据集（如 SWE-bench）中，问题描述与最终解决方案（Patch）之间经常存在“错配”或噪声。例如，描述可能在谈论警告处理，而实际修复的是图像编码逻辑。这种不一致会误导智能体，限制其解决问题的上限。
*   **整体含义**：提出了一种名为 **SWE-Fuse** 的训练框架，旨在通过融合“有描述”和“无描述”的样本，并结合改进的强化学习算法，提升轻量级模型（8B/32B）在复杂软件修复任务中的自主调试能力和鲁棒性。

### 2. 方法论
SWE-Fuse 包含两个核心模块：

#### (1) 无问题描述驱动的轨迹学习模块 (IFTL)
*   **多步轨迹构建**：利用强大的教师模型（如 Gemini 3）在沙盒环境中生成 ReAct 模式（思考-行动-观察）的解决轨迹。
*   **数据过滤**：严格剔除“Git 破解”行为（即禁止智能体通过 `git log` 查看标准答案），并过滤掉步骤过短或格式错误的轨迹。
*   **无问题描述 SFT**：核心创新点。在训练集中加入一定比例的**无描述样本**（仅提供测试用例和环境），强制模型通过逐步调试（Debugging）而非依赖文字提示来定位并修复错误。

#### (2) 熵感知 RLVR 训练模块 (ERLVR)
*   **RLVR 框架**：基于可验证奖励（测试是否通过）的强化学习，无需显式的奖励模型。
*   **RLOO 优势估计**：采用 Reward Leave-One-Out 算法降低方差。
*   **熵感知自适应裁剪（Entropy-Adaptive Clipping）**：
    *   **核心逻辑**：根据策略的熵（不确定性）动态调整 PPO 的裁剪半径 $\epsilon$。
    *   **机制**：当模型不确定性高（高熵）且表现优于基准时，放大裁剪半径以鼓励探索；当不确定性高但表现差时，缩小半径以防止错误更新。这平衡了训练的探索性与稳定性。

### 3. 实验设计
*   **数据集**：主要使用 **SWE-bench Verified**（包含 500 个真实 GitHub 问题）。
*   **消融实验子集**：为了平衡成本，选取了 200 个具有代表性的任务子集进行深入分析。
*   **对比方法**：
    *   **开源模型**：Qwen3 系列、SWE-Gym、DeepSWE、Skywork-SWE 等。
    *   **闭源模型**：GPT-4o、Claude 3.5/4.5 Sonnet、OpenAI-o3、Gemini 3 Pro。
*   **评估指标**：问题解决率（Resolve Rate, %）。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号（如 A100 或 H100）及具体训练时长。
*   **环境细节**：提到了使用 Docker 容器构建沙盒，并开发了沙盒管理器用于高并发执行。训练涉及 8B 和 32B 参数规模的模型。

### 5. 实验数量与充分性
*   **实验规模**：构建了包含 **1.4 万条**验证正确的轨迹数据集。
*   **实验维度**：
    *   **基准对比**：在全量榜单上与数十种 SOTA 模型对比。
    *   **数据缩放实验**：测试了从 1k 到 14k 数据量对性能的影响。
    *   **配比实验**：测试了无描述样本比例（0% 到 100%）的影响。
    *   **强化学习曲线**：展示了 8B 和 32B 模型在 RL 阶段的收敛情况。
*   **充分性评价**：实验设计较为全面，涵盖了从数据质量、数据量到算法机制的多个维度，对比对象包括了最新的闭源模型，具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **性能领先**：SWE-Fuse-32B 在开源模型中排名第一，解决率达到 **60.2%**，超过了许多参数量更大的模型（如 70B 或 400B+）。
*   **测试时扩展（TTS）**：结合 TTS@8 技术，32B 模型的解决率可进一步提升至 **65.2%**。
*   **无描述样本的价值**：加入 25%-50% 的无描述样本能显著提升模型在真实噪声环境下的调试能力，但比例过高（>75%）会导致性能下降。
*   **RL 稳定性**：熵感知裁剪机制有效解决了 SWE 任务中长序列、多轮交互导致的训练不稳定问题。

### 7. 优点
*   **直击痛点**：敏锐捕捉到真实数据中 Issue 描述质量参差不齐的问题，并提出了有效的“脱敏”训练方案。
*   **算法创新**：将策略熵引入 RL 裁剪机制，为复杂动作空间的强化学习提供了新思路。
*   **高效轻量**：证明了通过高质量轨迹学习和精细化 RL，较小规模的模型（8B/32B）也能达到甚至超过巨型模型的表现。

### 8. 不足与局限
*   **教师模型依赖**：初始轨迹生成高度依赖闭源的 Gemini 3，这可能限制了完全开源闭环的构建。
*   **语言局限性**：实验主要集中在 Python 语言（受限于 SWE-bench），在多语言环境下的泛化能力有待验证。
*   **测试用例假设**：无描述训练假设测试用例是完备且正确的，但在实际开发初期，编写正确的测试用例本身也是一大挑战。

（完）
