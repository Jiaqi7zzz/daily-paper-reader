---
title: "ExecVerify: White-Box RL with Verifiable Stepwise Rewards for Code Execution Reasoning"
title_zh: ExecVerify：具有可验证分步奖励的白盒强化学习，用于代码执行推理
authors: "Lingxiao Tang, He Ye, Zhaoyang Chu, Muyang Ye, Zhongxin Liu, Xiaoxue Ren, Lingfeng Bao"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.11226v1"
tags: ["query:rl-tl"]
score: 10.0
evidence: 来自执行轨迹的可验证白盒奖励
tldr: 针对代码大模型在执行推理方面的不足，本文提出 ExecVerify 框架。该方法通过约束程序合成构建多难度数据集，并利用强化学习引入可验证的白盒奖励（如变量值和下一语句预测），确保模型在每一步执行推理上的语义正确性。实验表明，该方法显著提升了 7B 模型的推理能力，使其在代码推理基准上媲美 32B 模型，并有效增强了代码生成性能。
selection_source: fresh_fetch
motivation: 现有的代码推理微调方法依赖于不可验证的教师解释，且缺乏对训练任务难度的显式控制。
method: 采用基于约束的程序合成生成多难度数据，并通过强化学习引入针对中间执行步骤的可验证白盒奖励。
result: "经过 ExecVerify 训练的 7B 模型在代码推理上达到了 32B 模型的水平，并在代码生成任务中将 pass@1 提升了高达 5.9%。"
conclusion: 通过强化学习结合可验证的中间步骤奖励，可以显著提升小型代码模型在复杂执行推理和下游生成任务中的表现。
---

## 摘要
代码大语言模型（Code LLMs）在代码执行推理方面仍面临挑战，尤其是在较小的模型中。现有方法依赖于使用教师生成解释的监督微调（SFT），主要有两种形式：（1）输入-输出（I/O）预测链，以及（2）执行轨迹的自然语言描述。然而，在 SFT 过程中，中间执行步骤无法被显式验证，因此训练目标可能会退化为仅仅匹配教师的解释。此外，训练数据的收集通常缺乏对任务难度的显式控制。我们提出了 ExecVerify，它通过引入源自执行轨迹的可验证白盒奖励（包括下一条语句预测以及变量值/类型预测），超越了单纯的文本模仿。我们的工作首先通过基于约束的程序合成构建了一个具有多个难度级别的数据集。然后，我们应用强化学习（RL）来奖励关于中间执行步骤和最终输出的正确回答，使训练目标与每个执行步骤的语义正确性保持一致。最后，我们采用了一个两阶段训练流水线，首先增强执行推理能力，然后迁移到代码生成任务。实验表明，使用 ExecVerify 训练的 7B 模型在代码推理基准测试中达到了与 32B 模型相当的性能，并且在代码生成任务上，相比强大的后训练基线，其 pass@1 提高了多达 5.9%。

## Abstract
Code LLMs still struggle with code execution reasoning, especially in smaller models. Existing methods rely on supervised fine-tuning (SFT) with teacher-generated explanations, primarily in two forms: (1) input-output (I/O) prediction chains and (2) natural-language descriptions of execution traces. However, intermediate execution steps cannot be explicitly verified during SFT, so the training objective can reduce to merely matching teacher explanations. Moreover, training data is typically collected without explicit control over task difficulty. We introduce ExecVerify, which goes beyond text imitation by incorporating verifiable white-box rewards derived from execution traces, including next-statement prediction and variable value/type prediction. Our work first builds a dataset with multiple difficulty levels via constraint-based program synthesis. Then, we apply reinforcement learning (RL) to reward correct answers about both intermediate execution steps and final outputs, aligning the training objective with semantic correctness at each execution step. Finally, we adopt a two-stage training pipeline that first enhances execution reasoning and then transfers to code generation. Experiments demonstrate that a 7B model trained with ExecVerify achieves performance comparable to 32B models on code reasoning benchmarks and improves pass@1 by up to 5.9\% on code generation tasks over strong post-training baselines.

---

## 论文详细总结（自动生成）

以下是对论文《ExecVerify: White-Box RL with Verifiable Stepwise Rewards for Code Execution Reasoning》的结构化总结：

### 1. 核心问题与整体含义
论文针对代码大语言模型（Code LLMs），尤其是参数量较小的模型，在**代码执行推理**（即模拟程序运行过程）能力上的不足展开研究。
*   **研究动机**：现有的监督微调（SFT）方法依赖教师模型生成的解释（如 I/O 预测链或执行轨迹描述），但这些解释在训练中仅作为文本匹配，缺乏对中间执行步骤的**显式验证**。
*   **数据缺陷**：现有训练数据往往缺乏难度控制，导致模型要么在简单任务上过拟合，要么在无法理解的极难任务上“死记硬背”。

### 2. 方法论
论文提出了 **ExecVerify** 框架，核心思想是将代码执行过程转化为可验证的“白盒”奖励信号。
*   **基于约束的程序合成**：
    *   系统性遍历 Python 内置类型和方法。
    *   引入结构化约束（如嵌套循环、条件分支深度），构建从易到难的课程学习式数据集。
    *   通过模型预测成功率进行难度过滤，保留“非平凡但可解”的样本。
*   **两阶段后训练流水线**：
    *   **第一阶段：白盒强化学习（White-Box RL）**。使用 GRPO 算法，奖励函数不仅包含最终 I/O 的正确性，还包含对执行轨迹中**中间状态**的预测准确率（包括：下一条执行语句预测、变量值及类型预测）。
    *   **第二阶段：代码生成强化学习**。将第一阶段获得的推理能力迁移到生成任务，利用单元测试（Unit-Test）通过率作为奖励信号进行对齐。

### 3. 实验设计
*   **数据集与基准测试**：
    *   **代码推理**：CRUXEval、LiveCodeBench-Exec、REval（评估控制流、变量状态等）。
    *   **代码生成**：EvalPlus (HumanEval/MBPP)、LiveCodeBench-V6、BigCodeBench。
    *   **跨语言推理**：CRUXEval-X（涵盖 Java, C++, Go 等 6 种语言）。
*   **对比方法**：包括 Qwen2.5-Coder (7B/14B/32B)、Llama3 (70B)、DeepSeek-Coder-V2-Lite 以及专门针对推理优化的 SemCoder 和 CODEI/O。

### 4. 资源与算力
*   **硬件环境**：使用了由 **8 台 NVIDIA H100 GPU** 组成的集群。
*   **训练细节**：
    *   SFT 阶段使用 LLaMAFactory 框架。
    *   RL 阶段使用 VeRL 框架，采用 GRPO 算法，每条提示词采样 8 个响应，训练 500 步。
    *   最大序列长度为 4096 字节。

### 5. 实验数量与充分性
论文进行了多维度的实验，具有较高的充分性和客观性：
*   **消融实验**：分别验证了数据合成中的约束生成、输入合成、难度过滤的有效性；并对比了仅奖励控制流与仅奖励数据流的效果。
*   **泛化性测试**：在不同规模（3B, 7B, 8B）和架构（Qwen, Llama）的模型上验证了方法的通用性。
*   **效率对比**：在固定数据规模（15K 样本）下，对比了 ExecVerify 与其他主流数据集的训练效率。
*   **真实场景测试**：构建了涉及外部库（Library-involved）的预测基准，验证模型在复杂环境下的表现。

### 6. 主要结论与发现
*   **以小博大**：经过 ExecVerify 训练的 **7B 模型**在代码推理任务上的表现优于许多 32B 甚至 70B 的模型，平均得分从 60.8 提升至 80.8。
*   **推理助益生成**：执行推理能力的提升能显著增强代码生成性能，在多个生成基准上 pass@1 提升了 2.5% 到 5.9%。
*   **跨语言迁移**：虽然训练数据主要是 Python，但模型在 Java、C++ 等其他语言的执行推理能力也得到了同步提升。

### 7. 优点
*   **可验证性**：将不可验证的自然语言解释转变为可由解释器自动验证的白盒问题，解决了 SFT 中的“幻觉”和文本模仿问题。
*   **数据质量控制**：通过主动合成和难度过滤，构建了高质量的课程化数据集，而非被动收集。
*   **两阶段策略**：清晰地展示了“先学逻辑推理，再学代码编写”的有效路径。

### 8. 不足与局限
*   **语言局限**：合成数据目前仅限于 Python，对其他编程语言的覆盖依赖于模型的跨语言泛化能力。
*   **任务粒度**：实验主要集中在函数级或片段级代码，尚未扩展到多文件、项目级的复杂软件工程场景。
*   **计算开销**：相比传统的 SFT，白盒 RL 涉及频繁的解释器调用和多轮采样，计算和工程实现成本较高。

（完）
