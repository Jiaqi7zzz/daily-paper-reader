---
title: "Try, Check and Retry: A Divide-and-Conquer Framework for Boosting Long-context Tool-Calling Performance of LLMs"
title_zh: 尝试、检查与重试：一种提升大语言模型长上下文工具调用性能的分而治之框架
authors: "Kunfeng Chen, Qihuang Zhong, Juhua Liu, Bo Du, Dacheng Tao"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11495v1"
tags: ["query:rl-tl"]
score: 9.0
evidence: 用于提升工具调用性能的分治框架
tldr: 针对大语言模型在长上下文工具调用中面临的海量噪声干扰问题，本文提出了一种名为 Tool-DC 的分治框架。该框架采用“尝试-检查-重试”范式，通过降低推理难度并利用模型自省能力来提升性能。Tool-DC 包含免训练（TF）和基于训练（TB）两个版本，实验证明其在多个基准测试上显著优于现有方法，甚至使小参数模型达到顶尖闭源模型的水平。
selection_source: fresh_fetch
motivation: 现有大语言模型在处理长上下文中海量且含噪声的候选工具时表现不佳，限制了其实际应用。
method: 提出 Tool-DC 框架，通过“尝试-检查-重试”的策略将复杂任务分解，并提供免训练和基于训练两种灵活变体。
result: "Tool-DC (TF) 在 BFCL 和 ACEBench 上平均提升达 25.10%，而 Tool-DC (TB) 使 Qwen2.5-7B 性能媲美 OpenAI o3 等闭源模型。"
conclusion: 该分治框架有效解决了长上下文工具调用的难题，显著增强了开源模型在复杂工具调用场景下的竞争力。
---

## 摘要
工具调用使大语言模型（LLMs）能够与外部环境进行交互。然而，现有方法在长上下文工具调用任务中往往难以处理海量且含噪声的候选工具，限制了其在现实世界中的应用。为此，我们提出了 Tool-DC，一种用于提升 LLMs 工具调用性能的分而治之框架。Tool-DC 的核心是通过“尝试-检查-重试”范式降低推理难度，并充分利用 LLMs 的自我反思能力。具体而言，Tool-DC 包含两个变体：1）免训练的 Tool-DC (TF)，具有即插即用和灵活的特点；2）基于训练的 Tool-DC (TB)，具有更高的推理效率。大量实验表明，两种 Tool-DC 方法均显著优于同类方法。在 BFCL 和 ACEBench 基准测试中，Tool-DC (TF) 相比基线模型带来了高达 +25.10% 的平均增益，而 Tool-DC (TB) 使 Qwen2.5-7B 能够达到甚至超越闭源 LLMs（如 OpenAI o3 和 Claude-Haiku-4.5）的性能。

## Abstract
Tool-calling empowers Large Language Models (LLMs) to interact with external environments. However, current methods often struggle to handle massive and noisy candidate tools in long-context tool-calling tasks, limiting their real-world application. To this end, we propose Tool-DC, a Divide-and-Conquer framework for boosting tool-calling performance of LLMs. The core of Tool-DC is to reduce the reasoning difficulty and make full use of self-reflection ability of LLMs via a "Try-Check-Retry" paradigm. Specifically, Tool-DC involves two variants: 1) the training-free Tool-DC (TF), which is plug-and-play and flexible; 2) the training-based Tool-DC (TB), which is more inference-efficient. Extensive experiments show that both Tool-DC methods outperform their counterparts by a clear margin. Tool-DC (TF) brings up to +25.10% average gains against the baseline on BFCL and ACEBench benchmarks, while Tool-DC (TB) enables Qwen2.5-7B to achieve comparable or even better performance than proprietary LLMs, e.g., OpenAI o3 and Claude-Haiku-4.5.

---

## 论文详细总结（自动生成）

以下是对论文《Try, Check and Retry: A Divide-and-Conquer Framework for Boosting Long-context Tool-Calling Performance of LLMs》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管大语言模型（LLMs）在工具调用方面取得了进展，但在面对**长上下文**（候选工具数量庞大）和**高噪声**（存在大量语义相似但参数不同的干扰工具）的现实场景时，性能会显著下降。
*   **核心挑战**：
    1.  模型难以在长上下文中精准定位目标工具。
    2.  相似工具的干扰会导致参数填充错误。
    3.  现有基于检索（Retriever）的方法高度依赖检索器的准确率，一旦检索失败，模型将无法获取正确工具。
*   **研究目标**：提出一种能够降低推理难度、充分利用模型自省能力，并能有效处理海量候选工具的通用框架。

### 2. 方法论：Tool-DC 框架
论文提出了 **Tool-DC**（Divide-and-Conquer，分而治之）框架，核心是 **“尝试-检查-重试”（Try-Check-Retry）** 范式。

#### 核心流程：
1.  **Try（尝试 - 分组与局部推理）**：
    *   **策略性锚点分组（Strategic Anchor Grouping）**：利用检索器获取 Top-K 工具作为“锚点”，将剩余工具（Tail Tools）均匀分配到各个组中，形成多个低噪声的子空间。
    *   **局部推理**：模型在每个子空间内并行进行工具调用预测，降低了单次推理的搜索空间复杂度。
2.  **Check（检查 - 模式一致性验证）**：
    *   使用基于规则的验证器对局部推理结果进行严格筛选。
    *   验证维度包括：函数名是否存在、参数键（Keys）是否匹配、数据类型（Data Type）是否一致。
3.  **Retry（重试 - 全局聚合与决策）**：
    *   将所有通过验证的候选工具汇总，重新构建一个精简的工具列表提供给模型。
    *   模型利用其自我反思能力，在过滤后的候选集中做出最终的全局决策。

#### 两个变体：
*   **Tool-DC (TF)**：免训练版本，即插即用，通过多次调用实现上述流程。
*   **Tool-DC (TB)**：基于训练的版本。将 TF 产生的正确推理轨迹转化为思维链（CoT）数据，通过监督微调（SFT）将分治能力内化到模型参数中，实现单次前向传播即可完成复杂决策。

### 3. 实验设计
*   **数据集与基准（Benchmark）**：
    *   **BFCL (Berkeley Function-Calling Leaderboard)**：包含合成（Non-Live）和手工（Live）子集。
    *   **ACEBench**：涵盖 8 大领域、68 个子领域的综合性工具使用评估集。
*   **实验设置**：
    *   **标准设置**：原始基准测试的工具规模（通常 < 10 个）。
    *   **扩展设置（Extended Setting）**：人为注入干扰工具，将候选工具规模扩大至 20-50 个，模拟真实噪声环境。
*   **对比方法**：
    *   **免训练基线**：All_Funs（全量输入）、Top-K（仅检索）、HiTEC-ICL（基于错误清单的提示词）、ToolGT（引导式模板）。
    *   **训练基线**：Vanilla SFT（标准微调）、各类工具专用模型（如 xLAM, ToolACE, Hammer）。
    *   **闭源模型**：OpenAI o3, Claude-Haiku-4.5, DeepSeek-V3.2, GPT-4o-mini 等。

### 4. 资源与算力
*   **硬件环境**：使用了 **2 张 NVIDIA A800 80GB GPU**。
*   **训练细节**：
    *   使用 LLaMA-Factory 框架。
    *   采用 LoRA 微调技术。
    *   训练时长：对于 7B 参数模型，在上述硬件上微调约需 **10 小时**。
    *   训练数据：基于 xlam-60k 构造了约 4.7 万条高质量 CoT 轨迹数据。

### 5. 实验数量与充分性
*   **实验规模**：论文在 Qwen2.5 (1.5B/3B/7B)、Llama-3.1/3.2、Gemma-3、Qwen3 等多个模型系列上进行了验证。
*   **充分性分析**：
    *   **多维度验证**：涵盖了不同参数规模的模型和不同架构。
    *   **消融实验**：分别验证了 Try、Check、Retry 三个阶段的必要性，结果显示缺失任何一环都会导致性能大幅下降。
    *   **参数敏感性**：分析了分组数量 $K$ 对结果的影响。
    *   **可扩展性分析**：测试了工具数量从 10 增加到 50 时的性能趋势。
*   **客观性**：实验对比了当前最顶尖的闭源模型，并使用了标准化的 AST 准确率作为评价指标，实验设计较为客观公平。

### 6. 主要结论与发现
*   **性能飞跃**：Tool-DC (TF) 在扩展设置下为小模型带来了显著提升（如 Qwen2.5-1.5B 提升了 **25.10%**）。
*   **超越闭源**：经过 Tool-DC (TB) 微调的 **Qwen2.5-7B** 在 BFCL 上的总分达到 83.16%，**超越了 OpenAI o3 和 Claude-Haiku-4.5**。
*   **长上下文局限性**：实验发现，即使是支持 1M 上下文的模型（如 InternLM2.5-7B-1M），在处理海量工具调用时依然表现糟糕，证明了单纯增加上下文长度无法解决工具调用的逻辑复杂性问题。
*   **分治法的有效性**：通过将全局搜索转化为局部验证再到全局决策，有效缓解了模型在长上下文中的“迷失”现象。

### 7. 优点与亮点
*   **范式创新**：提出的“尝试-检查-重试”范式非常符合人类处理复杂任务的逻辑，且易于实现。
*   **灵活性与效率兼顾**：TF 版本无需训练即可提升现有模型能力，TB 版本则通过知识内化解决了推理延迟问题。
*   **显著提升开源模型上限**：证明了通过高质量的推理轨迹微调，中等规模的开源模型可以在特定任务上达到甚至超过顶级闭源模型的水平。

### 8. 不足与局限
*   **任务类型限制**：目前主要针对**单步工具调用**进行了优化，尚未在多步嵌套（Multi-step nested）或复杂智能体规划场景中进行充分验证。
*   **推理开销**：Tool-DC (TF) 版本由于需要多次调用模型，会带来额外的推理延迟和 API 成本。
*   **数据依赖**：Tool-DC (TB) 的效果高度依赖于初始种子数据集（如 xlam-60k）的质量，且目前构造的 CoT 数据在工具多样性上仍有提升空间。

（完）
