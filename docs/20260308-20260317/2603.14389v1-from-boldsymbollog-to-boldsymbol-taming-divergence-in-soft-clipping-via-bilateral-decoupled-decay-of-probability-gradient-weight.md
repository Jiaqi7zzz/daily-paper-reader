---
title: "From $\\boldsymbol{\\logπ}$ to $\\boldsymbolπ$: Taming Divergence in Soft Clipping via Bilateral Decoupled Decay of Probability Gradient Weight"
title_zh: "从 $\\boldsymbol{\\logπ}$ 到 $\\boldsymbolπ$：通过概率梯度权重的双向解耦衰减抑制软截断中的发散"
authors: "Xiaoliang Fu, Jiaye Lin, Yangyi Fang, Chaowen Hu, Cong Qin, Zekai Shao, Binbin Zheng, Lu Pan, Ke Zeng"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14389v1"
tags: ["query:rl-tl"]
score: 10.0
evidence: 具有可验证奖励的强化学习（RLVR）的优化动力学
tldr: 针对大语言模型强化学习（RLVR）中硬截断导致探索受阻及软截断梯度发散的问题，本文提出DGPO算法。该方法将优化原语从对数概率梯度转向概率梯度，并引入双边解耦衰减机制。通过对边界Token应用非对称连续衰减，DGPO在保持训练稳定的同时增强了探索能力，在DeepSeek-R1系列模型上的数学任务中表现优于GRPO等基准。
selection_source: fresh_fetch
motivation: 现有的硬截断算法限制了模型探索，而基于对数概率梯度的软截断方法在概率趋近于零时会导致梯度权重发散，使训练不稳定。
method: 提出DGPO算法，将概率梯度作为优化核心，并利用基于重要性采样比率的双边解耦衰减机制来平滑处理边界Token。
result: 在DeepSeek-R1-Distill-Qwen系列模型（1.5B至14B）上的实验表明，DGPO在多个数学基准测试中均显著优于强基准模型。
conclusion: DGPO通过重新思考梯度优化原语，为RLVR提供了一种鲁棒且可扩展的解决方案，有效平衡了训练稳定性和持续探索。
---

## 摘要
具有可验证奖励的强化学习 (RLVR) 催生了大型语言模型 (LLM) 推理能力的飞跃，但其优化动力学仍然脆弱。像 GRPO 这样的标准算法通过“硬截断”来强制维持稳定性，但这会因丢弃置信区域外 token 的梯度而无意中抑制了探索。虽然最近的“软截断”方法试图恢复这些梯度，但它们面临一个关键挑战：依赖对数概率梯度 ($\nabla_θ\log π_θ$) 会在概率趋于零时产生发散权重，从而导致 LLM 训练不稳定。我们重新审视了这一惯例，将概率梯度 ($\nabla_θπ_θ$) 确立为更优的优化原语。据此，我们提出了解耦梯度策略优化 (DGPO)，它采用了一种基于重要性采样比率的解耦衰减机制。通过对边界 token 应用非对称的连续衰减，DGPO 解决了稳定性与持续探索之间的冲突。在 DeepSeek-R1-Distill-Qwen 系列模型 (1.5B/7B/14B) 上的广泛实验表明，DGPO 在各种数学基准测试中始终优于强基准模型，为 RLVR 提供了一种稳健且可扩展的解决方案。我们的代码和实现可在以下网址获取：https://github.com/VenomRose-Juri/DGPO-RL。

## 速览
**TLDR**：针对大语言模型强化学习（RLVR）中硬截断抑制探索和软截断因对数概率梯度导致训练不稳定的问题，本文提出DGPO算法。该方法将优化原语从对数概率梯度转向概率梯度，并引入双边解耦衰减机制。通过对边界Token应用非对称连续衰减，DGPO在保持训练稳定性的同时增强了探索能力，在DeepSeek-R1系列模型上的数学任务中表现优异。 \
**Motivation**：传统的硬截断算法会丢弃信任区域外的梯度从而抑制探索，而现有的软截断方法因对数概率梯度的发散特性导致训练不稳定。 \
**Method**：提出DGPO算法，将概率梯度作为优化原语，并基于重要性采样比率设计了一种双边解耦的梯度权重衰减机制。 \
**Result**：在DeepSeek-R1-Distill-Qwen系列模型上的实验表明，DGPO在多个数学基准测试中均显著优于GRPO等强基准模型。 \
**Conclusion**：DGPO通过解决梯度发散与探索受限的矛盾，为大语言模型在强化学习任务中提供了一种稳健且可扩展的优化方案。

---

## Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) has catalyzed a leap in Large Language Model (LLM) reasoning, yet its optimization dynamics remain fragile. Standard algorithms like GRPO enforce stability via ``hard clipping'', which inadvertently stifles exploration by discarding gradients of tokens outside the trust region. While recent ``soft clipping'' methods attempt to recover these gradients, they suffer from a critical challenge: relying on log-probability gradient ($\nabla_θ\log π_θ$) yields divergent weights as probabilities vanish, destabilizing LLM training. We rethink this convention by establishing probability gradient ($\nabla_θπ_θ$) as the superior optimization primitive. Accordingly, we propose Decoupled Gradient Policy Optimization (DGPO), which employs a decoupled decay mechanism based on importance sampling ratios. By applying asymmetric, continuous decay to boundary tokens, DGPO resolves the conflict between stability and sustained exploration. Extensive experiments across DeepSeek-R1-Distill-Qwen series models (1.5B/7B/14B) demonstrate that DGPO consistently outperforms strong baselines on various mathematical benchmarks, offering a robust and scalable solution for RLVR. Our code and implementation are available at: https://github.com/VenomRose-Juri/DGPO-RL.

---

## 论文详细总结（自动生成）

这是一份关于论文《From $\log\pi$ to $\pi$: Taming Divergence in Soft Clipping via Bilateral Decoupled Decay of Probability Gradient Weight》的结构化深入总结：

### 1. 核心问题与研究动机
*   **背景**：在具有可验证奖励的强化学习（RLVR）中，大语言模型（LLM）通过正确性反馈进行优化。标准算法如 GRPO 采用“硬截断”（Hard Clipping）机制来维持训练稳定，但这会丢弃置信区域外的 token 梯度，导致探索受阻、熵值快速下降和过早收敛。
*   **核心问题**：现有的“软截断”方法（如 CISPO、GPPO）试图保留这些梯度以增强探索，但它们大多基于**对数概率梯度** ($\nabla_\theta \log \pi_\theta$)。作者发现，当 token 概率趋近于零时，这种方法的梯度权重会呈发散状增长（趋于无穷），导致训练极度不稳定甚至崩溃。
*   **研究动机**：寻找一种既能保留边界梯度以促进探索，又能防止权重发散以维持训练稳定的优化机制。

### 2. 方法论：DGPO 算法
*   **核心思想**：将优化原语从“对数概率梯度”转向**“概率梯度”** ($\nabla_\theta \pi_\theta$)。作者证明了在 RL 目标下，概率空间比对数空间具有更好的几何对称性和有界性。
*   **关键技术细节**：
    *   **双边解耦衰减（Bilateral Decoupled Decay）**：针对重要性采样比率（IS ratio）的不同边界，设计了不同的衰减函数。
    *   **左边界（低比率、负优势）**：采用多项式衰减（Polynomial Decay）。当概率降低时，梯度权重主动收敛而非发散，从而保护模型免受极端负反馈的冲击。
    *   **右边界（高比率、正优势）**：采用倒数根号衰减（Reciprocal Radical Decay）。这允许模型在超出置信区间时仍能以受控的速度继续强化优秀的探索路径。
    *   **连续性保证**：通过引入连续性常数（$C_{left}, C_{right}$），确保梯度权重在截断边界处平滑过渡，避免优化过程中的突变。
*   **公式逻辑**：DGPO 重新定义了权重函数 $W_{DGPO}$，使其在不同区域（LN, HP 等）表现出不同的衰减特性，从而在数学上保证了梯度的连续性和权重的收敛性。

### 3. 实验设计
*   **模型架构**：采用 DeepSeek-R1-Distill-Qwen 系列模型，涵盖 1.5B、7B 和 14B 三种参数规模。
*   **数据集**：
    *   **训练集**：DAPO-Math-17K。
    *   **测试基准（Benchmarks）**：AIME24、AIME25（最新、无污染）、AMC23、MATH500、Minerva、OlympiadBench。
*   **对比方法**：
    *   **标准基准**：GRPO（硬截断）。
    *   **软截断变体**：CISPO、GPPO、CE-GPPO。
    *   **采样改进**：ASPO（非对称重要性采样）。

### 4. 资源与算力
*   **硬件环境**：使用了 30 个计算节点，每个节点配备 **8 张 NVIDIA A100 (80GB) GPU**（共 240 张 A100）。
*   **训练时长**：14B 模型的总有效训练时间约为 **500 小时**。
*   **软件框架**：基于 VeRL 框架，使用 FSDP 进行全参数更新，推理端使用 vLLM 的 PagedAttention 机制。

### 5. 实验数量与充分性
*   **实验规模**：
    *   跨越了从 1.5B 到 14B 的模型缩放实验，验证了算法的可扩展性。
    *   在 6 个主流数学推理基准上进行了详尽测试。
    *   进行了超参数消融实验（针对衰减指数 $n$ 和 $m$）。
    *   提供了训练动力学分析（Avg@32、Pass@32、策略熵变化曲线）。
*   **充分性评价**：实验设计非常充分且客观。作者不仅对比了最终分数，还通过可视化手段分析了梯度权重的分布情况，解释了为什么软截断会导致崩溃以及 DGPO 如何解决该问题。

### 6. 主要结论与发现
*   **性能提升**：DGPO 在所有规模的模型上均显著优于 GRPO。在 1.5B 模型上，平均 Avg@32 提升了 **4.3%**；在 7B 模型上提升了 **3.1%**。
*   **稳定性**：DGPO 有效解决了软截断方法在左边界（低概率区域）的梯度发散问题，避免了训练中后期的崩溃现象。
*   **探索平衡**：通过调节右边界的衰减速度，DGPO 能够维持更高的策略熵，证明其在防止模型过早陷入局部最优（熵塌缩）方面具有显著优势。

### 7. 优点
*   **理论深刻**：从 $\log \pi$ 到 $\pi$ 的视角切换具有启发性，揭示了 RL 优化不稳定的根本数学原因。
*   **鲁棒性强**：在不同规模的模型和不同的数学任务中表现一致，且对超参数有一定的容忍度。
*   **解决痛点**：直接针对 RLVR 中“探索与稳定”的经典矛盾提出了优雅的数学解决方案。

### 8. 不足与局限
*   **领域局限性**：实验主要集中在数学推理（可验证奖励）领域。在缺乏明确判定规则的任务（如创意写作、开放式对话）中，该方法的有效性尚待验证。
*   **模型规模上限**：受限于算力，实验最高只做到了 14B 参数。虽然表现出良好的 Scaling 趋势，但在 70B 或更大规模的基座模型上的表现仍需进一步确认。
*   **超参数敏感度**：虽然提供了调优指南，但 $n$ 和 $m$ 的最优值在不同规模模型上有所不同（如 1.5B 适合 2,2，而 7B 适合 1,2），这增加了一定的调优成本。

（完）
