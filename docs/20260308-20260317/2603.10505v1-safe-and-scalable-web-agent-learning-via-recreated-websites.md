---
title: Safe and Scalable Web Agent Learning via Recreated Websites
title_zh: 通过重建网站实现安全且可扩展的 Web 智能体学习
authors: "Hyungjoo Chae, Jungsoo Park, Alan Ritter"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10505v1"
tags: ["query:rl-tl"]
score: 10.0
evidence: Web 智能体的确定性、程序化可验证奖励
tldr: 针对真实网站在训练Web智能体时存在的不安全、难重置及缺乏验证反馈等问题，本文提出VeriEnv框架。该框架利用大模型将真实网站克隆为可执行且可验证的合成环境，并通过Python SDK提供内部访问权限，使智能体能自主生成任务并获得确定性奖励。实验证明，该方法能有效提升智能体的泛化能力、特定站点精通度及扩展性，实现了安全且可扩展的自我进化学习。
selection_source: fresh_fetch
motivation: 真实网站环境存在安全风险、重置困难且难以提供程序化验证反馈，严重限制了Web智能体的规模化训练与评估。
method: 提出VeriEnv框架，利用LLM克隆真实网站并构建带Python SDK的合成环境，支持任务自生成与确定性奖励验证。
result: 实验表明，使用VeriEnv训练的智能体在未知网站上具有泛化性，并能通过增加训练环境数量实现性能的持续扩展。
conclusion: VeriEnv通过解耦真实环境交互与智能体学习，为构建安全、可扩展且具备自我进化能力的Web智能体提供了新范式。
---

## 摘要
训练自主 Web 智能体从根本上受到其学习环境的限制：现实世界的网站探索起来不安全，难以重置，且很少提供可验证的反馈。我们提出了 VeriEnv，这是一个将语言模型视为环境创建者的框架，能够自动将现实世界的网站克隆为完全可执行、可验证的合成环境。通过 Python SDK 提供受控的内部访问，VeriEnv 使智能体能够自主生成具有确定性、程序化可验证奖励的任务，从而消除了对启发式或基于 LLM 的评判器的依赖。这种设计将智能体学习与不安全的现实世界交互解耦，同时通过环境扩展实现可扩展的自我进化。通过在 Web 智能体基准测试上的实验，我们证明了使用 VeriEnv 训练的智能体能够泛化到未见过的网站，通过自我进化训练实现特定站点的精通，并从训练环境数量的扩展中获益。代码和资源将在论文接收后发布于 https://github.com/kyle8581/VeriEnv。

## Abstract
Training autonomous web agents is fundamentally limited by the environments they learn from: real-world websites are unsafe to explore, hard to reset, and rarely provide verifiable feedback. We propose VeriEnv, a framework that treats language models as environment creators, automatically cloning real-world websites into fully executable, verifiable synthetic environments. By exposing controlled internal access via a Python SDK, VeriEnv enables agents to self-generate tasks with deterministic, programmatically verifiable rewards, eliminating reliance on heuristic or LLM-based judges. This design decouples agent learning from unsafe real-world interaction while enabling scalable self-evolution through environment expansion. Through experiments on web agent benchmarks, we show that agents trained with VeriEnv generalize to unseen websites, achieve site-specific mastery through self-evolving training, and benefit from scaling the number of training environments. Code and resources will be released at https://github.com/kyle8581/VeriEnv upon acceptance.

---

## 论文详细总结（自动生成）

### 论文详细总结：Safe and Scalable Web Agent Learning via Recreated Websites

#### 1. 核心问题与整体含义（研究动机和背景）
训练能够在现实世界中执行任务的自主 Web 智能体（Web Agents）面临三大挑战：
*   **安全性与限制**：直接在真实网站上探索可能干扰其他用户、违反平台政策，或被验证码（CAPTCHA）拦截。
*   **环境重置困难**：真实网站的状态难以回滚，导致重复实验成本极高。
*   **反馈不可靠**：现有方法通常依赖 LLM-as-a-Judge（大模型评判）来评估任务是否完成，这种方式存在幻觉且不具备确定性。
**VeriEnv** 框架的核心动机是：通过将 LLM 作为“环境创建者”，将真实网站克隆为完全可执行、可重置且内部状态可访问的合成环境，从而实现安全、可扩展且具备确定性验证的智能体训练。

#### 2. 方法论：核心思想与技术细节
VeriEnv 的核心流程分为三个阶段：
*   **网站重建（Recreating Websites）**：利用编码智能体（如 GPT-5.2）根据真实网站的截图，自动编写前端、后端逻辑和数据库代码。最终生成一个包含代码（C）、数据库（D）和 Python SDK（P）的合成环境 $\tilde{E}$。
*   **迭代优化与调试**：编码智能体通过 Playwright MCP 与部署的合成网站交互，识别功能差异并自动修复 Bug，确保环境的稳定性和可重置性。
*   **可验证任务与评判生成**：
    *   利用 LLM 生成自然语言任务指令。
    *   同步生成基于 Python SDK 的**验证程序**。该程序能直接查询数据库状态（例如：检查订单是否真的存入数据库），从而提供确定性的二进制奖励信号（成功/失败），而非模糊的文本评分。
*   **自我进化学习**：智能体在合成环境中通过“拒绝采样微调”（Rejection Fine-tuning）进行训练，仅保留通过验证程序验证的成功轨迹。

#### 3. 实验设计
*   **数据集与场景**：
    *   **跨域泛化实验**：在 149 个克隆网站上生成 7,400 个任务进行训练，在 **WebArena-Lite** 和 **Mind2Web-Online** 基准测试上评估。
    *   **站点精通实验**：针对特定站点进行多次迭代训练，观察智能体在单一环境下的性能提升。
*   **对比方法**：
    *   **闭源模型**：GPT-4o, GPT-4o-mini, Claude-3.5-Sonnet。
    *   **开源基准**：Qwen3-4B, LLaMA-3.2-3B-Instruct。
    *   **现有训练协议**：Synatra（合成轨迹）、ADP（聚合数据集协议）、PAE（基于 LLM 评判的自我进化方法）。

#### 4. 资源与算力
*   **硬件**：使用 2 台 **NVIDIA A40 GPU** 进行分布式训练。
*   **训练配置**：基于 LLaMA-Factory 框架，学习率 $1 \times 10^{-5}$，训练 2 个 Epoch，最大序列长度 8,000 tokens。
*   **环境构建成本**：平均每个网站的克隆过程耗时 **83.5 分钟**，成本约为 **3.6 美元**（主要为 API 调用费用）。

#### 5. 实验数量与充分性
*   **规模**：构建了 149 个网站环境，涵盖购物、社交、工具等多种领域，生成了 7,400 个涵盖易、中、难三个等级的任务。
*   **评估维度**：
    *   **人类评估**：4 名计算机背景研究生对 30 个实例进行了双盲评估，涵盖功能正确性（90%）、视觉质量（4.7/5）和评判准确性（76%）。
    *   **消融与扩展性分析**：研究了训练环境数量（从 0% 到 100%）对性能的影响，证明了环境扩展的有效性。
*   **充分性**：实验设计较为全面，不仅对比了 SOTA 模型，还深入分析了失败案例（如端口冲突、CORS 配置等），具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **性能提升**：在 WebArena 上，VeriEnv 使 Qwen3-4B 提升了 **6.06** 个百分点，LLaMA-3.2-3B 提升了 **9.70** 个百分点。
*   **验证的重要性**：相比于 PAE 等依赖 LLM 评判的方法，VeriEnv 的确定性奖励信号能提供更稳定、持续的性能增长。
*   **环境扩展效应**：智能体的性能随训练环境数量的增加而稳步提升，显示出“环境缩放定律”的潜力。
*   **站点精通**：智能体可以通过在克隆环境中的反复练习，显著提高在对应真实站点上的成功率。

#### 7. 优点
*   **安全性与合规性**：完全解耦了训练过程与真实网络环境，避免了法律和道德风险。
*   **确定性反馈**：通过 Python SDK 直接访问后端状态，解决了 Web 任务评估中长期存在的“幻觉评分”问题。
*   **高度自动化**：从网站克隆到任务生成、轨迹验证、模型微调，全流程基本无需人工干预。

#### 8. 不足与局限
*   **复杂基建挑战**：对于依赖重度多媒体（如 YouTube 视频流）或复杂第三方集成（如支付网关）的网站，克隆质量会下降。
*   **评判器误差**：虽然是程序化验证，但如果数据库重置未保持随机种子一致，可能导致 24% 左右的评判错误。
*   **成本问题**：虽然比人工标注便宜，但大规模克隆数千个网站仍需可观的 API 支出。
*   **Sim-to-Real 差异**：尽管论文声称差异较小，但合成环境与真实复杂网络环境（如动态加载、反爬策略）之间仍可能存在细微的分布偏移。

（完）
