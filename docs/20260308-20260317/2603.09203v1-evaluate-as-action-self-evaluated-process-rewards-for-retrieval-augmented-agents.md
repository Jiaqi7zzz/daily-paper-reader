---
title: "Evaluate-as-Action: Self-Evaluated Process Rewards for Retrieval-Augmented Agents"
title_zh: Evaluate-as-Action：检索增强智能体的自评估过程奖励
authors: "Jiangming Shu, Yuxiang Zhang, Ye Ma, Xueyuan Lin, Jitao Sang"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09203v1"
tags: ["query:rl-tl"]
score: 9.0
evidence: 检索增强智能体的自评过程奖励
tldr: 针对检索增强智能体在多步推理中因噪声检索和奖励信号粗糙导致的可靠性问题，本文提出EvalAct框架。该框架将检索质量评估转化为显式动作，通过“搜索-评估”协议为每步检索提供即时过程信号。配合基于GRPO的PCAR优化方法，根据评估分数在片段级别调整优势函数，显著提升了模型在多跳问答等复杂任务中的性能与可靠性。
selection_source: fresh_fetch
motivation: 现有的检索增强智能体在多步推理中容易受噪声干扰，且仅靠结果奖励（Outcome-only RL）难以有效优化中间检索步骤。
method: 提出EvalAct框架，将检索评估设为显式动作，并引入PCAR优化方法，利用过程评估信号对不同轨迹片段的优势函数进行校准。
result: 在七个开放域问答基准测试中，EvalAct 取得了最佳平均准确率，尤其在多跳任务上表现出显著的性能增益。
conclusion: 通过将检索评估显式化并结合过程校准的强化学习，可以显著增强检索增强智能体在复杂推理任务中的可靠性和准确性。
---

## 摘要
检索增强智能体可以查询外部证据，但其在多步推理中的可靠性仍然受限：噪声检索可能会使多跳问答偏离轨道，而仅基于结果的强化学习提供的信用信号过于粗糙，无法优化中间步骤。我们提出了 EvalAct (Evaluate-as-Action)，它将隐式的检索质量评估转化为显式动作，并实施一种耦合的“搜索-评估”（Search-to-Evaluate）协议，使每次检索后立即产生结构化的评估分数，从而生成与交互轨迹对齐的过程信号。为了利用这些信号，我们引入了过程校准优势重缩放（Process-Calibrated Advantage Rescaling, PCAR），这是一种基于 GRPO 的优化方法，它根据评估分数在片段级别重缩放优势值，在强调可靠片段的同时保守地更新不确定片段。在七个开放域问答基准上的实验表明，EvalAct 取得了最佳的平均准确率，在多跳任务上增幅最大，消融实验验证了显式评估循环驱动了主要的改进，而 PCAR 提供了持续的额外收益。

## 速览
**TLDR**：针对检索增强智能体在多步推理中因噪声检索和奖励信号粗糙导致的可靠性问题，本文提出EvalAct框架。该框架将检索质量评估转化为显式动作，通过“搜索-评估”协议为每步检索提供即时过程信号。配合基于GRPO的PCAR优化方法，根据评估分数在片段级别调整优势函数，显著提升了模型在多跳问答等复杂任务中的性能与可靠性。 \
**Motivation**：现有的检索增强智能体在多步推理中容易受噪声干扰，且仅靠结果奖励（Outcome-only RL）难以有效优化中间检索步骤。 \
**Method**：提出EvalAct框架，将检索评估设为显式动作，并引入PCAR优化方法，利用过程评估信号对不同轨迹片段的优势函数进行校准。 \
**Result**：在七个开放域问答基准测试中，EvalAct 取得了最佳平均准确率，尤其在多跳任务上表现出显著的性能增益。 \
**Conclusion**：通过将检索评估显式化并结合过程校准的强化学习，可以显著增强检索增强智能体在复杂推理任务中的可靠性和准确性。

---

## Abstract
Retrieval-augmented agents can query external evidence, yet their reliability in multi-step reasoning remains limited: noisy retrieval may derail multi-hop question answering, while outcome-only reinforcement learning provides credit signals that are too coarse to optimize intermediate steps. We propose \textsc{EvalAct} (Evaluate-as-Action), which converts implicit retrieval quality assessment into an explicit action and enforces a coupled Search-to-Evaluate protocol so that each retrieval is immediately followed by a structured evaluation score, yielding process signals aligned with the interaction trajectory. To leverage these signals, we introduce Process-Calibrated Advantage Rescaling (PCAR), a GRPO-based optimization method that rescales advantages at the segment level according to evaluation scores, emphasizing reliable segments while updating uncertain ones conservatively. Experiments on seven open-domain QA benchmarks show that \textsc{EvalAct} achieves the best average accuracy, with the largest gains on multi-hop tasks, and ablations verify that the explicit evaluation loop drives the primary improvements while PCAR provides consistent additional benefits.

---

## 论文详细总结（自动生成）

这是一份关于论文《Evaluate-as-Action: Self-Evaluated Process Rewards for Retrieval-Augmented Agents》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**检索增强智能体（RAG Agents）在处理复杂多步推理（如多跳问答）时的可靠性问题**。
*   **研究动机**：现有的 RAG 智能体在面对多跳问题时，容易受到噪声检索结果的干扰，导致推理轨迹偏离。
*   **核心痛点**：
    1.  **错误传播**：缺乏显式的中间验证机制，一个无关文档可能毁掉整个推理链。
    2.  **信用分配粗糙**：传统的强化学习（如 GRPO）通常只依赖最终答案的正确性（Outcome Reward），无法区分长轨迹中哪些检索步骤是有益的，哪些是冗余或误导的。

### 2. 方法论：核心思想与关键技术
论文提出了 **EvalAct** 框架，其核心思想是将“检索质量评估”从模型内部的隐式思考转变为一种**显式的、可训练的动作**。

*   **EvalAct (Evaluate-as-Action) 协议**：
    *   强制执行“**搜索 $\rightarrow$ 评估**”（Search $\rightarrow$ Evaluate）的耦合协议。
    *   每次执行 `Search` 动作后，智能体必须立即执行 `Evaluate` 动作，输出一段文本评估和 0-10 的置信度分数 $z$。
    *   环境根据分数 $z$ 返回离散的反馈信号（低/中/高可靠性），引导后续决策。
*   **PCAR (过程校准优势重缩放) 优化**：
    *   基于 **GRPO** 算法。
    *   **核心逻辑**：不再对整条轨迹应用统一的优势值（Advantage），而是利用 `Evaluate` 动作产生的置信度分数，在**片段（Segment）级别**对优势值进行重缩放。
    *   **公式原理**：计算轨迹内标准化的可靠性信号 $\tilde{z}$，通过公式 $\hat{A}_{i,t} = A_i \cdot \text{clamp}(1 + \lambda_{i,k} \tilde{z}_{i,k}, \delta, \infty)$ 调整梯度。这使得可靠的步骤获得更大的梯度更新，而不确定的步骤更新则更保守。

### 3. 实验设计
*   **数据集**：涵盖 7 个开放域问答基准。
    *   **单跳任务**：Natural Questions (NQ), TriviaQA, PopQA。
    *   **多跳任务**：HotpotQA, 2WikiMultihopQA, MuSiQue, Bamboogle。
*   **对比方法（Baselines）**：
    *   基础方法：直接生成、朴素 RAG。
    *   提示词方法：IRCoT。
    *   智能体/强化学习方法：Search-o1, Search-R1, AutoRefine。
*   **实验设置**：使用 Qwen2.5-3B/7B-Instruct 作为骨干模型，检索环境基于 Wikipedia (2018) 和 BM25 检索器。

### 4. 资源与算力
*   **硬件**：使用了 **8 张 NVIDIA A100 GPU**。
*   **训练细节**：采用全参数优化（Full-parameter optimization）和梯度检查点（Gradient Checkpointing）。
*   **训练规模**：RL 训练集包含 27k 实例，SFT 预热阶段使用 2k 条合成轨迹。
*   **时长**：文中未明确给出具体的训练总时长，但提到训练进行了 2 个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：在 7 个不同难度的标准数据集上进行了测试，涵盖了从简单事实检索到复杂逻辑推理的场景。
*   **消融实验**：非常充分。作者分别验证了：
    1.  **SFT 预热的作用**：证明 SFT 主要负责格式对齐。
    2.  **Eval 循环的结构作用**：证明显式评估是性能提升的核心（提升约 7.5%）。
    3.  **PCAR 算法的增益**：证明过程校准能带来额外的性能优化（提升约 1.2%）。
    4.  **超参数敏感性**：测试了不同重缩放强度对结果的影响。
*   **客观性**：通过固定检索预算、相同的检索器和语料库，确保了与基准方法在证据获取阶段的公平对比。

### 6. 主要结论与发现
*   **性能领先**：EvalAct 在 3B 和 7B 规模下均取得了最佳平均准确率，尤其在多跳任务上优势巨大（如在 2WikiMultihopQA 上比最强基准高出 10 个百分点以上）。
*   **显式评估的价值**：将评估设为动作比隐式的自我修正更有效，能显著抑制多步推理中的错误传播。
*   **PCAR 的有效性**：无需人工标注的过程奖励模型（PRM），仅靠模型自生成的置信度分数即可实现精细的信用分配，提升学习稳定性。

### 7. 优点（亮点）
*   **创新性**：将“内省”转化为“动作”，为智能体提供了一种天然的、与轨迹对齐的过程监督信号。
*   **无需外部 Oracle**：不需要昂贵的人工过程标注，也不依赖外部强大的模型作为评委，实现了自给自足的优化。
*   **即插即用**：PCAR 算法可以作为 GRPO 等主流 RL 算法的插件，具有较好的通用性。

### 8. 不足与局限
*   **灵活性受限**：强制性的 1:1 “搜索-评估”耦合虽然有效，但限制了智能体的自主性（例如在某些简单情况下可能不需要评估）。
*   **任务覆盖面**：目前仅在问答（QA）任务上验证，在网页导航、代码生成等更复杂的智能体场景下的表现尚待观察。
*   **模型规模限制**：实验最高仅做到 7B 参数，在超大规模模型（如 70B+）上的迁移效果和必要性有待进一步研究。

（完）
