---
title: Reinforcement Learning with Conditional Expectation Reward
title_zh: 基于条件期望奖励的强化学习
authors: "Changyi Xiao, Caijun Xu, Yixin Cao"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10624v1"
tags: ["query:rl-tl"]
score: 9.0
evidence: 具有可验证奖励和隐式验证器的强化学习
tldr: 针对强化学习在通用推理领域中依赖手工验证规则的局限性，本文提出条件期望奖励（CER）。CER利用大模型自身作为隐式验证器，通过计算给定生成答案下参考答案的条件期望似然，提供软性分级奖励。该方法无需外部验证器，适用于自由格式答案，在数学和通用推理任务中均表现出色，显著提升了模型的推理能力。
selection_source: fresh_fetch
motivation: 传统的强化学习验证奖励依赖手工规则，难以应用于答案形式多样的通用推理领域。
method: 提出CER机制，将大模型作为隐式验证器，计算生成答案后出现参考答案的条件期望似然作为软奖励。
result: 实验证明CER在数学和通用推理任务上均有效，能够提供比二元反馈更细致的奖励信号。
conclusion: CER是一种灵活且通用的验证机制，消除了对外部验证器的依赖，拓宽了强化学习在复杂推理任务中的应用。
---

## 摘要
具有可验证奖励的强化学习 (RLVR) 已被证明在增强大语言模型的推理能力方面非常有效，特别是在数学等可以构建可靠的基于规则的验证器的领域。然而，对手工设计的、特定领域验证规则的依赖，极大地限制了 RLVR 在具有自由形式答案的通用推理领域中的适用性，在这些领域中，有效答案通常表现出显著的多样性，难以建立完整且准确的规则。为了解决这一局限性，我们提出了条件期望奖励 (CER)，它利用大语言模型本身作为隐式验证器，因此适用于通用领域，并消除了对外部验证器或辅助模型的需求。CER 被定义为在给定生成答案的条件下，生成参考答案的预期似然度。与产生二元反馈的基于规则的验证器相比，CER 提供了一种软性的、分级的奖励信号，反映了不同程度的正确性，使其更适用于答案正确性存在差异的任务。实验结果表明，CER 在包括数学和通用领域在内的广泛推理任务中均有效，表明 CER 是一种灵活且通用的验证机制。代码可在 https://github.com/changyi7231/CER 获取。

## Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) has proven effective in enhancing the reasoning capabilities of large language models, particularly in domains such as mathematics where reliable rule-based verifiers can be constructed. However, the reliance on handcrafted, domain-specific verification rules substantially limits the applicability of RLVR to general reasoning domains with free-form answers, where valid answers often exhibit significant variability, making it difficult to establish complete and accurate rules. To address this limitation, we propose Conditional Expectation Reward (CER), which leverages the large language model itself as an implicit verifier, and is therefore applicable to general domains and eliminates the need for external verifiers or auxiliary models. CER is defined as the expected likelihood of generating the reference answer conditioned on the generated answer. In contrast to rule-based verifiers that yield binary feedback, CER provides a soft, graded reward signal that reflects varying degrees of correctness, making it better suited to tasks where answers vary in correctness. Experimental results demonstrate that CER is effective across a wide range of reasoning tasks, spanning both mathematical and general domains, indicating that CER serves as a flexible and general verification mechanism. The code is available at https://github.com/changyi7231/CER.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为**条件期望奖励（Conditional Expectation Reward, CER）**的新机制，旨在解决强化学习在通用推理领域中由于缺乏可靠验证器而难以应用的问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的“具有可验证奖励的强化学习”（RLVR）高度依赖于手工设计的规则验证器（如数学公式匹配、代码执行）。
*   **研究背景**：在物理、化学、金融等通用推理领域，答案形式多样且具有语义复杂性，难以制定完备的规则。
*   **局限性**：传统的规则验证器通常只提供二元反馈（对或错），导致奖励信号过于稀疏，且无法识别“部分正确”的答案，限制了模型在非数学领域的泛化能力。

### 2. 方法论：条件期望奖励 (CER)
*   **核心思想**：利用大语言模型（LLM）自身作为“隐式验证器”。CER 衡量的是：**在模型已经生成了某个答案 $a$ 的条件下，再次生成参考答案 $a^*$ 的预期似然度**。
*   **关键技术细节**：
    *   **定义**：$\rho(a, a^*) = E_{s' \sim \pi_\theta(\cdot|q, a)} [\pi_\theta(a^*|s', q)]$。其直觉是，如果生成的答案 $a$ 与参考答案 $a^*$ 语义一致，模型在后续生成中产出 $a^*$ 的概率会更高。
    *   **理论特性**：论文证明了 CER 是“精确匹配”（Exact-Match）奖励的平滑松弛版本，且在期望值上与精确匹配等价，但能提供连续的分级信号。
    *   **经验公式**：通过贝叶斯定理和蒙特卡洛采样，将 CER 转化为可计算的形式：
        $$R(q, s, a, a^*) \approx \frac{\sum_{j=1}^M \pi_\theta(a|s_j, q) \pi_\theta(a^*|s_j, q)}{\sum_{j=1}^M \pi_\theta(a|s_j, q)}$$
    *   **效率优化**：CER 无需额外的采样成本。它直接复用策略梯度训练中已经生成的 $N$ 个样本进行计算，并通过张量化运算提高效率。

### 3. 实验设计
*   **训练数据集**：
    *   **数学领域**：MATH-7.5K。
    *   **通用领域**：WebInstruct（筛选出 50K 条大学难度的非数学问题，涵盖物理、生物、金融等）。
*   **评估基准 (Benchmarks)**：
    *   数学：MATH500, AMC23, AIME2024, AIME2025。
    *   通用：SuperGPQA, MMLU-Pro。
*   **对比方法 (Baselines)**：
    *   **Exact-Match**：硬性字符串匹配。
    *   **Rule-based**：基于手工规则的验证（如 math-verify）。
    *   **VeriFree**：基于困惑度（Perplexity）的验证器。
    *   **General-verifier**：使用外部大模型作为判分器。

### 4. 资源与算力
*   **硬件环境**：实验使用了 **4 台 NVIDIA H100 GPU**。
*   **训练时长**：论文在 Table 3 中记录了不同方法的运行时间。例如，CER (M=16) 的运行时间约为 67.4 小时，而 Exact-match 约为 45.2 小时。
*   **模型规模**：主要针对 Qwen3-4B-Base 和 Qwen3-8B-Base 进行实验。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个不同规模的模型（4B 和 8B）以及两个不同领域的训练集。
*   **消融与分析**：
    *   对比了不同采样数 $M$ 对性能和效率的影响。
    *   验证了 CER 与规则奖励（Rule-based）结合的效果（Rule+CER）。
    *   提供了 CER 计算过程的可视化分析。
*   **评价**：实验设计较为全面，不仅验证了通用领域的有效性，还证明了在数学领域 CER 也能达到与专业规则验证器相当的水平，实验结果具有客观性。

### 6. 主要结论与发现
*   **通用性强**：CER 在非数学领域显著优于 Exact-Match 和基于困惑度的方法，甚至超过了某些需要外部模型的验证器。
*   **软奖励优势**：CER 能够为语义相似但字面不同的答案提供正向奖励，解决了奖励稀疏问题。
*   **互补性**：CER 与规则验证器可以结合使用，在数学任务中进一步提升性能。
*   **自一致性**：理论和实验均证明，CER 能够利用模型的内部一致性来强化正确推理路径。

### 7. 优点与亮点
*   **无需外部依赖**：不需要手工规则，也不需要额外的辅助模型（如判分模型），降低了 RL 训练的门槛。
*   **理论支撑**：将 CER 解释为精确匹配的平滑松弛，为其合理性提供了数学依据。
*   **计算高效**：通过复用采样数据，在不增加采样开销的情况下实现了复杂的奖励计算。

### 8. 不足与局限
*   **依赖参考答案**：CER 仍然需要标准答案（Ground Truth）作为参考，无法完全脱离监督信号进行纯粹的自我发现。
*   **计算开销**：虽然不增加采样成本，但计算条件概率矩阵仍需额外的模型前向传播，导致总训练时间比简单匹配长约 50%。
*   **模型能力依赖**：CER 的效果取决于模型本身的初始能力；如果模型对某个问题完全无法理解，其内部一致性信号可能会失效。

（完）
