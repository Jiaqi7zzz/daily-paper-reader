---
title: "SAGE: Multi-Agent Self-Evolution for LLM Reasoning"
title_zh: SAGE：大语言模型推理的多智能体自我演化
authors: "Yulin Peng, Xinxin Zhu, Chenxing Wei, Nianbo Zeng, Leilei Wang, Ying Tiffany He, F. Richard Yu"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15255v1"
tags: ["query:rl-tl"]
score: 9.0
evidence: 具有可验证奖励的强化学习用于推理
tldr: SAGE是一个多智能体自我进化框架，旨在提升大语言模型的推理能力。它通过挑战者、规划者、执行者和评论者四个角色的协同演化，在仅需少量种子数据的情况下，实现任务生成、结构化规划、问题求解及质量过滤的闭环。该方法有效解决了自我博弈中缺乏规划和质量控制的问题，在数学和代码生成任务上显著提升了模型性能。
selection_source: fresh_fetch
motivation: 现有的强化学习推理方法过度依赖人工标注，且自我博弈在长程多步推理中缺乏显式规划和稳定性。
method: 提出由挑战者、规划者、执行者和评论者组成的闭环框架，通过自我生成任务、制定计划、求解及质量过滤实现模型协同进化。
result: "在Qwen-2.5-7B模型上，SAGE在LiveCodeBench和OlympiadBench上分别实现了8.9%和10.7%的性能提升。"
conclusion: SAGE证明了通过多智能体协同进化和严格的质量控制，可以在减少对人工数据依赖的同时，显著增强大模型的复杂推理能力。
---

## 摘要
具有可验证奖励的强化学习提升了大语言模型（LLMs）的推理能力，但许多方法仍依赖于大规模的人工标注数据集。虽然自我博弈（self-play）减少了这种依赖，但它往往缺乏显式的规划和强大的质量控制，限制了在长程多步推理中的稳定性。我们提出了 SAGE（Self-evolving Agents for Generalized reasoning Evolution，用于广义推理演化的自我演化智能体），这是一个闭环框架，其中四个智能体：挑战者（Challenger）、规划者（Planner）、求解者（Solver）和评论者（Critic），仅使用少量的种子集，从共享的 LLM 主干网络中共同演化。挑战者不断生成难度递增的任务；规划者将每个任务转换为结构化的多步计划；求解者遵循该计划生成答案，其正确性由外部验证器确定。评论者对生成的题目和计划进行评分和过滤，以防止课程漂移并维持训练信号的质量，从而实现稳定的自我训练。在数学和代码生成基准测试中，SAGE 在不同模型规模上均实现了持续的提升，使 Qwen-2.5-7B 模型在 LiveCodeBench 上提升了 8.9%，在 OlympiadBench 上提升了 10.7%。

## Abstract
Reinforcement learning with verifiable rewards improves reasoning in large language models (LLMs), but many methods still rely on large human-labeled datasets. While self-play reduces this dependency, it often lacks explicit planning and strong quality control, limiting stability in long-horizon multi-step reasoning. We present SAGE (Self-evolving Agents for Generalized reasoning Evolution), a closed-loop framework where four agents: Challenger, Planner, Solver, and Critic, co-evolve from a shared LLM backbone using only a small seed set. The Challenger continuously generates increasingly difficult tasks; the Planner converts each task into a structured multi-step plan; and the Solver follows the plan to produce an answer, whose correctness is determined by external verifiers. The Critic scores and filters both generated questions and plans to prevent curriculum drift and maintain training signal quality, enabling stable self-training. Across mathematics and code-generation benchmarks, SAGE delivers consistent gains across model scales, improving the Qwen-2.5-7B model by 8.9% on LiveCodeBench and 10.7% on OlympiadBench.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **SAGE** 的多智能体自我演化框架，旨在通过强化学习（RL）提升大语言模型（LLM）在数学和代码领域的推理能力。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：虽然具有可验证奖励的强化学习（RLVR）能显著提升模型推理能力，但现有方法（如 DeepSeek-R1）通常依赖大规模人工标注数据。现有的“自我博弈”（Self-play）方法虽然减少了数据依赖，但在处理长程、多步推理任务时，往往缺乏显式的规划（Planning）和严格的质量控制，导致训练信号不稳定或出现“课程漂移”（Curriculum Drift）。
*   **核心问题**：如何在仅使用极少量种子数据的情况下，构建一个能够自主生成高质量任务、制定策略并稳定进化的闭环推理系统？

### 2. 方法论：核心思想与关键技术
SAGE 提出了一个由四个专门智能体组成的闭环框架，这些智能体共享同一个 LLM 主干网络，但通过不同的角色指令进行区分：
*   **核心思想**：通过“挑战-规划-求解-评判”的对抗与协作机制，实现模型的自我进化。
*   **关键角色**：
    *   **挑战者 (Challenger)**：基于种子示例生成新任务及其验证器（如代码测试用例）。它通过“难度奖励”进行优化——如果求解者失败，挑战者获得奖励，从而推动生成更难的任务。
    *   **规划者 (Planner)**：将任务分解为结构化的多步计划，为求解提供策略引导。
    *   **求解者 (Solver)**：遵循计划执行具体推理并输出最终答案。
    *   **评论者 (Critic)**：双重作用。一是提供格式奖励；二是对生成的题目和计划进行质量评分（1-10分），只有超过阈值的任务才会被加入训练池，起到“质量闸门”的作用。
*   **算法流程**：
    *   使用 **Task-Relative REINFORCE++** 算法进行联合更新。
    *   引入**角色相关的优势归一化 (Per-role Advantage Normalization)**，解决不同角色目标异构导致的训练不稳定问题。
    *   **闭环逻辑**：挑战者生成任务 -> 评论者过滤 -> 规划者制定计划 -> 求解者执行 -> 外部验证器（Verifier）给出最终正确性反馈。

### 3. 实验设计
*   **数据集与场景**：
    *   **种子数据**：仅使用 500 条来自 MATH、GSM8K、HumanEval 和 MBPP 的样本。
    *   **数学基准 (Math)**：GSM8K, MATH (分布内); AIME 2024/2025, AMC, OlympiadBench (分布外/竞赛级)。
    *   **代码基准 (Code)**：HumanEval+, MBPP+ (分布内); LiveCodeBench v1-v5 (分布外/实时更新)。
*   **对比方法**：
    *   基础模型 (Base Models)。
    *   **AZR (Absolute Zero Reasoning)**：一种基于自我博弈的推理训练方法。
    *   **MAE (Multi-Agent Evolve)**：另一种多智能体协同进化框架。

### 4. 资源与算力
*   **算力说明**：论文提到实验基于 **VeRL** 框架实现。
*   **具体配置**：使用了 **LoRA** 微调技术（Rank=128），学习率为 3e-6，训练步数为 200 步。
*   **缺失信息**：论文**未明确说明**具体的 GPU 型号（如 A100 或 H100）、GPU 数量以及总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在三个不同规模/类型的模型上进行了验证：Qwen-2.5-3B-Instruct、Qwen-2.5-7B-Instruct 和 Qwen-3-4B-Base。
    *   涵盖了 10 个以上的数学和代码主流基准测试。
    *   进行了详细的**消融实验**，分别验证了移除挑战者、求解者或评论者对最终性能的影响。
*   **充分性评价**：实验设计较为充分，不仅关注分布内（ID）性能，还重点评估了分布外（OOD）的泛化能力。通过训练动态分析（Training Dynamics），展示了模型在 100-120 步左右达到性能巅峰，随后可能出现过拟合，这为自我训练的停止策略提供了参考。

### 6. 主要结论与发现
*   **性能提升显著**：SAGE 在所有模型规模上均优于基线。例如，在 Qwen-2.5-7B 上，LiveCodeBench 提升了 8.9%，OlympiadBench 提升了 10.7%。
*   **泛化能力强**：SAGE 在 OOD 任务上的表现尤为突出，证明了多智能体协作生成的课程比单一角色的自我博弈更具鲁棒性。
*   **自主数据扩展**：通过挑战者智能体，训练题目池从最初的 500 条自动扩展到了 20,000 条以上，且保持了较高的质量。

### 7. 优点（亮点）
*   **角色专业化**：将推理过程拆解为规划和执行，有效降低了复杂问题的处理难度。
*   **质量控制机制**：引入评论者作为过滤器，有效防止了合成数据训练中常见的“垃圾进，垃圾出”问题。
*   **数据效率极高**：仅需 500 条种子数据即可启动，极大地降低了对人工标注的依赖。

### 8. 不足与局限
*   **领域局限性**：目前仅适用于**可验证领域**（数学、代码），因为这些领域有客观的正确性标准（Ground Truth 或单元测试）。对于主观性较强的开放式任务（如创意写作、法律分析），该框架难以直接应用。
*   **过拟合风险**：训练动态显示，长时间训练会导致性能下降，说明模型可能会过度拟合于自我生成的题目分布。
*   **计算开销**：虽然共享主干网络，但多智能体交互（生成计划、多次采样验证）在训练阶段的推理开销比单智能体 RL 更大。

（完）
