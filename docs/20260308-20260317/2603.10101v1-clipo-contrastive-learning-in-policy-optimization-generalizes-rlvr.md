---
title: "CLIPO: Contrastive Learning in Policy Optimization Generalizes RLVR"
title_zh: CLIPO：策略优化中的对比学习泛化了 RLVR
authors: "Sijia Cui, Pengyu Cheng, Jiajun Song, Yongbo Gai, Guojun Zhang, Zhechao Yu, Jianhe Lin, Xiaoxi Jiang, Guanjun Jiang"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.10101v1"
tags: ["query:rl-tl"]
score: 9.0
evidence: 通过策略优化中的对比学习推广RLVR
tldr: 针对大语言模型在强化学习（RLVR）中仅依赖最终答案奖励导致的过程错误和幻觉问题，本文提出了 CLIPO。该方法在策略优化中引入对比学习机制，通过优化多个成功推理路径之间的对比损失，使模型捕捉正确推理的共性结构。实验证明，CLIPO 在多个推理基准上显著提升了模型的泛化性与鲁棒性，有效抑制了推理过程中的幻觉现象。
selection_source: fresh_fetch
motivation: 现有的 RLVR 方法仅关注最终答案的正确性而忽略中间步骤，导致模型容易通过错误的推理过程凑出正确答案，产生幻觉和答案抄袭。
method: 提出 CLIPO 框架，在策略优化中加入对比学习损失，通过对多个成功轨迹进行正则化来捕捉正确推理路径中的共性结构。
result: CLIPO 在多个推理基准测试中一致性地超越了现有的 RLVR 基准，显著提升了模型在推理任务中的泛化能力和鲁棒性。
conclusion: 通过引入跨轨迹的对比学习正则化，CLIPO 有效缓解了推理步骤的不一致性，为提升大语言模型的逻辑推理质量提供了新路径。
---

## 摘要
具有可验证奖励的强化学习（RLVR）显著提升了大语言模型（LLM）的推理能力。然而，RLVR 仅依赖最终答案作为结果奖励，忽略了中间推理步骤的正确性。在这些过程错误但结果正确的样本（rollouts）上进行训练会导致幻觉和答案抄袭，严重损害模型的泛化能力和鲁棒性。为解决这一问题，我们将对比学习机制引入策略优化（CLIPO），以泛化 RLVR 过程。通过在成功的样本上优化对比损失，CLIPO 引导 LLM 捕捉正确推理路径中共享的不变结构。与 RLVR 中原始的单路径监督相比，这提供了一种更鲁棒的跨轨迹正则化，有效地缓解了步骤级推理的不一致性，并抑制了幻觉伪影。实验表明，CLIPO 在多种推理基准测试中持续改进了多个 RLVR 基准模型，证明了其在 LLM 策略优化的泛化能力和鲁棒性方面具有一致的提升。我们的代码和训练配方可在 https://github.com/Qwen-Applications/CLIPO 获取。

## 速览
**TLDR**：针对大语言模型在强化学习（RLVR）中仅依赖最终答案奖励导致的过程错误和幻觉问题，本文提出了 CLIPO。该方法在策略优化中引入对比学习机制，通过优化多个成功推理路径之间的对比损失，使模型捕捉正确推理的共性结构。实验证明，CLIPO 在多个推理基准上显著提升了模型的泛化性与鲁棒性，有效抑制了推理过程中的幻觉现象。 \
**Motivation**：现有的 RLVR 方法仅关注最终答案的正确性，忽略了中间推理步骤的准确性，容易导致模型产生幻觉和答案抄袭。 \
**Method**：在策略优化中引入对比学习（CLIPO），通过对多个成功的推理轨迹进行对比损失优化，提取正确推理路径中的不变结构。 \
**Result**：实验结果显示，CLIPO 在多个推理基准测试中均优于传统的 RLVR 基准，显著增强了模型的泛化能力和鲁棒性。 \
**Conclusion**：CLIPO 通过跨轨迹的正则化机制，有效解决了强化学习推理中的步骤不一致问题，是提升大模型推理质量的有效方案。

---

## Abstract
Reinforcement Learning with Verifiable Rewards (RLVR) has significantly advanced the reasoning capacity of Large Language Models (LLMs). However, RLVR solely relies on final answers as outcome rewards, neglecting the correctness of intermediate reasoning steps. Training on these process-wrong but outcome-correct rollouts can lead to hallucination and answer-copying, severely undermining the model's generalization and robustness. To address this, we incorporate a Contrastive Learning mechanism into the Policy Optimization (CLIPO) to generalize the RLVR process. By optimizing a contrastive loss over successful rollouts, CLIPO steers the LLM to capture the invariant structure shared across correct reasoning paths. This provides a more robust cross-trajectory regularization than the original single-path supervision in RLVR, effectively mitigating step-level reasoning inconsistencies and suppressing hallucinatory artifacts. In experiments, CLIPO consistently improves multiple RLVR baselines across diverse reasoning benchmarks, demonstrating uniform improvements in generalization and robustness for policy optimization of LLMs. Our code and training recipes are available at https://github.com/Qwen-Applications/CLIPO.

---

## 论文详细总结（自动生成）

### 论文总结：CLIPO - 策略优化中的对比学习泛化了 RLVR

#### 1. 核心问题与整体含义
论文针对当前大语言模型（LLM）推理训练中的**可验证奖励强化学习（RLVR）**存在的局限性提出了挑战。
*   **研究动机**：现有的 RLVR 方法（如 GRPO）主要依赖最终答案的正确性（Outcome-based Rewards）作为反馈。这种“黑盒”奖励机制忽略了中间推理步骤的逻辑正确性，导致模型容易产生“过程错误但结果正确”的样本，进而引发幻觉、答案背诵（过拟合）以及泛化能力差的问题。
*   **核心目标**：在不需要昂贵的人工过程标注（PRM）的前提下，通过引入对比学习机制，利用正确推理路径之间内在的逻辑一致性来约束模型，使其捕捉推理任务中的“不变结构”。

#### 2. 方法论：CLIPO 框架
CLIPO（Contrastive Learning in Policy Optimization）的核心思想是：**“幸福的推理路径都是相似的，而错误的推理路径则各有各的错误。”**
*   **核心思想**：通过最大化同一问题下多个成功推理轨迹（Positive Rollouts）之间的语义相似度，并最小化成功轨迹与失败轨迹（Negative Rollouts）之间的相似度，来提取稳健的逻辑流。
*   **关键技术细节**：
    1.  **句子级表示**：在 LLM 的 Transformer 骨干网络之上添加一个轻量级的**对比头（Contrastive Head）**。对响应序列的最后隐藏状态进行均值池化（Mean Pooling），得到轨迹级别的语义嵌入。
    2.  **对比损失（InfoNCE）**：在每个采样组（Rollout Group）内，将正确轨迹视为正样本对，错误轨迹视为负样本，计算 InfoNCE 损失。
    3.  **奖励集成**：将对比损失转化为一个密集的**辅助奖励信号 $r_{CL}$**，与原始的二元结果奖励 $r$ 相加，形成最终奖励 $r' = r + \lambda \cdot r_{CL}$。
    4.  **去噪机制**：这种跨轨迹的正则化充当了去噪器，放大了共享的逻辑结构，抑制了非系统性的推理错误和幻觉。

#### 3. 实验设计
论文设计了两个难度递增的实验轨道（Tracks）：
*   **数据集与场景**：
    *   **Track I（基础推理）**：在 **GSM8K** 上训练，评估 8 个基准测试，包括符号化变体（GSM8K-Symbolic）、扰动变体（P1, P2）以及通用推理（MMLU, TruthfulQA 等）。
    *   **Track II（竞赛级推理）**：在 **MATH 7.5K** 上训练，评估 MATH500、Math-Perturb（简单/困难）以及高难度的 AMC23、AIME、AIME25 竞赛题。
*   **Benchmark 与对比方法**：
    *   **基础模型**：Qwen2.5-3B/7B-Instruct, Llama3.1-8B-Instruct, DeepSeek-R1-Distill-Qwen-7B。
    *   **对比算法**：GRPO, GSPO, DAPO, GMPO 等主流组策略优化算法。

#### 4. 资源与算力
*   **框架**：使用阿里巴巴开源的 **VERL** 强化学习框架。
*   **算力细节**：文中提到实验配置为每节点 8 张 GPU（`n_gpus_per_node: 8`），但**未明确说明总节点数、具体的 GPU 型号（如 A100 或 H100）以及总训练时长**。
*   **超参数**：Track I 训练 5 轮，Track II 训练 8 轮；最大响应长度分别为 2048 和 4096 标记。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了大量的对比实验，涵盖了 4 种不同的 RL 算法基准和 4 种不同的模型架构。
*   **消融实验**：非常充分。作者探讨了：
    *   对比头是否需要更新（固定 vs. 联合训练）。
    *   不同的对比损失函数（InfoNCE, SupCon, SoftNN）。
    *   温度系数 $\tau$、组大小（Group Size）对性能的影响。
    *   语义嵌入的可视化分析（t-SNE）。
*   **客观性**：通过在多个分布外（OOD）和扰动数据集上测试，验证了方法的鲁棒性，实验设计较为公平、客观。

#### 6. 主要结论与发现
*   **一致性提升**：CLIPO 在所有测试的 RL 算法（GRPO, GSPO 等）之上均实现了性能增益。
*   **泛化与鲁棒性**：在 GSM8K-P2（复杂扰动）和 AIME（高难度竞赛）等任务上提升尤为显著，证明对比学习有助于模型理解深层逻辑而非死记硬背。
*   **跨模型有效性**：无论是 Qwen、Llama 还是经过蒸馏的 DeepSeek-R1 模型，CLIPO 均表现出良好的适配性。
*   **表征学习**：可视化证明，经过 CLIPO 训练后，正确推理路径在嵌入空间中形成了更清晰的聚类。

#### 7. 优点
*   **无需额外标注**：相比 PRM 需要昂贵的人工步骤标注，CLIPO 仅利用环境反馈的二元奖励即可实现过程监督。
*   **插件式设计**：可以无缝集成到任何基于组（Group-based）的策略优化算法中。
*   **解决幻觉问题**：通过强制正确路径的语义对齐，有效地过滤了推理过程中的随机噪声和逻辑断裂。

#### 8. 不足与局限
*   **计算开销**：虽然对比头很轻量，但计算轨迹嵌入和组内对比损失会带来额外的计算负担。
*   **依赖组采样**：CLIPO 依赖于组内同时存在多个正确和错误样本。如果模型性能极差（全错）或极好（全对），对比信号将失效（退化情况）。
*   **超参敏感性**：实验显示温度系数 $\tau$ 和奖励缩放因子 $\lambda$ 对结果有较大影响，可能需要针对不同任务进行精细调优。
*   **算力信息缺失**：未披露具体 GPU 资源消耗，不便于其他研究者评估复现成本。

（完）
