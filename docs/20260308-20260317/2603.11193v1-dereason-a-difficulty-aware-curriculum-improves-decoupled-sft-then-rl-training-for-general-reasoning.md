---
title: "DeReason: A Difficulty-Aware Curriculum Improves Decoupled SFT-then-RL Training for General Reasoning"
title_zh: DeReason：一种难度感知的课程学习方法，旨在改进通用推理的解耦式 SFT-RL 训练
authors: "Hanxu Hu, Yuxuan Wang, Maggie Huan, Jannis Vamvas, Yinya Huang, Zhijiang Guo, Rico Sennrich"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.11193v1"
tags: ["query:rl-tl"]
score: 9.0
evidence: 具有可验证奖励的强化学习（RLVR）用于推理能力提升
tldr: 本研究针对通用STEM领域的推理能力提升，探讨了监督微调（SFT）与强化学习（RL）的协同作用。研究发现直接对基座模型进行RL效率低下，而先SFT后RL效果更佳。为此，作者提出了DeReason框架，通过难度感知的数据解耦策略，将非推理密集型数据用于SFT以建立领域知识，将推理密集型难题留给RL以培养复杂推理能力。实验证明，该方法在多个基准测试中显著优于单一训练或随机划分策略，为通用推理能力的后训练提供了高效方案。
selection_source: fresh_fetch
motivation: 针对通用STEM领域，直接对基座模型进行强化学习效率极低，且SFT与RL在数据分配上的互补关系尚未得到充分探索。
method: 提出DeReason策略，利用LLM评分将数据按推理强度解耦，非推理密集型数据用于SFT建立基础，推理密集型数据用于RL提升复杂推理。
result: 在通用STEM和数学基准测试中，DeReason显著优于仅SFT、仅RL以及随机划分数据的序列训练基准。
conclusion: 难度感知的SFT与RL数据解耦训练是提升大模型通用推理能力的有效路径，为后训练阶段的数据分配提供了系统性指导。
---

## 摘要
带有可验证奖励的强化学习（RLVR）已成为激发大语言模型推理能力的强大范式，尤其是在数学和编程领域。虽然最近的研究已将该范式扩展到更广泛的通用科学（STEM）领域，但在这些背景下，监督微调（SFT）与强化学习（RL）之间复杂的相互作用仍未得到充分探索。在本文中，我们通过受控实验揭示了一个关键挑战：对于通用 STEM 领域，直接在基座模型上应用 RL 的样本效率极低，且其表现始终不如在适度质量响应上进行的 SFT。然而，先 SFT 后 RL 的顺序训练可以进一步提升性能，这表明这两个阶段发挥着互补作用，且训练数据在两者之间的分配方式至关重要。因此，我们提出了 DeReason，一种针对通用推理的基于难度的训练数据解耦策略。DeReason 通过基于 LLM 评分估算的推理强度，将训练数据划分为推理密集型和非推理密集型子集。它将覆盖面广、非推理密集型的问题分配给 SFT，以建立基础领域知识；并将一小部分集中的难题保留给 RL，以培养复杂的推理能力。我们证明，这种有原则的解耦比随机拆分数据进行顺序 SFT 和 RL 具有更好的性能。在通用 STEM 和数学基准测试上的广泛实验表明，我们的解耦课程训练显著优于仅 SFT、仅 RL 以及随机拆分的基准方案。我们的工作系统地研究了通用推理中 SFT 与 RL 之间的相互作用，提供了一种高效且通用的后训练方案。

## Abstract
Reinforcement learning with Verifiable Rewards (RLVR) has emerged as a powerful paradigm for eliciting reasoning capabilities in large language models, particularly in mathematics and coding. While recent efforts have extended this paradigm to broader general scientific (STEM) domains, the complex interplay between supervised fine-tuning (SFT) and RL in these contexts remains underexplored. In this paper, we conduct controlled experiments revealing a critical challenge: for general STEM domains, RL applied directly to base models is highly sample-inefficient and is consistently surpassed by supervised fine-tuning (SFT) on moderate-quality responses. Yet sequential SFT followed by RL can further improve performance, suggesting that the two stages play complementary roles, and that how training data is allocated between them matters. Therefore, we propose DeReason, a difficulty-based data decoupling strategy for general reasoning. DeReason partitions training data by reasoning intensity estimated via LLM-based scoring into reasoning-intensive and non-reasoning-intensive subsets. It allocates broad-coverage, non-reasoning-intensive problems to SFT to establish foundational domain knowledge, and reserves a focused subset of difficult problems for RL to cultivate complex reasoning. We demonstrate that this principled decoupling yields better performance than randomly splitting the data for sequential SFT and RL. Extensive experiments on general STEM and mathematical benchmarks demonstrate that our decoupled curriculum training significantly outperforms SFT-only, RL-only, and random-split baselines. Our work provides a systematic study of the interplay between SFT and RL for general reasoning, offering a highly effective and generalized post-training recipe.

---

## 论文详细总结（自动生成）

这篇论文《DeReason: A Difficulty-Aware Curriculum Improves Decoupled SFT-then-RL Training for General Reasoning》探讨了在大语言模型（LLM）后训练阶段，如何通过优化监督微调（SFT）与强化学习（RL）之间的数据分配来提升通用 STEM 领域的推理能力。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在通用 STEM 领域（非纯数学/代码），SFT 和 RL 应该如何协同工作？具体而言，有限的训练数据应如何在 SFT 和 RL 两个阶段之间进行最优分配？
*   **研究动机**：
    *   虽然 RLVR（具有可验证奖励的强化学习）在数学领域取得巨大成功，但直接在基座模型上应用 RL 存在**样本效率极低**的问题。
    *   实验发现，在通用领域，使用中等质量数据进行 SFT 的效果往往优于直接 RL。
    *   SFT 擅长知识获取，而 RL 擅长在复杂路径上进行探索。目前缺乏一种系统性的策略来解耦这两者的训练数据。

### 2. 方法论（DeReason 框架）
DeReason 提出了一种**基于难度的课程学习策略**，将数据按“推理强度”解耦：
*   **难度评估（Difficulty Estimation）**：使用一个与策略模型同规模的指令微调模型（如 Qwen3-4B-Instruct）作为裁判，对所有训练题目进行 1-5 分的打分。
    *   1-2 分：侧重知识检索或简单推导。
    *   4-5 分：需要多步逻辑分析、复杂分解或抽象思维。
*   **数据分区（Data Partitioning）**：
    *   **SFT 子集**：分配低难度、覆盖面广的问题。利用教师模型的响应进行蒸馏，帮助模型建立广泛的领域知识基础。
    *   **RL 子集**：保留高难度（4-5 分）的问题。利用 GRPO 算法进行强化学习，鼓励模型在没有教师示范的情况下探索复杂的推理路径。
*   **课程训练流程**：先在 SFT 子集上训练得到初始模型，再在 RL 子集上进行强化学习。

### 3. 实验设计
*   **基座模型**：Qwen3-4B-Base。
*   **训练数据集**：WebInstruct-Verified 和 Webscale-RL（均专注于 STEM 领域）。
*   **评估基准（Benchmarks）**：
    *   **通用推理**：MMLU-Pro、GPQA-Diamond、SuperGPQA、BBEH（BIG-Bench Extra Hard）。
    *   **数学推理**：AIME24、AIME25、MATH500。
*   **对比方法**：
    *   Pure SFT（全量数据微调）。
    *   Pure RL（全量数据强化学习）。
    *   Random Split（随机划分数据进行 SFT-then-RL）。
    *   SFT-then-Selected SFT（两阶段均为 SFT）。

### 4. 资源与算力
*   **框架工具**：SFT 使用 **Llama-Factory**，RL 使用 **VeRL** 框架。
*   **超参数**：SFT 学习率 1e-5，RL 学习率 1e-6，最大响应长度 8192，Batch Size 128。
*   **算力说明**：文中**未明确列出**具体的 GPU 型号（如 H100/A100）数量及总训练时长，但提到计算资源由瑞士 AI 倡议（Swiss AI Initiative）提供。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个大型 STEM 数据集上分别验证了方法，并在 4 个通用推理基准和 3 个数学基准上进行了测试。
*   **消融实验**：包括不同难度分布的数据占比分析、RL 阶段不同初始检查点（Base vs SFT）的对比、响应长度演变分析以及策略熵（Entropy）的变化观察。
*   **充分性评价**：实验设计较为全面，通过受控变量（控制数据总量一致）对比了 SFT 和 RL 的效率，证明了“易题 SFT + 难题 RL”的优越性，实验结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **SFT 的不可替代性**：对于小规模基座模型，SFT 是必不可少的冷启动机制，其样本效率远高于纯 RL。
*   **解耦优势**：DeReason 显著优于随机划分数据的序列训练。在难题（如 BBEH）上提升尤为明显。
*   **行为差异**：
    *   **长度**：RL 在 SFT 模型基础上起到“压缩”作用，缩短冗余响应；而在基座模型上则表现为“分化”，正确答案变长，错误答案变短。
    *   **熵**：RL 从基座模型开始会迅速收敛到极低熵（确定性高），而从 SFT 开始则下降较平缓。
*   **领域分布**：高难度题目高度集中在数学和物理领域，而低难度题目分布较均匀。

### 7. 优点
*   **策略简单有效**：无需修改 RL 算法架构，仅通过数据层面的优化即可获得显著增益，具有很强的工程实用性。
*   **正交性强**：该方法与现有的 RL 算法改进（如 GRPO 的变体）互补，可以轻松集成到现有管线中。
*   **深入的分析**：对 SFT 和 RL 在后训练中扮演的角色（知识获取 vs 推理压缩）提供了深刻的见解。

### 8. 不足与局限
*   **模型规模限制**：实验主要集中在 4B 参数规模的模型上，尚未在大规模模型（如 70B+）上验证该结论的普适性。
*   **验证器依赖**：在通用 STEM 领域，仍然依赖于模型基验证器（Model-based Verifier）来提供奖励信号，验证器的准确性可能成为性能瓶颈。
*   **难度评估成本**：需要额外的 LLM 评分步骤，虽然使用了小模型，但在处理超大规模数据集时仍有一定计算开销。

（完）
