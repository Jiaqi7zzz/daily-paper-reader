---
title: Improving Search Agent with One Line of Code
title_zh: 只需一行代码改进搜索智能体
authors: "Jian Li, Dongsheng Chen, Zhenhua Xu, Yizhang Jin, Jiafu Wu, Chengjie Wang, Xiaotong Yuan, Yabiao Wang"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.10069v1"
tags: ["query:rl-tl"]
score: 9.0
evidence: 基于工具的代理强化学习（TARL）用于搜索代理
tldr: 本研究针对工具型强化学习（TARL）中搜索智能体训练不稳定的问题，识别出 GRPO 算法中存在的重要性采样分布偏移（ISDD）现象。为此，作者提出了 SAPO 优化方法，通过引入条件式 token 级 KL 约束，在不阻断梯度流的前提下防止分布过度偏移。该方法实现极简，仅需一行代码修改，在多个 QA 基准测试中显著提升了 Search-R1 的性能，展现了极强的通用性和部署便捷性。
selection_source: fresh_fetch
motivation: 解决工具型强化学习中 GRPO 算法因重要性采样分布偏移（ISDD）导致的训练不稳定和模型崩溃问题。
method: 提出 SAPO 算法，通过在低概率正向 token 上引入条件式 token 级 KL 约束，仅需一行代码修改即可稳定训练。
result: "在 7 个 QA 基准测试中，SAPO 相比 Search-R1 实现了 10.6% 的绝对提升，并在不同规模和系列的语言模型上表现一致。"
conclusion: SAPO 提供了一种极简且高效的方案，有效解决了搜索智能体训练中的分布偏移问题，显著增强了模型的搜索与问答能力。
---

## 摘要
基于工具的智能体强化学习 (Tool-based Agentic Reinforcement Learning, TARL) 已成为训练搜索智能体自主与外部工具交互以进行多轮信息检索过程的一种极具前景的范式。然而，我们发现了一个导致灾难性模型崩溃的关键训练不稳定性：重要性采样分布偏移 (Importance Sampling Distribution Drift, ISDD)。在广泛采用的 TARL 算法——群体相对策略优化 (Group Relative Policy Optimization, GRPO) 中，ISDD 表现为重要性采样比率的急剧下降，这使得梯度更新失效并引发不可逆的训练失败。为了解决这一问题，我们提出了搜索智能体策略优化 (Search Agent Policy Optimization, SAPO)，通过条件性的 token 级 KL 约束来稳定训练。与忽略分布差异的硬截断 (hard clipping) 不同，SAPO 选择性地惩罚当前策略与旧策略之间的 KL 散度。关键在于，这种惩罚仅应用于策略发生过度偏移且概率较低的正向 token，从而在防止分布偏移的同时保留梯度流。值得注意的是，SAPO 仅需对标准 GRPO 进行一行代码修改，确保了极高的即时部署性。在七个问答 (QA) 基准测试上的广泛实验表明，SAPO 相比 Search-R1 实现了 +10.6% 的绝对提升（+31.5% 的相对提升），并在不同模型规模（1.5B、14B）和系列（Qwen、LLaMA）中均获得了一致的增益。

## 速览
**TLDR**：本研究针对工具型强化学习（TARL）中搜索智能体训练不稳定的问题，识别出 GRPO 算法中的重要性采样分布偏移（ISDD）是导致模型崩溃的关键。为此，作者提出了 SAPO 算法，通过引入条件式 token 级 KL 约束来抑制分布偏移，且该方法仅需一行代码修改。实验表明，SAPO 在多个 QA 基准上显著优于 Search-R1，在不同规模的模型上均表现出极强的鲁棒性和性能提升。 \
**Motivation**：解决工具型强化学习中 GRPO 算法因重要性采样分布偏移（ISDD）导致的训练不稳定和模型崩溃问题。 \
**Method**：提出 SAPO 算法，通过在低概率正向 token 上引入条件式 token 级 KL 约束，仅需一行代码修改即可稳定训练。 \
**Result**：在 7 个 QA 基准测试中，SAPO 相比 Search-R1 实现了 10.6% 的绝对提升，并在 1.5B 和 14B 模型上均取得显著增益。 \
**Conclusion**：SAPO 证明了通过极简的算法改进即可有效解决搜索智能体的训练漂移问题，显著增强了自主信息寻求能力。

---

## Abstract
Tool-based Agentic Reinforcement Learning (TARL) has emerged as a promising paradigm for training search agents to interact with external tools for a multi-turn information-seeking process autonomously. However, we identify a critical training instability that leads to catastrophic model collapse: Importance Sampling Distribution Drift(ISDD). In Group Relative Policy Optimization(GRPO), a widely adopted TARL algorithm, ISDD manifests as a precipitous decline in the importance sampling ratios, which nullifies gradient updates and triggers irreversible training failure. To address this, we propose \textbf{S}earch \textbf{A}gent \textbf{P}olicy \textbf{O}ptimization (\textbf{SAPO}), which stabilizes training via a conditional token-level KL constraint. Unlike hard clipping, which ignores distributional divergence, SAPO selectively penalizes the KL divergence between the current and old policies. Crucially, this penalty is applied only to positive tokens with low probabilities where the policy has shifted excessively, thereby preventing distribution drift while preserving gradient flow. Remarkably, SAPO requires only one-line code modification to standard GRPO, ensuring immediate deployability. Extensive experiments across seven QA benchmarks demonstrate that SAPO achieves \textbf{+10.6\% absolute improvement} (+31.5\% relative) over Search-R1, yielding consistent gains across varying model scales (1.5B, 14B) and families (Qwen, LLaMA).

---

## 论文详细总结（自动生成）

这篇论文由南京大学和腾讯优图实验室的研究团队合作完成，旨在解决搜索智能体在强化学习训练过程中的稳定性问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：基于工具的智能体强化学习（TARL）是训练搜索智能体自主进行多轮信息检索的主流范式。目前广泛采用 GRPO（群体相对策略优化）算法进行后训练。
*   **核心问题**：研究者发现搜索智能体在训练中极易出现“重要性采样分布偏移”（ISDD）现象。当新策略与旧策略偏差过大时，重要性采样比率会骤降至接近零，导致梯度消失，引发不可逆的模型崩溃。
*   **研究动机**：在多轮搜索中，动作 token（如 `<search>`）对策略偏移极其敏感。如果模型在某个关键步骤抑制了正确动作，由于梯度消失，模型将无法通过后续更新恢复该动作。

### 2. 方法论
*   **核心思想**：提出 **SAPO（Search Agent Policy Optimization）**，通过引入一个“条件式 token 级 KL 约束”来替代或补充传统的硬截断（Hard Clipping）机制。
*   **关键技术细节**：
    *   **条件触发**：该 KL 惩罚项并非无差别应用，而是仅在满足两个条件时触发：1. 该 token 的优势值（Advantage）为正；2. 重要性采样比率 $r_t$ 低于设定阈值 $\tau$（默认 1.0）。
    *   **软约束机制**：不同于 PPO 的硬截断会直接清零梯度，SAPO 通过对数比率（log-ratio）进行惩罚，在防止分布过度偏移的同时，允许梯度继续流动，使模型能够从低概率的正向样本中学习。
*   **算法实现**：该方法在代码实现上极其简单，仅需在 GRPO 的损失函数计算中增加一行关于条件 KL 惩罚的逻辑即可。

### 3. 实验设计
*   **数据集**：涵盖 7 个主流问答基准测试。
    *   **单跳问答**：NQ, TriviaQA, PopQA。
    *   **多跳问答**：HotpotQA, 2WikiMultihopQA, Musique, Bamboogle。
*   **对比方法（Baselines）**：
    *   **无检索方法**：直接生成、SFT、R1-Base/Instruct。
    *   **检索工作流**：Naive RAG, IRCoT。
    *   **搜索智能体**：Search-R1（主要基准）、Search-o1、ReSearch、AutoRefine、CriticSearch 等十余种最新方法。
*   **评估指标**：准确率（Exact Match, EM）和 F1 分数。

### 4. 资源与算力
*   **算力说明**：论文**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **实现框架**：使用了 VeRL 强化学习框架。
*   **模型规模**：实验涵盖了从 1.5B 到 14B 不同参数规模的模型，暗示了实验过程中消耗了相当规模的计算资源。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 7 个数据集上进行了全面测试。
    *   验证了 Qwen2.5（1.5B, 3B, 7B, 14B）和 LLaMA-3.2（3B）两个模型系列的泛化性。
    *   进行了消融实验，对比了无 KL、无条件 KL、仅比例条件 KL 以及 SAPO 完整方案的效果。
    *   进行了超参数 $\tau$ 的敏感性分析。
*   **充分性评价**：实验设计非常充分且客观。通过跨模型系列、跨规模以及与大量 2025/2026 年最新研究成果的对比，有力证明了 SAPO 的有效性和鲁棒性。

### 6. 主要结论与发现
*   **性能飞跃**：SAPO 相比 Search-R1 实现了 **10.6% 的绝对 EM 提升**（相对提升 31.5%）。
*   **复杂任务优势**：在多跳问答（Multi-hop QA）任务上表现尤为突出，平均提升达 14.7%。
*   **稳定性验证**：实验观察到 SAPO 有效抑制了训练后期的熵崩溃和奖励下降现象，解决了 ISDD 导致的模型崩溃。
*   **扩展性**：符合 Scaling Laws，模型规模越大，SAPO 带来的性能增益越显著。

### 7. 优点
*   **极简部署**：仅需一行代码修改，无需引入复杂的辅助网络（如 Critic 网络）。
*   **理论与实证结合**：深入分析了搜索智能体训练不稳定的数学本质（ISDD），并给出了针对性的解决方案。
*   **通用性强**：不依赖特定的模型架构，在 Base 和 Instruct 模型上均表现良好。

### 8. 不足与局限
*   **奖励函数单一**：目前主要依赖基于规则的 F1 分数作为奖励，对于开放式或更复杂的智能体任务，可能需要更精细的奖励建模。
*   **检索器固定**：实验中使用了固定的检索器（E5-base-v2），未探讨智能体与检索器联合训练的可能性。
*   **算力细节缺失**：缺乏具体的训练资源消耗数据，不便于其他研究者评估复现成本。

（完）
