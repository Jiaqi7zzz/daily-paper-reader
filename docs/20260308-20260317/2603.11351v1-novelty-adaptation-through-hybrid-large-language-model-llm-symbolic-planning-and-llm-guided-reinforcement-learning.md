---
title: Novelty Adaptation Through Hybrid Large Language Model (LLM)-Symbolic Planning and LLM-guided Reinforcement Learning
title_zh: 通过混合大语言模型（LLM）-符号规划与LLM引导的强化学习实现新颖性适应
authors: "Hong Lu, Pierrick Lorang, Timothy R. Duggan, Jivko Sinapov, Matthias Scheutz"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.11351v1"
tags: ["query:rl-tl"]
score: 9.0
evidence: LLM编写奖励函数以引导针对新物体的强化学习
tldr: 针对开放世界中自主智能体难以应对新颖事物的问题，本文提出一种结合大语言模型（LLM）、符号规划与强化学习（RL）的神经符号架构。利用LLM的常识推理识别缺失算子，通过符号规划生成方案，并由LLM编写奖励函数引导RL学习新算子的控制策略。该方法在连续机器人领域的算子发现与学习任务中表现优于现有技术，显著提升了智能体的环境适应性。
selection_source: fresh_fetch
motivation: 传统符号规划器在面对缺乏相应操作算子的新颖环境对象时，往往无法生成有效的执行计划。
method: 提出一种集成LLM、符号规划与强化学习的架构，利用LLM识别缺失算子并生成RL奖励函数以学习控制策略。
result: 实验证明该方法在连续机器人领域的算子发现和算子学习效率上均超越了当前最先进的方法。
conclusion: 结合LLM的常识推理与符号规划、强化学习，能有效提升自主智能体在动态开放环境中的新颖性适应能力。
---

## 摘要
在动态的开放世界环境中，自主智能体经常会遇到阻碍其寻找实现目标规划的新颖性。具体而言，当机器人的规划领域缺乏使其能够与环境中新颖物体进行适当交互的算子时，传统的符号规划器将无法生成规划。我们提出了一种神经符号架构，该架构集成了符号规划、强化学习和大语言模型（LLM），以学习如何处理新颖物体。特别地，我们利用 LLM 的常识推理能力来识别缺失的算子，利用符号 AI 规划器生成规划，并编写奖励函数以引导强化学习智能体学习新识别算子的控制策略。我们的方法在连续机器人领域的算子发现和算子学习方面均优于现有最先进的方法。

## Abstract
In dynamic open-world environments, autonomous agents often encounter novelties that hinder their ability to find plans to achieve their goals. Specifically, traditional symbolic planners fail to generate plans when the robot's planning domain lacks the operators that enable it to interact appropriately with novel objects in the environment. We propose a neuro-symbolic architecture that integrates symbolic planning, reinforcement learning, and a large language model (LLM) to learn how to handle novel objects. In particular, we leverage the common sense reasoning capability of the LLM to identify missing operators, generate plans with the symbolic AI planner, and write reward functions to guide the reinforcement learning agent in learning control policies for newly identified operators. Our method outperforms the state-of-the-art methods in operator discovery as well as operator learning in continuous robotic domains.

---

## 论文详细总结（自动生成）

这篇论文提出了一种结合大语言模型（LLM）、符号规划与强化学习（RL）的神经符号架构，旨在解决自主机器人在开放世界中遇到新颖物体（Novelty）时，因缺乏相应操作算子而导致规划失败的问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的任务与运动规划（TAMP）依赖于预定义的符号算子（如 PDDL 定义）。当环境中出现新物体（如一个从未见过的抽屉或盖子）时，机器人因缺乏与之交互的算子而无法达成目标。
*   **研究背景**：在动态开放世界中，机器人必须具备“新颖性适应”能力。现有的算子发现方法通常依赖随机探索，在连续动作空间中效率极低。
*   **整体含义**：本文试图利用 LLM 的常识推理能力来“填补”符号规划中的知识空白，并利用 LLM 生成的代码来引导底层强化学习，实现从高层逻辑推理到底层动作控制的闭环。

### 2. 方法论
论文提出了 **“规划-学习-执行”（Plan-Learn-Execute）** 循环架构，包含两个关键技术环节：

*   **混合 LLM-符号规划（Search-and-Prompt）**：
    *   当符号规划器报错（找不到路径）时，系统将当前状态、目标和新物体信息输入 LLM（GPT-o3）。
    *   LLM 负责定义缺失算子的 PDDL 结构（前提条件、参数、效果）。
    *   符号规划器验证这些新算子是否能打通通往目标的路径。采用“自我一致性”（Self-consistency）技术，采样多个候选算子并取多数。
*   **LLM 引导的子目标强化学习**：
    *   **奖励函数生成**：利用 LLM（GPT-o4-mini）为新算子的每个子目标编写密集的奖励函数代码（Python）。
    *   **遗传消除策略**：为每个子目标启动多个 RL 智能体（每个对应不同的 LLM 奖励函数候选），定期评估性能并剔除表现最差的候选者，以降低 LLM 生成错误代码的风险。
    *   **课程学习**：将算子的效果分解为有序的子目标序列，逐阶段训练 PPO 智能体。

### 3. 实验设计
*   **实验场景**：基于 `mimicgen` 仿真环境，设计了四个难度递增的连续机器人操纵任务：
    1.  **厨房（简单）**：新物体是锅盖，阻碍了取物。
    2.  **螺母组装（中等）**：新物体是圆柱销，需学习新的拾取技能。
    3.  **咖啡机-盒子（中等）**：咖啡荚在盒子内，需学习从容器中取物。
    4.  **咖啡机-抽屉（困难）**：咖啡荚在关闭的抽屉里，需先开抽屉再取物。
*   **Benchmark 与对比方法**：
    *   **算子发现（OD）**：基于随机探索的传统混合规划方法。
    *   **LEAGUE-Sparse**：仅在完成所有效果时给予稀疏奖励的基准。
    *   **奖励机（Reward Machine, RM）**：仅提供子目标奖金（无 LLM 密集奖励塑造）的基准。

### 4. 资源与算力
*   **硬件环境**：Ubuntu 20.04 系统，配备 24 核 Intel Core i9-12900K CPU，以及 **1 张 NVIDIA GeForce RTX 4090 GPU**。
*   **训练时长**：论文未明确给出总训练小时数，但提到算子发现（OD）方法在复杂任务中超过 7 小时仍无法找到解，而本文方法在几百秒内即可完成算子识别。RL 训练部分，每个智能体上限为 100 万个时间步（Timesteps）。

### 5. 实验数量与充分性
*   **实验规模**：每个领域运行了 **10 个随机种子**。
*   **充分性评价**：
    *   **覆盖度**：涵盖了从简单到极难的四个梯度，实验设计较为合理。
    *   **统计显著性**：使用了 Wilcoxon 符号秩检验（p < 0.05）来验证性能提升，结果具有统计学意义。
    *   **消融/对比**：对比了稀疏奖励和仅有子目标奖励的情况，证明了 LLM 生成的密集奖励函数的必要性。

### 6. 主要结论与发现
*   **算子识别效率**：LLM 能够 100% 准确识别简单到中等难度的缺失算子，在最难的抽屉任务中准确率也达到 70%，远超传统随机探索方法。
*   **学习性能**：LLM 引导的 RL 智能体在所有任务中的成功率均超过 90%，而基准方法在复杂任务（如开抽屉）中几乎完全失败。
*   **鲁棒性**：通过并行运行多个奖励函数候选并进行剔除，有效解决了 LLM 偶尔生成低质量代码的问题。

### 7. 优点
*   **神经符号融合**：巧妙地将 LLM 的常识（解决“做什么”）与符号规划的严谨性（解决“逻辑一致性”）以及 RL 的灵活性（解决“怎么做”）结合。
*   **自动化程度高**：实现了从发现问题到编写奖励函数再到训练策略的全自动化流程。
*   **解决冷启动问题**：在连续空间中，随机探索很难触发复杂的交互（如开抽屉），LLM 提供的先验知识极大地加速了 RL 的收敛。

### 8. 不足与局限
*   **感知假设**：实验假设机器人已经拥有能够检测新物体状态（如 `open(drawer)`）的谓词检测器，但在现实中，新物体的视觉感知和谓词映射本身就是一个巨大挑战。
*   **仿真局限**：所有实验均在仿真环境中完成，尚未在物理机器人上验证 Sim-to-Real 的迁移效果。
*   **LLM 成本与延迟**：虽然比随机探索快，但多次调用 GPT-o3 等高级模型会产生一定的 API 成本和推理延迟。
*   **单一新颖性**：目前主要针对单一新物体的引入，对于多个物体同时出现或环境规则发生根本改变的情况，处理能力尚待验证。

（完）
