---
title: "Evaluate-as-Action: Self-Evaluated Process Rewards for Retrieval-Augmented Agents"
title_zh: Evaluate-as-Action：检索增强智能体的自评估过程奖励
authors: "Jiangming Shu, Yuxiang Zhang, Ye Ma, Xueyuan Lin, Jitao Sang"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09203v2"
tags: ["query:rl-tl"]
score: 7.0
evidence: 检索增强智能体的过程奖励
tldr: 针对检索增强智能体在多步推理中因噪声检索和奖励信号粗糙导致的可靠性问题，本文提出EvalAct框架。该框架将检索质量评估转化为显式动作，通过“搜索-评估”协议为每步检索提供即时过程信号。配合基于GRPO的PCAR优化方法，根据评估分数在片段级别调整优势函数，显著提升了多跳问答等复杂任务的性能与可靠性。
selection_source: fresh_fetch
motivation: 现有的检索增强智能体在多步推理中容易受噪声干扰，且仅靠结果奖励（Outcome-only RL）难以有效优化中间推理步骤。
method: 提出EvalAct框架，将检索评估设为显式动作，并引入PCAR优化方法，利用过程评估信号对不同轨迹片段的优势函数进行校准。
result: 在七个开放域问答基准测试中，EvalAct 取得了最佳平均准确率，尤其在多跳问答任务上表现出显著的性能增益。
conclusion: 通过将检索评估显式化并结合过程校准的强化学习，可以显著增强检索增强智能体在处理复杂推理任务时的鲁棒性。
---

## 摘要
检索增强智能体可以查询外部证据，但其在多步推理中的可靠性仍然有限：噪声检索可能会干扰多跳问答，而仅基于结果的强化学习提供的信用分配信号过于粗糙，难以优化中间步骤。我们提出了 EvalAct (Evaluate-as-Action)，它将隐式的检索质量评估转换为显式动作，并强制执行一种耦合的“搜索-评估”协议，使得每次检索后立即跟随一个结构化的评估分数，从而产生与交互轨迹对齐的过程信号。为了利用这些信号，我们引入了过程校准优势重缩放 (Process-Calibrated Advantage Rescaling, PCAR)，这是一种基于 GRPO 的优化方法，它根据评估分数在片段级别对优势进行重缩放，在强调可靠片段的同时保守地更新不确定的片段。在七个开放域问答基准测试上的实验表明，EvalAct 实现了最佳的平均准确率，在多跳任务上的提升最为显著；消融实验证实，显式评估循环是主要的改进驱动力，而 PCAR 提供了持续的额外收益。

## 速览
**TLDR**：针对检索增强智能体在多步推理中因噪声检索和奖励信号粗糙导致的可靠性问题，本文提出EvalAct框架。该框架将检索质量评估转化为显式动作，通过“搜索-评估”协议为每步检索提供即时过程信号。配合基于GRPO的PCAR优化方法，根据评估分数在片段级别调整优势函数，显著提升了多跳问答等复杂任务的性能与可靠性。 \
**Motivation**：现有的检索增强智能体在多步推理中容易受噪声干扰，且仅靠结果奖励（Outcome-only RL）难以有效优化中间推理步骤。 \
**Method**：提出EvalAct框架，将检索评估设为显式动作，并引入PCAR优化方法，利用过程评估信号对不同轨迹片段的优势函数进行校准。 \
**Result**：在七个开放域问答基准测试中，EvalAct 取得了最佳平均准确率，尤其在多跳问答任务上表现出显著的性能增益。 \
**Conclusion**：通过将检索评估显式化并结合过程校准的强化学习，可以显著增强检索增强智能体在处理复杂推理任务时的鲁棒性。

---

## Abstract
Retrieval-augmented agents can query external evidence, yet their reliability in multi-step reasoning remains limited: noisy retrieval may derail multi-hop question answering, while outcome-only reinforcement learning provides credit signals that are too coarse to optimize intermediate steps. We propose \textsc{EvalAct} (Evaluate-as-Action), which converts implicit retrieval quality assessment into an explicit action and enforces a coupled Search-to-Evaluate protocol so that each retrieval is immediately followed by a structured evaluation score, yielding process signals aligned with the interaction trajectory. To leverage these signals, we introduce Process-Calibrated Advantage Rescaling (PCAR), a GRPO-based optimization method that rescales advantages at the segment level according to evaluation scores, emphasizing reliable segments while updating uncertain ones conservatively. Experiments on seven open-domain QA benchmarks show that \textsc{EvalAct} achieves the best average accuracy, with the largest gains on multi-hop tasks, and ablations verify that the explicit evaluation loop drives the primary improvements while PCAR provides consistent additional benefits.

---

## 论文详细总结（自动生成）

这是一份关于论文《Evaluate-as-Action: Self-Evaluated Process Rewards for Retrieval-Augmented Agents》的深度结构化总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：检索增强生成（RAG）智能体在处理复杂的多步推理（如多跳问答）时面临两大瓶颈：
    1.  **错误传播**：一次无关的检索可能导致后续推理彻底偏离轨道，且缺乏即时的验证机制。
    2.  **信用分配困难**：现有的强化学习（RL）方法（如 GRPO）通常只依赖最终结果的正确性（Outcome Reward），这种粗粒度的信号无法区分长轨迹中哪些中间检索步骤是有益的，哪些是冗余或误导的。
*   **核心问题**：如何为检索增强智能体提供更细粒度的过程监督，以提高其在长程交互中的可靠性和学习效率？

### 2. 论文提出的方法论
论文提出了 **EvalAct** 框架，核心思想是将“评估”从隐式的内部思考转变为显式的、可优化的“动作”。

*   **EvalAct (Evaluate-as-Action)**：
    *   **耦合协议**：强制执行 `Search → Evaluate` 协议。智能体在执行每次 `Search` 动作后，必须立即执行一个 `Evaluate` 动作。
    *   **显式反馈**：`Evaluate` 动作输出一个结构化的置信度分数 $z \in [0, 10]$。环境根据该分数返回离散的控制指令（如：分数低则要求重新搜索，分数高则建议开始生成答案）。
*   **PCAR (过程校准优势重缩放)**：
    *   **核心逻辑**：在 GRPO 算法的基础上，利用智能体自产的评估分数对优势函数（Advantage）进行片段级（Segment-level）调整。
    *   **公式实现**：首先对轨迹内的分数进行标准化处理得到 $\tilde{z}_{i,k}$，然后计算校准后的优势信号：
        $$\hat{A}_{i,t} = A_i \cdot \text{clamp}(1 + \lambda_{i,k} \tilde{z}_{i,k}, \delta, \infty)$$
    *   **作用**：放大可靠步骤的梯度更新，同时保守地更新不确定或低质量的步骤，从而实现细粒度的信用分配。

### 3. 实验设计
*   **数据集**：涵盖 7 个开放域问答基准测试。
    *   **单跳任务**：Natural Questions (NQ), TriviaQA, PopQA。
    *   **多跳任务**：HotpotQA, 2Wiki-MultihopQA, MuSiQue, Bamboogle。
*   **对比方法（Baselines）**：
    *   基础方法：直接生成（Direct Generation）、朴素 RAG（Naïve RAG）。
    *   提示词/智能体方法：IRCoT（交替检索与推理）。
    *   RL/搜索增强方法：Search-o1, Search-R1, AutoRefine（迭代精炼）。
*   **评估指标**：主要使用精确匹配（Exact Match, EM）。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA A100 GPU**。
*   **训练细节**：采用全参数优化（Full-parameter optimization）和梯度检查点（Gradient Checkpointing）。
*   **模型规模**：基于 Qwen2.5-3B-Instruct 和 Qwen2.5-7B-Instruct 两个版本进行实验。
*   **训练时长**：文中未明确给出总训练小时数，但提到 RL 阶段进行了 2 个 Epoch，全局 Batch Size 为 256。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 7 个数据集上进行了主实验，验证了不同模型规模（3B/7B）下的泛化性。
    *   **模型消融实验**：对比了 SFT 预热、Vanilla RL 与 EvalAct 的差异，证明了格式对齐的重要性。
    *   **方法消融实验**：分别拆解了“评估循环（Eval Loop）”和“PCAR 优化”的贡献。
    *   **超参数敏感性分析**：测试了 PCAR 不同缩放强度（Low/Mid/High）的影响。
*   **客观性与公平性**：所有检索增强方法使用相同的检索环境（Wikipedia 2018 镜像、BM25 检索器、Top-k=3），确保了对比的公平性。

### 6. 论文的主要结论与发现
*   **性能领先**：EvalAct 在所有基准测试中取得了最佳平均成绩，3B 模型平均 EM 达到 44.0%，7B 模型达到 47.1%。
*   **多跳任务增益显著**：在需要多次迭代证据收集的任务（如 2Wiki, Bamboogle）上，EvalAct 相比最强基线有显著提升（最高达 13.6 个百分点）。
*   **结构胜过算法**：消融实验显示，**显式的评估循环结构**是性能提升的核心驱动力（贡献了约 7.5% 的提升），而 PCAR 算法提供了额外的稳定性和增益（约 1.2%）。
*   **SFT 的必要性**：直接进行 EvalAct RL 效果较差，必须先通过少量（2k 条）SFT 数据让模型学会“搜索-评估”的格式规范。

### 7. 优点与亮点
*   **创新性地将隐式行为显式化**：将难以监督的“检索质量判断”转化为可观测、可奖励的“动作”，解决了 RAG 智能体黑盒推理的问题。
*   **无需外部奖励模型**：PCAR 利用智能体自身的置信度进行校准，避免了训练昂贵的外部过程奖励模型（PRM）。
*   **推理可解释性**：显式的评估分数和文本评估为用户提供了智能体决策过程的透明度。

### 8. 不足与局限
*   **对格式对齐的依赖**：模型必须严格遵守 `Search → Evaluate` 协议，如果 SFT 阶段未对齐好，RL 阶段容易崩溃。
*   **奖励黑客风险（Reward Hacking）**：虽然 PCAR 旨在激励校准，但理论上智能体仍可能通过操纵置信度分数来获取更高的优势值（尽管实验中未明显观察到）。
*   **单跳任务优势不明显**：在简单的单跳任务上，EvalAct 的表现与 AutoRefine 等基线持平甚至略逊，说明该框架的复杂度在简单任务中可能收益递减。
*   **检索器固定**：实验仅使用了 BM25，未探讨在更先进的向量检索或重排序环境下的表现。

（完）
