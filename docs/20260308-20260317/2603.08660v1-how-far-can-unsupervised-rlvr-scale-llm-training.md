---
title: How Far Can Unsupervised RLVR Scale LLM Training?
title_zh: 无监督可验证奖励强化学习（URLVR）能在多大程度上扩展大语言模型（LLM）训练？
authors: "Bingxiang He, Yuxin Zuo, Zeyuan Liu, Shangziqi Zhao, Zixuan Fu, Junlin Yang, Cheng Qian, Kaiyan Zhang, Yuchen Fan, Ganqu Cui, Xiusi Chen, Youbang Sun, Xingtai Lv, Xuekai Zhu, Li Sheng, Ran Li, Huan-ang Gao, Yuchen Zhang, Bowen Zhou, Zhiyuan Liu, Ning Ding"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.08660v1"
tags: ["query:rl-tl"]
score: 10.0
evidence: 具有可验证奖励的无监督强化学习 (URLVR)
tldr: 本研究深入探讨了无监督可验证奖励强化学习（URLVR）在扩展大语言模型训练中的潜力与局限。通过建立统一理论框架，揭示了内在奖励方法本质上是强化模型初始分布，其效果高度依赖于初始置信度与正确性的对齐。实验表明，内在奖励在训练中普遍呈现“先升后降”的模式，且崩溃时机受模型先验限制。研究提出了衡量RL训练能力的指标，并指出基于计算不对称性的外部奖励是未来突破瓶颈的关键方向。
selection_source: fresh_fetch
motivation: 旨在探索如何利用无监督强化学习突破大语言模型训练中对高质量人工标注数据的依赖瓶颈。
method: 通过对URLVR进行分类并建立统一理论框架，系统分析了内在与外部奖励机制在模型分布收敛中的作用。
result: 实验证明内在奖励方法的效果受限于模型初始置信度与正确性的对齐程度，且普遍存在性能最终崩溃的现象。
conclusion: 内在奖励适用于小规模测试时训练，但要实现大规模持续扩展，需转向基于计算不对称性的外部验证方法。
---

## 摘要
无监督可验证奖励强化学习（URLVR）通过在没有地面真值标签的情况下推导奖励，为突破监督瓶颈、扩展大语言模型（LLM）训练提供了一条路径。近期的研究利用模型内在信号，展示了令人鼓舞的早期收益，但其潜力和局限性仍不明确。在这项工作中，我们重新审视了 URLVR，并提供了涵盖分类学、理论和广泛实验的全面分析。我们首先根据奖励来源将 URLVR 方法分为内在和外在两类，然后建立了一个统一的理论框架，揭示了所有内在方法都趋向于使模型的初始分布变得尖锐。当初始置信度与正确性一致时，这种尖锐化机制会取得成功；但当两者不一致时，则会导致灾难性的失败。通过系统性的实验，我们展示了内在奖励在不同方法中一致遵循“先升后降”的模式，且崩溃的时机由模型先验而非工程选择决定。尽管存在这些扩展限制，我们发现内在奖励在小数据集的测试时训练中仍然具有价值，并提出了“模型崩溃步数”（Model Collapse Step）来衡量模型先验，作为强化学习可训练性的实用指标。最后，我们探索了基于计算不对称性进行验证的外在奖励方法，初步证据表明它们可能突破置信度-正确性的上限。我们的发现划定了内在 URLVR 的边界，同时为寻求可扩展的替代方案提供了动力。

## 速览
**TLDR**：本研究深入探讨了无监督可验证奖励强化学习（URLVR）在扩展大语言模型训练中的潜力与局限。通过将URLVR分为内在和外在奖励，建立统一理论框架揭示了内在奖励本质上是锐化初始分布，其效果高度依赖于模型初始置信度与正确性的对齐。实验表明内在奖励普遍存在“先升后降”的模式，并提出了“模型崩溃步数”作为训练指标，最后指出了外在奖励是突破扩展瓶颈的潜在方向。 \
**Motivation**：旨在解决大语言模型训练中监督数据匮乏的瓶颈，探索无需人工标注的无监督强化学习的扩展边界与失效机制。 \
**Method**：提出了URLVR分类法和统一理论框架，通过系统实验分析内在奖励的锐化机制及其与模型先验分布的关系。 \
**Result**：发现内在奖励训练呈现先升后降的规律，崩溃时机由模型先验决定，并验证了其在小规模测试时训练中的价值。 \
**Conclusion**：内在URLVR受限于模型初始置信度与正确性的对齐程度，而基于计算不对称性的外在奖励是实现持续扩展的更有前景的路径。

---

## Abstract
Unsupervised reinforcement learning with verifiable rewards (URLVR) offers a pathway to scale LLM training beyond the supervision bottleneck by deriving rewards without ground truth labels. Recent works leverage model intrinsic signals, showing promising early gains, yet their potential and limitations remain unclear. In this work, we revisit URLVR and provide a comprehensive analysis spanning taxonomy, theory and extensive experiments. We first classify URLVR methods into intrinsic versus external based on reward sources, then establish a unified theoretical framework revealing that all intrinsic methods converge toward sharpening the model's initial distribution This sharpening mechanism succeeds when initial confidence aligns with correctness but fails catastrophically when misaligned. Through systematic experiments, we show intrinsic rewards consistently follow a rise-then-fall pattern across methods, with collapse timing determined by model prior rather than engineering choices. Despite these scaling limits, we find intrinsic rewards remain valuable in test-time training on small datasets, and propose Model Collapse Step to measure model prior, serving as a practical indicator for RL trainability. Finally, we explore external reward methods that ground verification in computational asymmetries, showing preliminary evidence they may escape the confidence-correctness ceiling. Our findings chart boundaries for intrinsic URLVR while motivating paths toward scalable alternatives.

---

## 论文详细总结（自动生成）

这篇论文对**无监督可验证奖励强化学习（URLVR）**进行了系统、深入且具有理论支撑的分析，探讨了在缺乏人工标注（Ground Truth）的情况下，如何通过模型自身信号或外部机制扩展大语言模型（LLM）的训练。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：虽然强化学习（RLVR）在提升模型推理能力（如 DeepSeek-R1）方面效果显著，但它极度依赖高质量的人工标注。随着模型能力接近或超越人类，获取正确答案的成本变得不可接受，形成了“监督瓶颈”。
*   **核心问题**：完全不依赖人工标注的“无监督强化学习”能走多远？现有的内在奖励方法（如多数投票、熵最小化）是否存在根本性的局限？

### 2. 方法论
论文将 URLVR 分为**内在奖励（Intrinsic）**和**外部奖励（External）**两大类，并提出了统一的理论框架：
*   **内在奖励方法**：
    *   **确定性基础（Certainty-Based）**：利用 Logits、熵或概率差距来衡量模型对输出的信心。
    *   **集成基础（Ensemble-Based）**：通过多次采样（Rollouts）进行多数投票（Majority Voting），假设一致性与正确性正相关。
*   **核心理论：尖锐化机制（Sharpening Mechanism）**：
    *   论文证明了所有内在奖励方法在数学本质上都在操纵模型分布与“锚点分布”之间的交叉熵。
    *   **定理 1**：证明了内在奖励训练会使模型分布几何级数地收敛于初始分布的“尖锐化”版本。这意味着训练只是在放大模型已有的偏好，而不会发现新知识。
*   **外部奖励方法**：利用**计算不对称性**（生成难但验证易），如代码执行结果、数学证明检查器（Lean）、或未标注文本中的潜在结构。

### 3. 实验设计
*   **数据集与场景**：
    *   **训练集**：DAPO-17k（数学）、MATH500、Countdown（倒计时数字游戏）。
    *   **评测基准（Benchmarks）**：AIME 2024/2025、AMC 2023。
*   **对比方法**：
    *   对比了 5 种内在奖励：多数投票（Majority Voting）、自我确定性（Self-Certainty）、Token 级熵、轨迹级熵、概率。
    *   对比了有监督 RL（Oracle Supervision）与外部奖励（Self-Verification）。
*   **模型家族**：涵盖 Qwen 系列（Qwen2.5, Qwen3）、Llama 系列（Llama-3.1, Tulu-3）、OLMo-2 等 11 个不同规模和阶段的模型。

### 4. 资源与算力
*   **框架与算法**：使用 **veRL** 分布式强化学习框架，采用 **GRPO** 算法。
*   **算力说明**：论文未明确给出总的 GPU 消耗小时数或具体的显卡总数（如 H100 数量），但提到在测量“模型崩溃步数”时，其方法比完整 RL 训练快 **5.6 倍**，且只需消耗约 1.19B Token 的计算量。

### 5. 实验数量与充分性
*   **实验规模**：非常充分。论文不仅做了宏观的训练曲线对比，还深入到了**单题级别（Per-problem）**的动态分析。
*   **消融与调优**：对温度（Temperature）、Mini-batch 大小、KL 散度系数、采样次数（Rollout N）等关键超参数进行了详尽的消融实验。
*   **客观性**：通过跨模型家族（Qwen vs Llama）的对比，揭示了架构对无监督 RL 稳定性的影响，实验设计公平且具有统计意义。

### 6. 主要结论与发现
*   **“先升后降”模式**：内在奖励训练普遍存在性能先提升后崩溃的现象。崩溃的时机取决于**模型先验**（初始置信度与正确性的对齐程度），而非工程技巧。
*   **崩溃模式多样化**：概率奖励导致“长度崩溃”（趋向极短输出）；熵奖励导致“重复崩溃”（通过重复高频词降低熵）。
*   **小数据集的安全性**：在样本量极小（如 ≤128）或测试时训练（Test-Time Training）中，内在奖励是安全的，因为它们只引起局部过拟合而非全局策略偏移。
*   **模型崩溃步数（Model Collapse Step）**：这是一个衡量模型 RL 训练潜力的有效指标，比传统的 Pass@k 更准确且无需标签。
*   **外部奖励的优越性**：基于自我验证（Self-Verification）的外部奖励能有效避免崩溃，是实现大规模扩展的更优路径。

### 7. 优点
*   **理论与实验结合**：不仅观察到现象，还通过数学证明解释了内在奖励为何会导致分布尖锐化。
*   **实用指标**：提出的“模型崩溃步数”为预估模型在强化学习下的表现提供了低成本的诊断工具。
*   **分类清晰**：为混乱的无监督 RL 领域建立了清晰的分类学（内在 vs 外部）。

### 8. 不足与局限
*   **任务局限性**：实验主要集中在数学和代码等**可验证任务**上，对于创意写作、开放式对话等难以自动验证的领域，该结论的适用性尚待验证。
*   **外部奖励的门槛**：虽然论文推崇外部奖励，但构建高效的外部验证器（如 Lean 形式化、复杂的代码沙箱）本身具有极高的技术门槛。
*   **模型规模限制**：虽然测试了多个模型，但最高只到 8B 规模，超大规模模型（如 70B+）是否能通过极强的先验抵御内在奖励的崩溃仍存疑问。

（完）
