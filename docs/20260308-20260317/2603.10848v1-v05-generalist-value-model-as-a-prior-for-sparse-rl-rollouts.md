---
title: "$V_{0.5}$: Generalist Value Model as a Prior for Sparse RL Rollouts"
title_zh: "$V_{0.5}$：通用价值模型作为稀疏强化学习 Rollout 的先验"
authors: "Yi-Kai Zhang, Yueqing Sun, Hongyan Hao, Qi Gu, Xunliang Cai, De-Chuan Zhan, Han-Jia Ye"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10848v1"
tags: ["query:rl-tl"]
score: 10.0
evidence: 具有可验证奖励的强化学习（RLVR）价值模型
tldr: "针对可验证奖励强化学习（RLVR）中优势基准构建的挑战，本文提出 V0.5 模型。它将预训练通用价值模型作为先验，与稀疏采样得到的经验均值进行自适应融合。通过实时假设检验和动态预算分配，有效平衡了采样的高方差与模型的系统偏差。在极低采样率下，该方法显著降低了基准估计的均方误差，在多个数学推理任务中性能提升超10%且收敛更快。"
selection_source: fresh_fetch
motivation: 旨在解决强化学习中稀疏采样导致的高方差以及预训练价值模型可能存在的系统偏差问题。
method: 提出一种自适应融合机制，利用实时统计检验评估先验可靠性，并动态分配采样预算以最小化基准估计的均方误差。
result: "在六个数学推理基准测试中，V0.5 的表现显著优于 GRPO 和 DAPO，实现了更快的收敛速度和超过 10% 的性能提升。"
conclusion: V0.5 证明了将通用价值模型作为先验并结合动态采样策略，是提升稀疏强化学习效率和稳定性的有效途径。
---

## 摘要
在具有可验证奖励的强化学习（RLVR）中，构建鲁棒的优势基准（advantage baseline）对于策略梯度至关重要，它能有效引导策略模型强化目标行为。最近的研究引入了通用价值模型（如 $V_0$），通过在上下文中显式编码模型能力来实现预训练的价值估计，从而无需在更新策略模型的同时同步更新价值模型。在本文中，我们提出了 $V_{0.5}$，它自适应地融合了此类价值模型预测的基准（作为先验）与来自稀疏采样（sparse rollouts）的经验均值。这构建了一个在计算效率与极低方差之间取得平衡的鲁棒基准。具体而言，我们引入了实时统计检验和动态预算分配，以平衡稀疏采样引起的高方差与价值模型先验中固有的系统偏差（或幻觉）。通过构建假设检验来实时评估先验的可靠性，系统可根据需求动态分配额外的采样预算。该机制最小化了基准估计器的均方误差（MSE），即使在组大小为 4 的极端稀疏情况下，也能保证稳定的策略梯度。在六个数学推理基准测试上的广泛评估表明，$V_{0.5}$ 显著优于 GRPO 和 DAPO，实现了更快的收敛速度和超过 10% 的性能提升。

## Abstract
In Reinforcement Learning with Verifiable Rewards (RLVR), constructing a robust advantage baseline is critical for policy gradients, effectively guiding the policy model to reinforce desired behaviors. Recent research has introduced Generalist Value Models (such as $V_0$), which achieve pre-trained value estimation by explicitly encoding model capabilities in-context, eliminating the need to synchronously update the value model alongside the policy model. In this paper, we propose $V_{0.5}$, which adaptively fuses the baseline predicted by such value model (acting as a prior) with the empirical mean derived from sparse rollouts. This constructs a robust baseline that balances computational efficiency with extremely low variance. Specifically, we introduce a real-time statistical testing and dynamic budget allocation. This balances the high variance caused by sparse sampling against the systematic bias (or hallucinations) inherent in the value model's prior. By constructing a hypothesis test to evaluate the prior's reliability in real-time, the system dynamically allocates additional rollout budget on demand. This mechanism minimizes the baseline estimator's Mean Squared Error (MSE), guaranteeing stable policy gradients, even under extreme sparsity with a group size of 4. Extensive evaluations across six mathematical reasoning benchmarks demonstrate that $V_{0.5}$ significantly outperforms GRPO and DAPO, achieving faster convergence and over some 10% performance improvement.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《$V_{0.5}$: Generalist Value Model as a Prior for Sparse RL Rollouts》** 的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
在大型语言模型（LLM）的强化学习（RLVR）阶段，构建一个鲁棒的**优势基准（Advantage Baseline）**对稳定训练至关重要。
*   **现有挑战：** 
    *   **GRPO（经验均值法）：** 依赖在线采样的均值作为基准。在长推理任务中，由于计算成本高，采样通常很稀疏（Sparse Rollouts），导致估计值方差极大，训练不稳定。
    *   **PPO（参数化价值模型）：** 需要同步训练一个 Critic 模型，计算开销大且存在耦合风险。
    *   **$V_0$（通用价值模型）：** 虽然能提供零梯度更新的价值预测，但作为静态先验，在面对分布外（OOD）数据时容易产生“幻觉”或系统偏差。
*   **研究动机：** 如何安全地将通用价值模型的先验知识与稀疏的在线采样数据融合，既利用先验降低方差，又利用采样纠正先验偏差？

### 2. 方法论：核心思想与关键技术
$V_{0.5}$ 提出了一个自适应基准估计和预算分配框架，核心包含两个机制：
*   **经验收缩融合（Empirical Shrinkage Fusion）：**
    *   **核心公式：** 基准 $\mu^*$ 是经验均值 $\bar{v}_k$ 和先验预测 $V$ 的加权组合：$\mu^* = w \cdot \bar{v}_k + (1-w) \cdot V$。
    *   **自适应权重：** 通过最小化均方误差（MSE）推导出最优权重 $w^*$，该权重取决于采样噪声方差和先验偏差。
    *   **实时假设检验：** 使用正部分截断函数（Positive-part truncation）评估先验的可靠性。如果采样结果与先验一致，则重用先验以抑制方差；若检测到严重冲突（暗示先验幻觉），则自动切换到经验均值。
*   **顺序 OSLA 预算分配（Sequential One-Step-Look-Ahead Allocation）：**
    *   **动态采样：** 系统不再使用固定的采样组大小，而是根据实时评估的偏差，决定是提前停止采样以节省算力，还是增加采样次数以纠正先验偏差。
    *   **数学保证：** 证明了该动态停止规则在渐近意义下是帕累托最优的。

### 3. 实验设计
*   **数据集/场景：** 专注于复杂的数学推理任务，涵盖 6 个主流基准测试：AIME 2024、AIME 2025、Olympiad Bench、MATH500、Minerva Math、AMC 2023。
*   **基准模型（Backbone）：** Qwen3-4B-Instruct-2507。
*   **对比方法（Baselines）：** 
    *   **GRPO：** 深度求索（DeepSeek）提出的组相对策略优化。
    *   **DAPO：** 另一种大规模 LLM 强化学习系统。
*   **评估指标：** 准确率（Accuracy %）和收敛速度。

### 4. 资源与算力
*   **通用价值模型（$V_0$）增强训练：** 使用了 128 张 GPU，训练时长约 40 小时。
*   **强化学习（RL）训练：** 在 4 个节点（共 32 张 GPU）上进行，使用 sglang 推理引擎。
*   **数据规模：** $V_0$ 训练使用了约 42.4 万个高质量训练对；RL 阶段的 Prompt Batch Size 为 512。

### 5. 实验数量与充分性
*   **实验规模：** 论文在 6 个不同的数学推理数据集上进行了完整测试，并展示了训练全过程的收敛曲线。
*   **消融与对比：** 
    *   对比了不同稀疏程度（组大小 $k=1, 2, 4, 8$）下的表现。
    *   分析了梯度范数（Gradient Norm）和策略熵（Policy Entropy）的变化，证明了 $V_{0.5}$ 在降低方差和保持探索能力方面的优势。
*   **客观性：** 实验通过调整 Batch Size 确保了在相同总算力（Total Compute）下进行公平对比。

### 6. 主要结论与发现
*   **性能卓越：** $V_{0.5}$ 在所有基准测试中均显著优于 GRPO 和 DAPO，最终准确率提升超过 10%。
*   **收敛更快：** 引入价值先验后，模型在训练初期就能获得高质量的优势信号，大幅缩短了达到目标性能所需的时间。
*   **稳定性强：** 相比 GRPO，$V_{0.5}$ 的梯度范数更低且更平稳，有效避免了稀疏采样导致的梯度爆炸或训练崩溃。
*   **极端稀疏可用性：** 即使在组大小仅为 4 的情况下，$V_{0.5}$ 依然能保持稳定的策略梯度更新。

### 7. 优点与亮点
*   **理论完备：** 论文提供了详尽的数学证明（见附录），包括 MSE 的正交分解、偏差边界分析以及动态分配的后悔边界（Regret Bound）。
*   **解耦价值模型：** 延续了 $V_0$ 的思路，无需在 RL 过程中同步训练 Critic，极大地节省了显存和计算开销。
*   **动态效率：** OSLA 机制实现了“按需采样”，在保证精度的前提下优化了推理成本。

### 8. 不足与局限
*   **依赖预训练先验：** $V_{0.5}$ 的效果高度依赖于底层通用价值模型 $V_0$ 的质量。如果 $V_0$ 在某些全新领域表现极差，动态采样可能会频繁触发最大预算，导致效率优势下降。
*   **奖励形式限制：** 目前主要针对可验证的二元奖励（Binary Rewards, -1/1），对于更复杂的连续奖励或过程奖励（PRM）的适用性尚待进一步验证。
*   **最小采样约束：** 理论分析指出，为了保证统计检验的有效性，初始采样组大小不能低于 4，这在极度受限的场景下仍有优化空间。

（完）
